<div id=toc></div>

# Table of Contents

- [cs.DC](#cs.DC) [Total: 16]
- [cs.AR](#cs.AR) [Total: 7]
- [cs.PF](#cs.PF) [Total: 1]
- [cs.NI](#cs.NI) [Total: 8]


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [1] [The Internet of Collaborating Things: Agentic Edge AI for Autonomous Cross-Domain Collaboration](https://arxiv.org/abs/2609.10542)
*Walid A. Hanafy,Nader Sehatbakhsh,David Irwin,Mani Srivastava,Prashant Shenoy*

Main category: cs.DC

TL;DR: 这是一篇立场论文，提出"协作物联网"(IoCT)范式：让海量异构IoT设备以横向对等方式直接协作、池化算力/感知/执行资源，而非纵向卸载到云边服务器；其可行性依赖可移植安全执行基座 + 具备情境推理、瞬时信任建立与开放世界自适应能力的Agentic边缘AI控制面。


<details>
  <summary>Details</summary>
Motivation: IoT正走向万亿美元级、跨域部署的万亿设备规模，设备本身已演变为具备嵌入式算力与端侧智能服务的移动平台。现有"设备→云/边服务器"的纵向卸载范式受限于中心化基础设施的带宽、时延、可扩展性与隐私成本，无法随设备规模线性/超线性增长。因此需要横向范式：设备间直接通信与协作，把算力、感知、执行资源汇集成动态、跨域的设备集群。

Method: 属立场/愿景论文，无系统实现与实验。作者做三件事：(1) 定义IoCT这一新的通信、计算与协作范式，刻画从vertical offloading到horizontal device-to-device collaboration的范式转变；(2) 论证Agentic Edge AI在其中的核心作用——覆盖设备协作全生命周期(发现、协商、编排、执行、退出)的情境推理、瞬态信任建立、开放世界自适应，且无需人工干预；(3) 提出实现该愿景所需的两大支柱：面向异构硬件的可移植且安全的执行基座(execution substrate)，以及agentic控制面；并给出研究挑战与未来方向。

Result: 无实证结果。产出为概念框架、范式定义与开放问题清单，指向可信、自治的IoCT协作。

Conclusion: 中心化云边卸载不可扩展，横向设备协作是万亿级IoT的必然演进方向；其落地瓶颈不在通信协议本身，而在异构设备上的安全可移植执行环境与自治的agentic控制面。可信与自治是该范式能否成立的前提。

Abstract: The Internet of Things is on a trajectory toward a trillion connected devices deployed across multiple domains. These devices are no longer simple sensing and actuation endpoints; they are mobile platforms with embedded processing and intelligent on-device services. The dominant paradigm of offloading computation to cloud and edge servers is a vertical device-to-server interaction model that cannot scale with this trajectory. What is needed instead is a horizontal paradigm in which devices communicate and collaborate directly, pooling their compute, sensing, and actuation into dynamic, cross-domain clusters rather than offloading to centralized infrastructure. We refer to this paradigm as the Internet of Collaborating Things (IoCT). Realizing this vision requires both a portable and secure execution substrate for heterogeneous hardware and an agentic control plane capable of contextual reasoning, transient trust establishment, and open-world adaptation throughout the device collaboration lifecycle without human intervention. In this position paper, we define this new communication, compute, and collaboration paradigm, articulate the role of agentic edge AI in realizing it, and outline research challenges and future directions toward trustworthy, autonomous IoCT collaboration.

</details>


### [2] [MUC-FL: Block-Wise Marginal Utility Contribution for Communication-Efficient Federated Learning](https://arxiv.org/abs/2609.10545)
*Akshay Mhatre,Vikram Karthick,Deepti Gupta,Jia Zou*

Main category: cs.DC

TL;DR: 提出 Block-Wise Marginal Utility Contribution (MUC) 框架，在联邦学习中按"数据块对模型性能的边际贡献"选择性传输，仅发送高价值数据块以削减通信开销，并在多模态 MIMIC 临床数据集上验证了有效性与性能提升。


<details>
  <summary>Details</summary>
Motivation: 联邦学习避免数据集中化，但通信开销高是其核心瓶颈。现有做法多为压缩梯度/模型（量化、稀疏化）或客户端采样，属于"表示层"压缩，并未从信息价值角度判断哪些数据真正值得传输。临床多模态数据中大量样本存在冗余、噪声或不平衡，盲传所有数据块既浪费带宽又可能引入噪声干扰，尤其在欠代表类别上损害模型质量。

Method: 核心是"块级边际效用"度量：将数据划分为块（block），估计每个块对模型性能的边际贡献（marginal utility），只选择性地传输贡献最高的块。框架中结合了基于去重的块选择（deduplication-based block selection）策略，剔除冗余块。评估采用多模态临床数据集，由多个 MIMIC 数据集整合而成，候选块总数 1,135。

Result: 1,135 个候选块中仅 24 个（1.76%）携带有效改进信号；通信量潜在降低 45–50%，同时模型质量保持或提升。基于去重的块选择取得 macro F1 = 0.8566，优于标准联邦优化的 0.8155，且在欠代表类别上改善明显。

Conclusion: 该工作表明联邦学习中的通信冗余主要来自低价值/重复数据块，通过块级效用筛选可实现"传得更少、学得更好"。贡献偏向训练侧数据/通信调度优化与去重机制；其效用评估本身可能带来额外计算与统计估计开销，跨数据集与非 IID 场景下的泛化性、以及"边际贡献"的可靠估计方法仍需进一步验证。

Abstract: Federated Learning (FL) enables distributed model training without centralizing data but suffers from high communication overhead. To address this, we propose Block-Wise Marginal Utility Contribution (MUC), a framework that selectively transmits only the most impactful data blocks based on their contribution to model performance. To evaluate our framework, we apply it to a multimodal dataset integrated from multiple MIMIC clinical datasets and show that only 24 out of 1,135 candidate blocks (1.76%) carry meaningful improvement signals, enabling a potential communication reduction of 45-50% while maintaining or improving model quality. Our deduplication-based block selection achieves a macro F1 score of 0.8566 compared to 0.8155 for standard federated optimization, demonstrating that selective transmission can improve performance, particularly in underrepresented classes.

</details>


### [3] [From Token Interfaces to Token Semantics: A Formal Composition and Conformance Model for Implementation-Neutral Token Specifications](https://arxiv.org/abs/2609.10547)
*John deVadoss*

Main category: cs.DC

TL;DR: 论文将 InterWork Alliance 的 Token Taxonomy Framework (TTF) 形式化为一种类型化语义组合模型，用于实现无关的代币规范，并给出分层一致性模型与参考验证流程，通过案例研究论证代币语义可独立于平台绑定进行规范、比较、验证、映射与治理。


<details>
  <summary>Details</summary>
Motivation: 现有代币标准（ERC-20/721/1155 等）只标准化可调用接口，无法完整刻画代币语义：相同 transfer 接口可能隐含不同的供应、可分性、赎回、取消、证据、治理与生命周期终局性假设；而语义等价的代币又可能落地在不同账本与不同交易模型上。因此需要一个实现中立的语义规范层，以支撑跨平台的代币互操作与一致性校验。

Method: 1) 将 TTF 制品（artifact）、代币公式（token formula）、行为与属性集组合形式化；2) 定义良构性约束（well-formedness）；3) 提出覆盖 formula、artifact、message、state、trace 五层的分层一致性模型；4) 定义参考验证流程，并说明平台中立控制消息如何导出一致性义务与实现测试；5) 通过文档代币、仓单、碳/数字 MRV 三个案例及代表性非法组合进行解析式评估。

Result: 分析表明：代币语义可在不绑定具体平台的前提下被规范、比较、验证、映射和治理；分层一致性模型能够暴露仅靠接口标准无法发现的非法组合与语义冲突，并为控制消息到实现测试的映射提供可操作路径。

Conclusion: 该工作为智能合约平台、许可链与共享状态系统之间的代币互操作提供了语义层基础，有望推动更可靠的跨账本代币工程与合规治理。对基础设施工程而言，其价值主要在于把"代币语义与一致性测试"提升为可复用的规范与验证流水线，而非新的运行时或性能优化。

Abstract: Digital-token standards such as ERC-20, ERC-721, and ERC-1155 have been essential to blockchain adoption because they standardize callable interfaces. Interface standardization, however, does not fully specify token meaning. Two implementations may expose the same transfer function while encoding different assumptions about supply, divisibility, redemption, cancellation, evidence, governance, and lifecycle finality; conversely, two semantically equivalent tokens may be implemented on different ledgers and through different transaction models. This paper presents the InterWork Alliance Token Taxonomy Framework (TTF) as a typed semantic composition model for implementation neutral token specifications. We formalize TTF artifacts, token formulas, behavior and property-set composition, well-formedness constraints, and a layered conformance model covering formula, artifact, message, state, and trace conformance. We further define a reference validation procedure and show how platform neutral control messages can induce conformance obligations and implementation tests. The model is evaluated analytically through document-token, warehouse-receipt, and carbon/digital-MRV case studies, together with representative invalid compositions that interface standards alone do not expose. The analysis shows that token semantics can be specified, compared, validated, mapped, and governed independently of platform binding, providing a foundation for more reliable token interoperability across smart contract platforms, permissioned ledgers, and shared-state systems.

</details>


### [4] [Design and Operation of a Federated GPU Cluster for Digital Humanities within DHinfra.at](https://arxiv.org/abs/2609.10552)
*Florian Atzenhofer-Baumgartner,David Fleischhacker,Max Resch,Lukas Waldhofer,Michael Otto*

Main category: cs.DC

TL;DR: 本文是一篇关于为数字人文（DH）研究构建小型联邦式 GPU 集群的技术报告，核心是一个把联邦身份、项目配额和模型目录映射到自助服务界面的控制平面，通过交互式 Notebook、SSH 和 OpenAI 兼容推理 API 三种界面暴露算力，并给出低预算领域专用集群的建设手册。


<details>
  <summary>Details</summary>
Motivation: 动机：数字人文研究者需要 GPU 算力与大语言模型能力，但通常不具备集群账号、HPC 运维经验和充足预算。作者所在项目需在固定功耗预算、欧盟采购限制和约 1.5 个全职人力（FTE）的极小团队规模下，跨两所大学站点为 DH 社区提供可用算力，因此需要一个能承接联邦身份、配额治理与模型服务的轻量控制平面。

Method: 方法：构建跨两校区的联邦 GPU 集群。接入国家级身份联邦以代理登录；控制平面统一映射联邦身份、按项目配额与模型目录；所有特权操作经由请求-审批队列中介；模型服务采用两层设计，即常驻服务加按需模型换入换出；对外提供 Notebook、SSH 与 OpenAI 兼容 HTTPS 推理 API 三类接口，使无集群账号的研究者也能调用大模型。软件以 Apache 2.0 许可发布。

Result: 结果：系统在固定功耗预算、欧盟范围采购、约 1.5 FTE 团队的现实约束下完成部署与运行；报告记录了优先访问权与利用率之间的关系，并将主要建设决策与调研后未采用的替代方案逐一对照说明。

Conclusion: 结论：该平台验证了低预算、小团队也能交付面向特定领域的联邦 GPU 服务；其控制平面与两层模型服务设计可作为同类集群的参考，并最终凝练为一份面向领域专用 GPU 集群建设团队的操作手册，文档以版本化"活技术报告"形式持续更新。

Abstract: We describe the design, implementation, and operation of a small federated GPU cluster built for Digital Humanities (DH) research within the Austrian DHinfra.at project. The system spans two university sites, brokers logins from a national identity federation, and exposes compute through three interfaces: interactive notebooks, SSH, and an OpenAI-compatible inference API. The core of the platform is a control plane that maps federated identity, per-project quotas, and a model catalogue onto self-service interfaces. We document this control plane in detail, including the request-and-approval queue that mediates every privileged cluster effect and the two-tier model-serving setup, which combines always-on services with on-demand model swapping and lets researchers consume large language models over HTTPS without holding a cluster account. We report the constraints that shaped the build: a fixed power budget, EU-wide procurement, a team of about 1.5 full-time equivalents, and the relationship between prioritized access and utilization. We also record the main build decisions against the alternatives we researched and did not adopt. The platform is released under the Apache License 2.0. We close with a playbook for teams building a domain-specific GPU cluster on a limited budget. This is a living technical report, versioned and updated as the platform evolves.

</details>


### [5] [Memory Profiling and Migration for Heterogeneous Memory Architectures](https://arxiv.org/abs/2609.10554)
*Marios Asiminakis,Polydoros Petrakis,Manolis Marazakis*

Main category: cs.DC

TL;DR: SHAMBLES 是一个内核集成、无需改应用的异构内存（HBM+DDR）页面放置与迁移框架，提供策略无关接口和可插拔策略，在 HPCG、DGEMM、Himeno 上以部分 HBM 容量逼近全 HBM 性能。


<details>
  <summary>Details</summary>
Motivation: HBM 与商用 DRAM 组成的多层内存可加速带宽受限的 HPC 负载，但现有页面放置依赖手工调优或面向单层设计的 OS 启发式策略，难以适应多级内存的动态特性，也无法随访问局部性变化自动调整。目标是在不修改应用的前提下获得性能可移植性。

Method: 在商品化 Linux 系统上将 HBM 与 DDR 暴露为 NUMA 节点；由内核集成层以低开销剖析应用内存访问行为，并在层级间迁移数据。框架提供策略无关接口、轻量用户态 runtime、可插拔策略（如基于 recency 与 frequency 的动态策略）以及用于受控实验的静态放置；另设 logging 模式，记录可复现的分配与迁移时间线以便分析。

Result: HPCG：仅将 40% 问题规模放在 HBM，可达全 HBM 基线的 93.75%。DGEMM：动态策略仅用约 1/3 矩阵占用驻留 HBM，即维持全 HBM 性能的 99%。Himeno：快速层选择需同时感知工作负载与规模；50% 快速层预算下对 L 规模超过全 HBM 与全 DDR 固定放置，而 XL 规模则回归偏向 HBM。

Conclusion: 透明、策略驱动的迁移能响应变化的访问局部性，将热数据自动汇聚到 HBM，无需开发者干预，为分层内存上的性能可移植性提供了可行路径；同时表明快速层容量分配没有通用最优解，需工作负载与规模感知的联合决策。

Abstract: Heterogeneous memory systems that combine high-bandwidth memory (HBM) with commodity DRAM can accelerate bandwidth-bound HPC workloads, but current page placement largely depends on manual tuning or OS heuristics not designed for multi-tier dynamics. We present SHAMBLES, a kernel-integrated framework that profiles application memory behavior at low overhead and migrates data across tiers without requiring application changes. SHAMBLES exposes a policy-agnostic interface and a lightweight user-space runtime with pluggable policies (e.g. recency and frequency based) as well as static placement for controlled studies. A logging mode provides reproducible timelines of allocations and migrations to aid analysis. We implement SHAMBLES on a commodity Linux system with HBM and DDR exposed as NUMA nodes and evaluate it with the HPCG, DGEMM benchmarks and Himeno stencil mini-app. Our design and methodology show how transparent, policy-driven migration can respond to changing access locality and concentrate hot data in HBM without developer intervention, offering a practical path to performance portability on tiered memory. Results from HPCG show that we can maintain up to 93.75% of the all-in-HBM baseline performance, while keeping only 40% of the problem size in the HBM. DGEMM experiments show that dynamic policies in SHAMBLES sustain up to 99% of the all-in-HBM performance, while keeping only one third of the DGEMM matrix footprint in HBM. For Himeno, SHAMBLES shows that fast-tier selection must be both workload-aware and size-aware: with a 50% fast-tier budget, it can outperform fixed all-in-HBM and all-in-DDR placements for the L size, while the XL size shifts back toward HBM.

</details>


### [6] [Composable CXL Memory as a Kubernetes-Native Shared Memory for LLM Serving](https://arxiv.org/abs/2609.10790)
*Hongjian Fan,Kevin Zhang,David Habinsky,Sean Dykstra*

Main category: cs.DC

TL;DR: 该文实现了一个 Kubernetes DRA 驱动，把可组合 CXL 内存抽象为集群级可调度资源，并通过 CDI 让跨节点 Pod 共享同一物理 CXL region；基于此在 vLLM/llm-d 中构建跨节点 KV-cache 复用层（槽目录内嵌于共享介质，无需外部元数据服务）。两节点 512 GiB CXL 设备 + Qwen2.5-7B-Instruct 上，跨节点前缀复用将 TTFT 降低 5.5×–36.6×，外部命中率 95.4–99.5%，跨节点与同节点复用延迟差距仅 1–4%。定位为可行性研究。


<details>
  <summary>Details</summary>
Motivation: LLM 服务中前缀/KV-cache 复用可显著降低 TTFT，但现有缓存层级（GPU prefix caching、CPU-DRAM offload）均受限于节点本地内存，跨节点无法共享，导致不同副本间重复 prefill、回退到全量重算。核心痛点是缺少一种让远端内存成为"可调度、可被多节点同时挂载"的集群资源的机制，以及缺少与之配套的、无需额外元数据服务的共享 KV-cache 索引。论文试图验证：以 CXL 内存为共享介质的跨节点 KV 复用，在延迟上是否接近同节点复用。

Method: 1) 实现 Kubernetes DRA 驱动，按需组合（compose）CXL region，在各参与主机上物化为 DAX 设备（/dev/daxX.Y），并以单一 CDI 名称注入 Pod，使不同节点上的 Pod 访问同一物理区域；2) 为 vLLM/llm-d 编写 shared-memory connector，将该 region 作为一级 KV-cache tier；3) 在共享介质内部嵌入 slot directory（槽目录）做 KV 块寻址，去除对外部元数据服务（如 Redis/etcd）的依赖；4) 两个节点各自运行完整推理引擎副本（非 PD 分离），仅共享 KV 层；5) 以外部命中率、TTFT 和"sharing gap"（跨节点 vs 同节点复用延迟比）为指标，在 Qwen2.5-7B-Instruct 与 512 GiB CXL 设备上做对比。

Result: TTFT 在 95.4–99.5% 外部命中率下降低 5.5×–36.6×；跨节点与同节点复用的延迟比（sharing gap）仅 1–4%；节点本地层级（GPU prefix caching、CPU-DRAM offload）在跨节点场景下无法命中，退化为全量重算。实验规模为两节点、单 512 GiB CXL 设备、7B 模型。

Conclusion: 论文证明将 CXL 内存作为可调度集群资源、用于跨节点 KV-cache 复用是工程可行的，且跨节点复用引入的额外延迟很小，说明内存解耦（memory disaggregation）可作为提升 LLM 服务前缀复用率与 TTFT 的有效路径。作者明确将其定位为可行性研究而非性能评测，尚未覆盖更大集群规模、更高并发、CXL 带宽/故障隔离与调度策略等生产级问题。

Abstract: We present a Kubernetes Dynamic Resource Allocation (DRA) driver that makes composable CXL memory a schedulable cluster resource, and evaluate the resulting shared-memory tier for cross-node KV-cache reuse in LLM serving. The driver composes CXL regions on demand, materializes them as DAX devices on each participating host, and injects them into pods under a single Container Device Interface (CDI) name so that pods on different nodes access the same physical region. A shared-memory connector for vLLM/llm-d uses that region as a KV-cache tier with a slot directory embedded inside the shared medium, which eliminates the need for an external metadata service. On a two-node cluster with a 512\,GiB CXL appliance and Qwen2.5-7B-Instruct, cross-node prefix reuse reduces TTFT by 5.5$\times$--36.6$\times$ at an external hit rate of 95.4--99.5\,\%, while node-local tiers (GPU prefix caching, CPU-DRAM offload) fall back to full recompute. The sharing gap, defined as the latency ratio between cross-node and same-node reuse, is 1--4\%, indicating that cross-node reuse incurs little additional latency relative to same-node reuse on our testbed. Both replicas run full engines; the study demonstrates memory disaggregation rather than prefill/decode disaggregation. We report this as a feasibility study rather than a performance evaluation.

</details>


### [7] [ExaServe: Large-Scale LLM Serving on Exascale HPC Systems](https://arxiv.org/abs/2609.10812)
*Wenyi Wang,Shu Shi,Yadu Babuji,Ian Foster,Kyle Chard*

Main category: cs.DC

TL;DR: ExaServe 是一个可通过 pip 安装的声明式框架，把 YAML 规格转换为可复现的大规模 LLM 服务部署。在 ALCF Aurora 上实现 1→256 节点（3072 个 vLLM 副本）的部署：非流式推理近线性扩展到 256 节点，达 27.1k requests/s（3.8M tokens/s）；但 token 流式受集中式代理限制停在约 4.7k requests/s，且 Ray Serve 控制面存在 O(N²) 瓶颈，256 节点上线耗时约 30 分钟。


<details>
  <summary>Details</summary>
Motivation: 云原生 LLM 服务框架（vLLM、Ray Serve 等）在数据中心已很成熟，但在 leadership-class 超算上部署仍需大量人工工程：调度器集成、MPI 启动、加速器选择、节点本地权重暂存、平台专用补丁。缺乏可复现、可移植的部署路径，导致超算上的 LLM 服务难以规模化和复现，也掩盖了真正阻碍 exascale 服务的关键瓶颈。

Method: 以声明式 YAML 为唯一输入，自动化上述平台适配流程（调度器提交、MPI 启动、加速器/节点绑定、节点本地权重 staging、平台补丁），产出可复现的多节点 LLM 服务部署。在 ALCF Aurora 上进行 1 至 256 节点强扩展实验（3072 个 vLLM 副本），分别测量非流式推理、token 流式推理的吞吐，以及集群 bring-up 时间随节点数的变化，定位控制面开销。

Result: 非流式推理接近线性扩展到 256 节点，吞吐 27.1k requests/s（3.8M tokens/s）；token 流式吞吐在集中式代理处饱和于约 4.7k requests/s，而模型服务器仍处于 SLO 之内，说明瓶颈在代理/数据通路而非推理引擎；Ray Serve 控制面存在 O(N²) 瓶颈，256 节点时集群上线约 30 分钟。

Conclusion: ExaServe 提供了在超算上实用且可复现的 LLM 服务部署路径，同时系统性暴露了面向 exascale LLM 服务的三类屏障：集中式流式代理的单点吞吐上限、Ray Serve 控制面的 O(N²) 扩展性、以及大规模 bring-up 的分钟级固定开销。对后续超算 LLM 服务系统设计具有直接的工程指导价值。

Abstract: Cloud-native LLM serving frameworks have made deployment routine in data centers, yet deploying them on leadership-class supercomputers remains an engineering challenge requiring scheduler integration, MPI launch, accelerator selection, node-local weight staging, and platform-specific patches. We present \textit{ExaServe}, a pip-installable framework that transforms a declarative YAML specification into a reproducible large-scale LLM serving deployment. Using ExaServe, we deploy LLM serving on ALCF Aurora from 1 to 256 nodes (3072 vLLM replicas). Non-streaming inference scales nearly linearly to 256 nodes, reaching 27.1\,k requests/s (3.8\,M tokens/s). Token streaming scales differently: a centralized proxy plateaus at $\sim$4.7\,k requests/s despite the model servers remaining within the service-level objective. We also identify an \emph{O}(\emph{N}\textsuperscript{2}) Ray Serve control-plane bottleneck that increases cluster bring-up to $\sim$30 minutes at 256 nodes. ExaServe provides a practical, reproducible deployment path while exposing key barriers to future exascale LLM serving.

</details>


### [8] [Entwine: Coordinating Tiled Computation and Fine-Grained Communication across GPUs](https://arxiv.org/abs/2609.11562)
*Kai Ma,Quanfeng Lv,Jingguo Ge,Bowei Dai,Kefan Ruan*

Main category: cs.DC

TL;DR: Entwine通过协调tile计算顺序、细粒度通信与SM资源分配，解决GPU计算-通信重叠中进度失配与资源竞争问题，在张量并行LLM负载上较cuBLAS+NCCL取得1.232x几何平均加速（最高1.433x）。


<details>
  <summary>Details</summary>
Motivation: 现代高性能GPU计算将张量划分为tile以利用数据复用与并行性，单个tile计算完成早于整体张量计算，理论上存在计算-通信重叠机会。但计算与通信的进度失配会限制这些机会：无数据就绪时通信停顿，数据突发到达时通信滞后；同时通信占用共享资源（SM）会拖慢计算，抵消重叠收益。核心矛盾是计算产出与通信消费之间的速率不匹配，以及两者对同一SM资源的竞争。

Method: Entwine从三个维度协同设计：(1) 重排tile计算顺序，使对外通信的数据以更规整、平稳的节奏产出，缓解突发与停顿；(2) 将该调度与基于SM的细粒度通信耦合，低延迟、低开销地处理tile结果，而非等待粗粒度集合通信；(3) 由于通信kernel同样消耗SM资源，Entwine协调两者的SM分配，在通信推进速度与计算降速之间取得平衡。整体以最小化总完成时间为目标。

Result: 在代表性张量并行LLM工作负载上，Entwine相对cuBLAS+NCCL实现1.232x几何平均加速（最高1.433x），并比最先进的重叠基线在几何平均上优3.1%–9.8%。实现将在论文发表后开源。

Conclusion: Entwine表明，将计算调度、细粒度通信与SM资源分配作为统一的协同优化问题处理，可显著提升GPU上计算-通信重叠效率，对张量并行LLM的分布式训练与推理具有直接的工程价值。

Abstract: Modern high-performance GPU computations partition tensors into tiles to exploit data reuse and parallelism. Individual tile computations complete earlier than the full tensor computation, creating opportunities to overlap computation and communication. However, a mismatch between computation and communication progress can limit these opportunities. Communication stalls when no data is ready, and may lag when data arrives in bursts. Communication can also slow computation by consuming shared resources, offsetting the benefits of overlap.
  We present Entwine, which coordinates tile computation order, fine-grained communication, and SM resource allocation to minimize overall completion time. Entwine reorders tile computation to produce data for communication at a more regular pace. Entwine couples this schedule with fine-grained SM-based communication to process tile results with low latency and low overhead. Since the communication kernel also consumes SM resources, Entwine coordinates their allocation to balance communication progress against computation slowdown. Across representative tensor-parallel LLM workloads, Entwine achieves a geomean speedup of 1.232x (up to 1.433x) over cuBLAS+NCCL, and outperforms state-of-the-art overlap baselines by 3.1-9.8% in geomean. We will open-source our implementation upon publication.

</details>


### [9] [Navigating Small-World Networks with Distance Predictions](https://arxiv.org/abs/2609.10885)
*Ladan Kian,Ming Ming Tan,Dariusz Kowalski*

Main category: cs.DC

TL;DR: 该论文研究"预测辅助的贪心路由"(predicted-greedy routing)：在Kleinberg增广k维格上，转发代理每步依据一个逐步重采样、依赖完整路由历史的带噪(ε,δ)预测，选择使预测距离最小的邻居。作者区分两种观测能力：(1) 具备坐标感知(能精确算格距离、但无法算图距离)，给定图距离的(ε,δ)预测可达期望投递时间O(log n/(1-4kεδ))，渐近优于经典Θ(log²n)；(2) 完全无坐标感知(隐私保护场景)，给定格距离预测仍可在O(n/(1-4kεδ))内到达。结论是"正确类型的少量预测信息"即可突破Kleinberg经典界，且即使节点完全不泄露坐标也能保证可靠投递。


<details>
  <summary>Details</summary>
Motivation: Kleinberg的small-world去中心化贪心路由下界Θ(log²n)长期以来是经典结论，但现实中节点往往能获得关于目标的"预测/提示"信息(如学习型预测器、embedding、缓存热度等)。本文的动机是：量化"带噪预测"能在多大程度上加速去中心化贪心路由，尤其是在隐私敏感、节点不披露坐标的网络中，是否仍能实现可靠投递。这既是对Kleinberg模型的理论扩展，也对应了现实中"预测辅助系统"的设计直觉。

Method: 模型：增广k维格 + 长程捷径。代理每步选取使(ε,δ)预测距离最小的邻居；预测由一个oracle在每步重新采样，且条件于完整路由历史。两种设定：
1) 具备坐标感知：可精确计算格距离，但由于未访问节点的捷径未知，无法计算图距离；因此需要图距离的(ε,δ)预测，这是经典模型不提供的新信息。分析期望投递时间的上界。
2) 完全无坐标感知：节点不披露坐标，无法计算格距离，只能依赖格距离的(ε,δ)预测。分析其期望投递时间。
关键分析工具为误差累积项1-4kεδ，刻画噪声预测在每步决策中的衰减效应。

Result: (1) 坐标感知 + 图距离(ε,δ)预测：期望投递时间O(log n/(1-4kεδ))，在εδ足够小时渐近优于Kleinberg经典的Θ(log²n)。
(2) 完全无坐标感知 + 格距离(ε,δ)预测：期望投递时间O(n/(1-4kεδ))，虽为线性但保证可达，说明零坐标披露下投递依然可靠。

Conclusion: "正确类型的少量预测信息"足以将去中心化路由加速至远低于Kleinberg经典界；而预测的类型必须与代理的观测能力匹配(有坐标时需图距离预测，无坐标时仅能依赖格距离预测，代价是复杂度从对数升到线性)。这表明预测辅助路由的收益取决于"预测对齐"与隐私/信息可用性之间的权衡。对AI基础设施与分布式系统而言，其启示是：轻量的学习型距离/位置预测器可作为路由/调度启发式，在不完全信息下改善可扩展性；但预测偏差(εδ)会以1/(1-4kεδ)的方式直接放大投递时间，因此预测质量与校准是工程落地的核心风险点。

Abstract: The small-world phenomenon was given an algorithmic foundation by Kleinberg, who showed that in an augmented $k$-dimensional lattice a decentralized greedy algorithm delivers a message in $O(\log^2 n)$ expected steps. We study predicted-greedy routing, in which a mobile agent forwarding the message moves at each step to the neighbor minimizing a noisy $(\varepsilon,δ)$-prediction of its distance to the target, redrawn at every step from an oracle conditioned on the full routing history. Two cases arise from what this agent can observe. An agent with the coordinate awareness can still compute lattice distance exactly, but not graph distance in the shortcut-augmented network, since that depends on the shortcuts of nodes it has not yet visited; given an $(\varepsilon,δ)$-prediction of graph distance, information the classical model never supplies, it achieves expected delivery time $O(\log n/(1-4k\varepsilonδ))$, an asymptotic improvement over $Θ(\log^2 n)$. An agent with no coordinate awareness at all, the natural model for a privacy-preserving network whose nodes never disclose their coordinates, cannot compute even lattice distance; given an $(\varepsilon,δ)$-prediction of lattice distance instead, it still reaches the target in $O(n/(1-4k\varepsilonδ))$ expected steps. Together these results show that a modest amount of predicted information, of the right kind, is enough to accelerate decentralized routing well below Kleinberg's classical bound, and that even when nodes reveal no coordinates at all, reliable delivery remains achievable.

</details>


### [10] [Online Treasure Hunt in Vertex-Permuted Dynamic Rings](https://arxiv.org/abs/2609.11013)
*Kamran Ayoubi,Bernard Mans,Lata Narayanan*

Main category: cs.DC

TL;DR: 研究顶点置换动态环(VP)上k个agent的寻宝问题，给出不可解性、可行性、最优搜索时间与竞争比、多agent加速及随机置换(R-VP)下的期望复杂度紧界。


<details>
  <summary>Details</summary>
Motivation: 动态网络的拓扑随时间变化，其中顶点置换动态环(VP)是一类极端但重要的模型：顶点集合不变但每步被重新排列，导致边集持续变化。需要刻画在此类强对抗性拓扑动态下，移动agent团队进行寻宝(treasure hunt，即搜索未知目标)的可行性与复杂度边界，并区分对手强度（oblivious vs adaptive）与扰动受限程度（VP(δ)）。

Method: 理论建模与算法分析：(1) 对无限制置换序列证明k≤n-3时寻宝不可能；(2) 定义VP(δ)：任意顶点对i,j的边(i,j)保证在δ步内出现，证明仅当δ≥⌈(n-1)/2⌉时该类可行；(3) 单agent情形，在δ≥2n下给出最坏搜索时间与任意在线算法竞争比的Θ(δn)紧界；(4) 构造k个agent的最优算法，证得k倍加速；(5) 在R-VP（每步按随机置换成环）下分析期望步数：oblivious对手Θ(n)，adaptive对手Θ(n log n)。

Result: k≤n-3时无限制VP不可解；VP(δ)可行当且仅当δ≥⌈(n-1)/2⌉；单agent在δ≥2n时最坏搜索时间与竞争比均为Θ(δn)且紧；k-agent最优算法实现k倍最坏时间加速；R-VP下期望搜索时间为oblivious对手Θ(n)、adaptive对手Θ(n log n)。

Conclusion: 该工作系统刻画了顶点置换动态环上寻宝的可解性阈值与最优复杂度，明确了δ、agent数量k与对手模型（oblivious/adaptive）对搜索时间的影响，给出紧界与最优算法，为动态网络搜索与移动agent协调提供了理论基准。

Abstract: We study the problem of treasure hunt by a group of $k \geq 1$ agents in vertex-permuted dynamic rings (VP). In this model, the $n$ vertices remain on a ring but are permuted at each time step. We first show that treasure hunt is impossible for any $k \leq n-3$ agents, if there are no restrictions on the sequence of permutations used in the dynamic ring. We then study the $VP(δ)$ setting, in which for every pair $i, j$ of vertices, the edge $(i, j)$ is guaranteed to appear within $δ$ steps. We show that the class $VP(δ)$ is feasible only for $δ\geq \left\lceil \frac{n-1}{2}\right\rceil$. For the one-agent case, we show a tight bound of $Θ(δn)$ on the worst-case search time as well as competitive ratio of any online algorithm for treasure hunt, provided $δ\geq 2n$. We then give an optimal algorithm for $k$ agents, thereby showing that $k$ agents can obtain a speedup of $k$ on the worst-case search time. Finally, in the R-VP setting, in which in every step, the vertices are arranged as a ring according to a random permutation, we show that treasure hunt takes expected $Θ(n)$ steps against an oblivious adversary and $Θ(n \log n)$ steps against an adaptive adversary.

</details>


### [11] [The Computing Channel: How Modulation Programs the Airwaves](https://arxiv.org/abs/2609.11145)
*Saeed Razavikia,Carlo Fischione*

Main category: cs.DC

TL;DR: 本文提出"数字函数导向通信"(digital function-oriented communication),用有限字母表符号与接收端判决的联合设计,让多址叠加直接编码目标函数,避免先恢复各设备消息再聚合,从而弥合模拟OAC与数字通信/有限精度数据之间的鸿沟。


<details>
  <summary>Details</summary>
Motivation: 分布式计算与分布式AI需要频繁交换中间结果,但很多应用只需聚合值而非各设备原始消息。传统系统先逐条恢复消息再聚合,浪费带宽与算力。过空计算(OAC)利用同时传输直接得到聚合,但主流OAC依赖模拟信令,与有限精度数据和数字通信流程不匹配,难以落地于实际数字系统。

Method: 提出数字函数导向通信框架:联合设计有限字母表符号表示与接收端判决规则,使多址叠加本身编码所需函数(如求和/平均),而非承载可独立译出的信息。给出计算星座(computational-constellation)原理,梳理主要设计方法、扩展方向与实现挑战;以联邦边缘学习为场景,直接在量化后的模型更新上运算。

Result: 框架可在数字信令下直接对量化模型更新执行函数计算,减少与用户数相关的承载数据资源开销(用户无关/弱相关的资源占用),实现数字域的函数聚合。

Conclusion: 作为综述/展望类文章,定位函数导向通信为数字OAC的范式:通过星座与判决的联合设计在数字链路上实现聚合计算,为联邦边缘学习等分布式AI提供资源高效的通信-计算协同路径,后续挑战在于星座设计、信道/SIC兼容与量化误差控制。

Abstract: Distributed computing and distributed artificial intelligence require frequent exchanges of intermediate results, although many applications need only an aggregate rather than messages from individual devices. Conventional systems recover each message before computing the aggregate, whereas over-the-air computation (OAC) exploits simultaneous transmission to obtain it directly. However, dominant OAC implementations rely on analog signaling, creating a mismatch with finite-precision data and digital communication procedures. This article presents digital function-oriented communication, in which finite-alphabet symbol representations and receiver decisions are jointly designed so that multiple-access superposition encodes the desired function without recovering individual inputs. We introduce its computational-constellation principle, main design approaches, extensions, and implementation challenges. Federated edge learning illustrates how the framework can reduce user-dependent data-bearing resources while operating directly on quantized model updates.

</details>


### [12] [Can AI Remediate Backend Failures Safely? GuardedAct with Blast-Radius-Aware Sandboxing](https://arxiv.org/abs/2609.11264)
*Wanrong Cai,Tianyu Yu,Shaorui Pi,Xiaoxuan Sun,Wenrui Ma*

Main category: cs.DC

TL;DR: GuardedAct 是一个"沙箱优先"的 LLM 微服务故障修复框架，在 LLM 动作生成器与生产环境之间插入一个爆炸半径感知的验证层，实现"低风险自动执行、高风险人工升级"的安全闭环。在 DeathStarBench 社交网络应用的 5 类注入故障上，恢复率达 87.4%，附带损害从 25.6% 降至 5.2%（降低 79.7%），MTTR 仅增加约 8 秒。


<details>
  <summary>Details</summary>
Motivation: LLM 已能为微服务故障生成修复动作，但直接在生产环境执行 AI 生成的修复存在级联式连带损害风险：单点修复动作可能通过服务依赖图的扇出效应放大影响范围。现有工作聚焦于"生成质量"（诊断准确率、动作合理性），缺乏面向执行阶段的准入控制机制，即如何在保证自动化收益的同时约束失败动作的爆炸半径。

Method: GuardedAct 采用四阶段流水线：(1) 输入摄取——汇聚诊断报告、实时系统拓扑与近期遥测数据；(2) 动作生成——提示 LLM 产出按优先级排序的候选修复动作列表；(3) 数字孪生沙箱仿真——在轻量级 digital-twin 中对每个候选动作做预演，估算爆炸半径并赋予风险标签；(4) 回滚置信度门控——仅对低风险动作自动执行，高风险动作升级至人工复核。评估基于 DeathStarBench social-network 应用注入的 5 类故障场景，并辅以消融实验验证各组件对"安全-速度"权衡的独立贡献。

Result: 整体恢复率 87.4%；连带损害从直接 LLM 执行的 25.6% 降至 5.2%，相对降低 79.7%；代价是沙箱引入的 MTTR 增加约 8 秒。消融研究确认各组件（拓扑/遥测输入、沙箱仿真、回滚置信度门控）对安全-速度权衡均有实质性贡献。

Conclusion: 该工作的系统贡献在于把"LLM 生成修复"从开放式生成问题重构为受控执行问题：通过拓扑感知的预演仿真 + 风险分级门控，将 AI 运维动作的自动化边界显式化，并给出可量化的安全-速度折中曲线。工程层面的启示是：8 秒的 MTTR 增量相对于 4 倍以上的连带损害抑制是可接受的杠杆，且"沙箱 + 置信度门控"这一模式可推广到任何高风险 LLM Agent 写操作场景（配置变更、扩缩容、流量调度），而不仅限于故障修复。

Abstract: Large Language Models (LLMs) have shown promising capabilities in generating remediation actions for microservice failures. However, directly executing AI-generated repair actions in production risks cascading collateral damage. We propose GuardedAct, a sandbox-first remediation framework that interposes a blast-radius-aware verification layer between the LLM action generator and the production environment. GuardedAct operates in four phases: (1) ingesting a diagnosis report together with the live system topology and recent telemetry, (2) prompting an LLM to produce a ranked list of candidate remediation actions, (3) simulating each action in a lightweight digital-twin sandbox that estimates the blast radius and assigns a risk label, and (4) enforcing a rollback-confidence gate that auto-executes only low-risk actions while escalating high-risk ones for human review. We evaluate GuardedAct on five fault scenarios injected into the DeathStarBench social-network application. Experimental results show that GuardedAct achieves an overall recovery rate of 87.4% while reducing collateral damage by 79.7% relative to direct LLM execution (from 25.6% to 5.2%), at the cost of a modest sandbox-induced increase in mean time to recovery (approximately 8 s). Ablation studies confirm that each component contributes meaningfully to the safety-speed trade-off.

</details>


### [13] [Rethinking Sparse Formats for RISC-V: A Hierarchical Approach to High-Performance SpMV](https://arxiv.org/abs/2609.11352)
*Anna Pirova,Anastasia Vodeneeva,Konstantin Kovalev,Alexander Ustinov,Maksim Zagriadskov,Daniil Litvyakov,Arthur Kulik,Evgeny Kozinov,Valentin Volokitin,Iosif Meyerov*

Main category: cs.DC

TL;DR: 该论文面向 RISC-V 平台的 SpMV 内核，系统比较了 8 种稀疏矩阵存储格式的 RVV 1.0 向量化实现，并提出 CSR 的层次化变体 HCSR；实测在 SpacemiT K1/K3 上，格式选择平均带来 1.6x 加速，HCSR 在广泛稀疏矩阵类别中执行时间最短。


<details>
  <summary>Details</summary>
Motivation: SpMV 是线性代数基础内核，也是科学计算中迭代求解器的核心构建块，属于典型内存带宽受限（memory-bound）稀疏算子。RISC-V 向量扩展（RVV 1.0）生态尚缺成熟、开源且多格式可对比的 SpMV 实现，开发者缺乏"哪种存储格式在该架构上更快"的经验性依据，而格式选择对不规则访存与向量利用率的影响极大。

Method: 以 RVV 1.0 intrinsics 实现向量化 SpMV，覆盖 8 种既有稀疏存储格式（如 CSR、COO、ELL、SELL-C-σ、DIA、BSR 等）做横向对比；在此基础上提出 HCSR（Hierarchical CSR），对 CSR 的行/块结构做层次化组织以改善向量长度利用与访存局部性；全部实现封装为开源 C++ 库 RVVLASparse，在 SpacemiT K1、K3 两块 RISC-V 开发板上做计算实验。

Result: 选用合适的存储格式可使 SpMV 平均加速约 1.6x；所提 HCSR 在覆盖广泛稀疏矩阵类别的测试中，执行时间优于其余全部被比较的格式。

Conclusion: 存储格式与硬件向量特性的匹配是 RISC-V 上 SpMV 性能的一阶因素，其收益量级可与向量化本身相当；HCSR 作为轻量 CSR 变体在多数稀疏结构下具备稳健优势，配合开源库 RVVLASparse，可直接被 RISC-V 上的迭代求解器与稀疏算子栈复用，并为后续 autotuning / 格式选择策略提供基线。

Abstract: The sparse matrix-vector multiplication (SpMV) algorithm is a fundamental computational kernel of linear algebra and serves as a building block for numerous applications, primarily iterative solvers for systems of linear equations used in scientific and engineering simulations. This paper compares vectorized implementations of the SpMV algorithm across eight established sparse matrix storage formats and proposes a novel modification of the CSR format, Hierarchical CSR (HCSR), which enhances SpMV performance on RISC-V processors. Our SpMV implementations utilize RVV 1.0 intrinsics and are publicly available as an open-source C++ library named RVVLASparse. Computational experiments conducted on SpacemiT K1 and K3 RISC-V boards demonstrate that selecting an appropriate matrix storage format accelerates SpMV computations by an average of 1.6x, while the proposed HCSR format achieves the shortest execution time among all considered formats across a broad class of sparse matrices.

</details>


### [14] [Taming Bitwise Behavior in GPU Kernels with Tensor Core: Black-Box Reconstruction, Compiler Enforcement, and Static Verification](https://arxiv.org/abs/2609.11356)
*Ziteng Yang,Nicholas J. Riasanovsky,Warren Deng,Vivek Sarkar*

Main category: cs.DC

TL;DR: 该工作系统刻画了 GPU 归约与 GEMM 的位级（bitwise）行为决定因素，提出 GEMM 归约顺序描述符、Triton 中强制平衡树归约的数据布局优化，以及跨 NVIDIA PTX/AMD GCN 的位等价静态检查器，实现可复现且高性能的确定性核函数。


<details>
  <summary>Details</summary>
Motivation: ML 系统对 GPU kernel 的确定性与数值可复现性要求日益提高，但即使"确定性"实现，同一 kernel 仍可能逐位不同。根因是浮点归约顺序，以及部分和精度、FMA、舍入位置等。这些选择可能由手写代码、Triton 等块级语言或 cuBLAS/rocBLAS 等闭源库决定。为性能选的 tile 形状同时决定了算术顺序，可能破坏 batch invariance（批次不变性）。固定顺序最多带来约 20% 性能损失，而现有 autotuner 无法识别哪些配置在位级等价，导致搜索空间浪费。

Method: 1) 提出 GEMM 归约顺序的描述符（含 split-K GEMM 中 K 维切分方式），据此首次对闭源库算术进行黑盒重建，构造的 Triton GEMM 系列在 Blackwell/Hopper 上所有测试用例中与 NVIDIA cuBLAS 位级一致，并在带 fused epilogue 的真实 LLM 形状上达到或超过 torch.compile 性能；2) 在 Triton lowering 中强制平衡树归约，并引入数据布局优化；3) 开发可靠的静态检查器判定已编译 GPU kernel 间的位等价，首次覆盖 NVIDIA PTX 与 AMD GCN，并集成进 Triton autotuner，将搜索限制在单一位置等价类内。

Result: 黑盒重建的 Triton GEMM 在测试用例中与 cuBLAS 完全位级一致；数据布局优化使 GB300 与 H100 上 27 个 kernel 中有 19 个进入自由顺序性能的 10% 以内；静态检查器支持跨 PTX/GCN 的位等价判定，并可将 autotuner 搜索空间收敛到单一位置等价类。

Conclusion: 归约顺序而非仅"确定性与否"才是位级可复现性的关键变量；通过显式描述、可控 lowering 与静态等价检查，可在保持接近自由顺序性能的同时获得跨库、跨编译器的位级一致性与批次不变性。

Abstract: Determinism and numerical reproducibility are increasingly required of GPU kernels in machine learning systems, yet deterministic implementations of the same kernel can still differ bit for bit. Floating-point reduction order is the primary cause, alongside partial-sum precision, fused multiply-add operations, and rounding placement. These choices may be hand-coded, selected by a block-level language such as Triton, or hidden inside a closed-source library such as cuBLAS or rocBLAS. A tile shape chosen for performance therefore also determines the arithmetic, potentially breaking batch invariance. Preserving a fixed order can cost up to 20 percent, while an autotuner cannot identify which configurations are bitwise equivalent.
  We characterize the factors determining the bitwise behavior of reductions and general matrix multiplication (GEMM). First, we introduce a descriptor of GEMM reduction order, including the partitioning of K in split-K GEMM. Using it, we perform the first black-box reconstruction of a closed-source library's arithmetic for bit-level correctness. Our family of Triton GEMMs matches NVIDIA cuBLAS in all tested cases on Blackwell and Hopper. For realistic LLM shapes with fused epilogues, it matches or exceeds torch.compile performance. Second, we enforce balanced-tree reduction during Triton lowering and introduce a data-layout optimization that brings 19 of 27 kernels on GB300 and H100 within 10 percent of free-order performance. Third, we develop sound static checkers for bitwise equivalence between compiled GPU kernels, including the first checker spanning NVIDIA PTX and AMD GCN. Integrated into Triton's autotuner, the checker restricts search to a single bit-equivalence class.

</details>


### [15] [Building py-kvcache: A Performance Characterization of External KV Caching for vLLM with NVMe SSDs](https://arxiv.org/abs/2609.11744)
*Joseph Kanichai,Tiziano De Matteis,Animesh Trivedi*

Main category: cs.DC

TL;DR: py-kvcache：面向vLLM的KV Offload连接器，通过异步直接I/O、有界共享暂存与调度感知预加载，在80k token场景下磁盘加载比LMCache快2.0x，三级缓存下快1.23x，并指出外部KV缓存应按部署环境做准入决策。


<details>
  <summary>Details</summary>
Motivation: 前缀缓存可复用已计算的KV状态降低长上下文LLM请求的TTFT，但短前缀或快GPU场景下重新计算可能比从外部缓存加载更快。作者指出该权衡不仅取决于设备带宽，还受传输粒度、中间内存占用以及传输进入请求调度时机的影响，因此需要系统刻画跨GPU/CPU/NVMe的缓存性价比边界。

Method: 在vLLM上跨GPU/CPU/NVMe三层，使用合成负载、长上下文基准、生产trace刻画前缀缓存与重计算的权衡；据此实现py-kvcache：vLLM KV Offload连接器，具备异步直接I/O、有界共享暂存缓冲、调度器感知预加载（在请求排队等待时即启动磁盘读，与计算重叠）。用LongBench、SCBench及Bailian trace回放评估，并与LMCache、原生vLLM KV Offload对比。

Result: 80k token时磁盘加载比LMCache快2.0x，其中预加载贡献1.34x；三级缓存（GPU+CPU+disk）下比LMCache快1.23x，与原生vLLM KV Offload差距约4%。LongBench和SCBench显示优势可扩展到不规则前缀链与多轮负载。Bailian trace回放在较弱GPU上改善TTFT，但在H100上平均请求已低于盈亏平衡点，仅靠GPU显存即可保留足够前缀。

Conclusion: 外部KV缓存并非普适优化，应视为与具体部署环境（GPU算力、前缀长度分布、访存/IO带宽）相关的准入决策：只有当重计算成本高于外存加载成本时才值得启用。py-kvcache通过异步I/O与调度感知预加载显著缩小了外存缓存与重计算/原生实现的差距，代码已开源。

Abstract: Prefix caching can reduce the time to first token (TTFT) of long-context LLM requests by reusing previously computed key-value (KV) states, but for short prefixes or fast GPUs, recomputation can be faster than loading from an external cache. We characterize this tradeoff in vLLM across GPU, CPU, and NVMe tiers using synthetic workloads, long-context benchmarks, production traces, and find that cache performance depends on transfer granularity, intermediate memory use, and when transfers enter the request schedule, not only on device bandwidth. These findings motivate py-kvcache, a vLLM KV Offload connector with asynchronous direct I/O, bounded shared staging, and scheduler-aware preloading, which starts disk reads while requests are still waiting, overlapping with compute. At 80k tokens, py-kvcache loading from disk is 2.0x faster than LMCache, with preloading contributing 1.34x. With GPU, CPU, and disk caching enabled, it is 1.23x faster than LMCache and within approximately 4% of the native vLLM KV Offload implementation. LongBench and SCBench show that these benefits extend to irregular prefix chains and multi-turn workloads. Bailian trace replays improve TTFT on a weaker GPU, but on an H100 the average request falls below the break-even point and GPU memory alone retains enough prefixes. External KV caching should therefore be treated as a setup specific admission decision. The py-kvcacheimplementation is available at: https://github.com/atlarge-research/py-kvcache.

</details>


### [16] [GPU-CFR: 80x Faster Counterfactual Regret Minimization by Compiling the Game to Static Dataflow and CUDA Graph Replay](https://arxiv.org/abs/2609.11923)
*Boning Li,Longbo Huang*

Main category: cs.DC

TL;DR: GPU-CFR 将博弈树编译为静态 dataflow（扁平边/信息集数组、预计算索引、深度级批处理），把整个 CFR 迭代固定下来，仅求解器状态在迭代间变化；配合 CUDA Graph Replay 单次图启动，在 A100 上比最快 GPU CFR 快 29.8–80.4x，比 LiteEFG CPU 实现快 14–258x。


<details>
  <summary>Details</summary>
Motivation: CFR 是少数仍在 CPU 上快于 GPU 的大规模数值负载：每次迭代要遍历上至数十亿状态，通过通用树接口发出数百万个细碎且相互依赖的 gather/scatter。在 GPU 上每个 kernel 仅数微秒，kernel launch 与框架 dispatch 成为瓶颈，导致此前 GPU 实现普遍跑不过优化 CPU 代码。

Method: 核心洞察：对固定博弈，CFR 迭代中除数值外的一切在第一轮迭代前已知。据此构建编译器 + 运行时：(1) 一次编译成静态 dataflow——扁平 edge/information-set 数组、预计算索引、深度级批量 pass，固定整个算子序列；(2) 静态 chance folding、depth-level 执行块、dual-lane reach buffer，最多减少 18.1x 框架操作数；(3) 形状/索引/缓冲区地址不变，故用 CUDA Graph Replay 录制一次迭代并以单次图启动重放。

Result: 在 A100 上，8 个游戏（纸牌、骰子、棋盘）套件中比同一加速器上最快 GPU CFR 快 29.8–80.4x；在四个最大游戏中比 LiteEFG 快 14–258x。编译表示贡献了主要收益：8 线程纯 CPU、无加速器时已比 GPU baseline 快 2.2–51.1x。CPU 优化路径按位复现参考 iterates，树构建与图捕获在首次求解内即回本；在未改变更新规则的前提下，中大型游戏全面击败所有 CPU/GPU baseline。

Conclusion: 该工作把 CFR 从「通用树接口 + 动态调度」转变为「一次性编译的静态 dataflow + 图重放」，用编译期知识消除 GPU 上的 launch/dispatch 开销，证明迭代结构完全可静态化的工作负载即便在 CPU 上也因编译表示而大幅提速。对系统界的启示：对固定结构的迭代型 workload，precompilation + graph replay 是绕开细粒度 kernel 调度税的有效范式。

Abstract: Counterfactual regret minimization (CFR) is one of the few large numerical workloads that still runs faster on CPUs than on GPUs. Each iteration sweeps a game tree with up to billions of states in millions of small, interdependent gather and scatter steps issued through a generic tree interface. On a GPU every kernel finishes in microseconds, so kernel launches and framework dispatch dominate the run time, and prior GPU implementations have lost to optimized CPU code. We observe that for a fixed game, everything about a CFR iteration except the numerical values is known before the first iteration runs. We propose GPU-CFR, a compiler and runtime built on this observation. It compiles any game once into static dataflow: flat edge and information-set arrays, precomputed indices, and depth-level batched passes fix the entire operation sequence, and only solver state changes between iterations. Static chance folding, depth-level execution blocks, and a dual-lane reach buffer cut the number of framework operations by up to 18.1x. Because shapes, indices, and buffer addresses never change, CUDA Graph Replay records the iteration once and replays it with a single graph launch. On one A100, across an eight-game suite that spans card games, dice games, and board games, GPU-CFR runs 29.8--80.4x faster than the fastest prior GPU CFR on the same accelerator, and 14--258x faster than LiteEFG, one of the fastest open-source CPU implementations, on the four largest games. The compiled representation carries most of that margin: on eight CPU threads with no accelerator it is already 2.2--51.1x faster than the GPU baseline. On the CPU the optimized path reproduces the reference iterates bitwise, and tree construction and graph capture pay for themselves within the first solve. GPU-CFR beats every CPU and GPU baseline on the mid-to-large games of the suite without changing the update rule.

</details>


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [17] [REACH: Controller-Managed Long-Span ECC for HBM AI Inference](https://arxiv.org/abs/2609.10861)
*Rui Xie,Yunhua Fang,Asad Ul Haq,Linsen Ma,Sanchari Sen,Swagath Venkataramani,Liu Liu,Tong Zhang*

Main category: cs.AR

TL;DR: REACH 是一种 HBM 控制器微架构，利用 LLM decode 阶段读主导、写稀疏的访存特征，通过"内码纠错+长外码擦除修复"的分层 ECC 方案，在不牺牲 32B 事务粒度和高带宽的前提下，以显著更低的面积与功耗提供更强的 HBM 保护。


<details>
  <summary>Details</summary>
Motivation: HBM 成本高，需要更强的控制器级 ECC 以覆盖更宽的设备错误率区间。长跨度（long-span）纠错码在同码率下保护更强，但直接实现会把小粒度访问与跨整个跨度的状态耦合，并且需要在 HBM 带宽下进行昂贵的解码，导致带宽与面积/功耗代价过高。LLM decode 以顺序读为主，天然适合跨度聚合；写稀疏则限制奇偶校验更新的写放大。

Method: REACH 采用分层 ECC：内码（成熟短码）负责纠正常见错误并标记"未解决 chunk"，长外码仅用于对已知擦除（known erasure）进行修复，从而把昂贵的长跨度解码限制在极少数的异常请求上。差分奇偶校验（differential parity）约束奇偶写流量。端点协同设计使 32B 事务无需额外数据 burst。用 Ramulator2 做带宽与错误压力仿真，用 ASAP7 综合的 kernel 进行接口尺寸与功耗/面积建模。

Result: Ramulator2 在最高错误压力下维持 1.88 TB/s 应用流量；在单独的全接口尺寸分析中，支持 2.69 TB/s 的应用目标。在该分析目标下，REACH 标称配置相比所评估的均值工作量直接长码设计，控制器面积减少 55.8%、建模功耗减少 57.7%。

Conclusion: 把长跨度恢复保留给异常请求，是在 HBM 上实现强纠错同时保持带宽、面积与功耗可接受的关键设计原则；对读主导、写稀疏的 LLM 推理负载尤为有效。

Abstract: High-Bandwidth Memory (HBM) cost motivates stronger controller protection that can support a wider range of device error rates. Long-span error-correcting codes provide stronger protection at a comparable code rate, but a direct implementation couples small accesses to span-wide state and requires costly decoding at HBM bandwidth. Read-dominated LLM decode offers a favorable setting: sequential reads support span aggregation, while sparse writes limit parity-update traffic. This paper presents REACH, a controller microarchitecture that uses established inner codes to correct common errors and identify unresolved chunks, reserving a long outer code for known-erasure repair. Differential parity bounds write traffic, and a co-designed endpoint preserves 32\,B transactions without an extra data burst. Ramulator2 sustains 1.88\,TB/s of application traffic at the highest error stress, while separate full-interface sizing supports a 2.69\,TB/s application target using ASAP7-synthesized kernels. At this analytical target, REACH's nominal composition uses 55.8\% less controller area and 57.7\% less modeled power than the evaluated mean-work direct-long design, showing the benefit of reserving long-span recovery for exceptional requests.

</details>


### [18] [Fengshui: Demystifying Chiplet Ecosystem and Bespoke Neural Network Accelerator Codesign](https://arxiv.org/abs/2609.10970)
*Haoran Jin,Jirong Yang,Zhiheng Zhang,Justin Shin,Barry Lyu,Kangqi Zhang,Yunpeng Liu,Nathan Bleier*

Main category: cs.AR

TL;DR: Fengshui：一个 chiplet 生态与加速器协同设计框架，通过算子级解耦 + chiplet 池组合联合优化，仅用 8 个 chiplet 即可逼近无约束异构定制设计的能效与成本表现，并覆盖数据中心 MoE/dense LLM 服务与边缘自动驾驶感知场景。


<details>
  <summary>Details</summary>
Motivation: 现代 ML 负载的延迟与能耗约束日益严苛，同构通用硬件难以高效承载。作者主张按算子做"解耦"（operator-level disaggregation），为每个算子定制微架构、批处理策略和存储层次；但由此产生的全定制加速器 NRE（一次性工程）成本过高。基于 chiplet 的集成可将 NRE 摊薄到多应用中，然而"造哪些 chiplet、如何拼装成加速器"存在循环依赖：chiplet 池的价值取决于所构建的加速器，而加速器质量又受限于已有 chiplet。

Method: Fengshui 将 chiplet 池组成与定制 ASIC（称为 BASIC）设计放在同一框架内联合优化。它通过算子级解耦构建 BASIC，协同探索 chiplet 异构性与存储异构性、张量融合（tensor fusion），以及流水线/张量/专家并行（pipeline/tensor/expert parallelism），并用 place-and-route 验证保证物理可实现性。chiplet 池中只需 8 个战略性选取的 chiplet，涵盖网络交换、存内计算（PIM）单元以及多种微架构的加速器。

Result: 相比同构加速器，Fengshui 生成的 BASIC 在 energy、energy-cost（EC）、energy-delay（EDP）、energy-delay-cost（EDPC）上分别降低 48.5%、88.1%、93.0%、97.8%；在多种神经网络上，与无约束异构设计的差距在 4.1% 以内。数据中心场景下，MoE 与 dense LLM 服务的 prefill 能耗与 EC 最多降低 16.8% 与 28.7%；边缘自动驾驶感知在实时延迟约束下实现 12.0% 能耗与 23.6% EC 的降低。

Conclusion: 该工作把 chiplet 池选择与加速器架构设计从"循环依赖"转化为可联合求解的协同优化问题，证明小规模、策略性选取的 chiplet 池即可支撑跨应用的高能效定制加速器，为 ML 系统在能效/成本/延迟多维约束下提供了一条可摊销 NRE 的实现路径。

Abstract: Modern ML workloads, with stringent latency and energy constraints, are increasingly hard to run efficiently on homogeneous commodity hardware. We argue that operator-level disaggregation--tailoring microarchitecture, batching, and memory hierarchy to each operator--is essential to overcome these limitations, though the resulting highly bespoke accelerators incur prohibitive Non-Recurring Engineering (NRE) costs. Chiplet-based integration amortizes NRE across applications, but choosing which chiplets to build and how to compose them into accelerators is circularly dependent--a chiplet pool's value depends on the constructed accelerators, while accelerator quality is constrained by available chiplets. This paper introduces Fengshui, a chiplet ecosystem and accelerator co-design framework that jointly optimizes chiplet pool composition and bespoke application-specific integrated circuit (BASIC) design. Fengshui constructs BASICs through operator-level disaggregation, co-exploring chiplet and memory heterogeneity, tensor fusion, and pipeline/tensor/expert parallelism with place-and-route validation for physical implementability. With just 8 strategically selected chiplets, encompassing network switches, processing-in-memory units, and accelerators with diverse microarchitectures, Fengshui-generated BASICs achieve 48.5%, 88.1%, 93.0%, and 97.8% reductions in energy, energy-cost product (EC), energy-delay product (EDP), and energy-delay-cost product (EDPC) over homogeneous accelerators, while scoring within 4.1% of unconstrained heterogeneous designs across diverse neural networks. For datacenter MoE and dense LLM serving, Fengshui reduces prefill energy and EC by up to 16.8% and 28.7%, respectively; for edge autonomous vehicle perception, it achieves 12.0% energy and 23.6% EC reductions under real-time latency constraints.

</details>


### [19] [BEACON: A Versatile Accelerator for Computational Pathology Applications](https://arxiv.org/abs/2609.11044)
*Sumanth Gudaparthi,Ananth Krishna Prasad,Lin Jia,Rajeev Balasubramonian,Srinivasan Parthasarathy*

Main category: cs.AR

TL;DR: 提出"AI+X"路线：以基线AI加速器为底座，用最小增量逻辑支持新领域算子，避免为小众领域从零造芯。以计算病理学为验证场景，在脉动阵列AI芯片上增加特征聚合、负载均衡、欧氏距离、分箱与计数器聚合等机制，得到BEACON。芯片面积仅增1.1x，但在计算病理多阶段流水线上吞吐比基线CPU/GPU高一个数量级以上。


<details>
  <summary>Details</summary>
Motivation: AI加速器已商业化成功，但其他专用领域难以复制该成功：新领域算子多样、市场规模不足以支撑专用芯片的研发与流片成本。作者主张应复用高量产AI芯片的经济性，通过少量逻辑扩展而非全新架构来覆盖新领域。计算病理学是合适的多阶段、多算子测试场景：全切片图像分割与特征提取→kNN构图→迭代GCN推理（Aggregation/Combination交替），现有CPU、GPU、AI及GCN加速器在其上都执行低效。

Method: 1) 对基线CPU、GPU、AI加速器、GCN加速器在计算病理各阶段进行性能剖析，定位低效来源（访存受限、算子在脉动阵列上不可映射、GCN不规则的邻居聚合导致负载不均）。2) 软件重构：将各阶段算子重写为可映射到脉动阵列的形式，减少中间数据搬运。3) 硬件最小扩展：在基线脉动AI加速器中加入PE间灵活数据通路与寄存器访问机制，新增特征聚合、负载均衡执行、欧氏距离计算、分箱（binning）、计数器聚合五类支持，使kNN构图与GCN的Aggregation/Combination可直接在阵列上执行。

Result: 额外灵活性与逻辑仅使基线AI chiplet面积增长1.1x；通过规避内存墙并获得高并行度，BEACON在计算病理工作负载上吞吐相比基线CPU与GPU平台提升超过一个数量级。

Conclusion: "AI+X"是一条降低新领域加速器门槛的可行路径：不追求领域专用芯片，而是以高量产AI芯片为基座做极小面积代价的算子扩展，即可同时服务主流AI与新兴专业领域，兼顾规模经济与专用性能。对工程实践的启示是加速器设计应优先考虑可重配的数据通路与阵列级通信/归约原语，而非为每个新算子新增独立数据路径。

Abstract: While accelerators for AI have seen great commercial success, it is challenging to replicate that success for other specialized domains due to a number of factors. We make the case that barriers for new accelerators can be lowered by starting with a baseline AI accelerator, and adding minimal logic to support new operators demanded by new specialized domains. This leads to a versatile chip that can be manufactured at high volume and deployed for a range of popular applications. We refer to this as the AI+X approach. This paper explores its potential for the emerging domain of Computational Pathology, which involves analysis of large whole-slide tissue images with a multi-stage pipeline. The pipeline requires support for a number of different kernels and operators - early stages perform segmentation and feature extraction, followed by graph creation with k nearest neighbor (kNN) algorithms, and finally inference with an iterative graph convolutional network (GCN) that alternates between Aggregation and Combination. We show that these stages execute inefficiently on a range of baseline CPU, GPU, AI, and GCN accelerators. That inefficiency is addressed with a combination of software re-structuring and small modifications to a baseline systolic AI accelerator. Many of the above kernels can be mapped to a systolic accelerator by offering a flexible datapath between processing elements and register access mechanisms. We add support for feature aggregation, load balanced execution, Euclidean distance calculation, binning, and counter aggregation. This additional flexibility and logic grows the area of a baseline AI chiplet by 1.1x, but by avoiding the memory wall and offering high parallelism, the proposed accelerator BEACON yields over an order of magnitude higher throughput for Computational Pathology than baseline CPU and GPU platforms.

</details>


### [20] [Bio-inspired Learning and Decision-Making with Probabilistic In-Memory Computing Hardware: Part 2](https://arxiv.org/abs/2609.11288)
*Thomas Dalgaty,Eiji Kawasaki,Miguel de Prado,Tommaso Salvatori,Germain Haugou,Eric Flamand*

Main category: cs.AR

TL;DR: 该报告（Part 2）认为大规模概率型能量模型在GPU上的延迟瓶颈并非模型本身不可扩展，而是GPU依赖HBM接口导致低算术强度、大规模并行负载被串行化。作者用事务级模型（TLM）仿真一种概率模拟存内计算（AIMC）处理器，去掉HBM接口、在片上存储器内直接计算，声称同一能量模型可比数据中心级硬件快1000倍以上。


<details>
  <summary>Details</summary>
Motivation: Part 1提出的基于能量的学习与决策模型使用随机Langevin动力学持续演化神经元状态与权重的近似概率分布。GPU实现证实此类大规模概率能量模型延迟过高。作者认为根因是架构错配：低算术强度、大规模并行的模型跑在以HBM为中心、受带宽接口串行约束的GPU上，从而被误判为"不可扩展"。

Method: 方法是构建概率模拟存内计算（AIMC）处理器的事务级模型（TLM），将计算直接放在片上存储器内执行，消除HBM接口，从而消除访存瓶颈对并行度的约束；并在该TLM上运行与Part 1相同的能量模型（含Langevin采样动力学）以评估性能。

Result: 报告称在该TLM下，同一能量模型的执行速度比数据中心级GPU硬件快1000倍以上，并将此归因于去除HBM接口与片上存内计算带来的访存与并行度收益。

Conclusion: 结论是：对这类低算术强度、强并行的概率能量模型，GPU（依赖HBM接口）本身不是可扩展的处理器架构，而非模型不可扩展；存内计算/模拟计算架构能从根本上缓解该瓶颈。该结论目前基于仿真TLM，尚缺实测芯片数据与数值精度、能耗、面积等完整评估。

Abstract: This report extends our previous work (Part 1), which introduced an energy-based model for learning and decision-making under uncertainty. The model leverages stochastic Langevin dynamics to continuously evolve approximate probability distributions over neuron states and model weights. However, as noted in Part 1 and confirmed through GPU-based implementations, large-scale probabilistic energy-based models of this nature face significant scalability challenges due to excessive execution latency. This latency stems from a fundamental mismatch: massively parallel models with low arithmetic intensity (such as energy-based models) are being executed on processor architectures like GPUs that rely on high-bandwidth memory (HBM) interfaces. The HBM imposes brutally sequential execution constraints on inherently parallelizable models, creating the false impression that such models are unscalable. In reality, it is the GPU architecture itself, with its dependence on HBM interfaces, that is not a scalable processor architecture for this class of AI model. In this report, we demonstrate using a detailed transaction-level model (TLM) of a probabilistic analogue in-memory computing (AIMC) processor that the same energy-based model can execute well over 1000x faster than data-center-grade hardware by eliminating the HBM interface and performing computation directly within on-chip memory.

</details>


### [21] [PATTON: Enabling Commodity PIM for Production LLM Serving](https://arxiv.org/abs/2609.11392)
*Hangyeol Kim,Sanghyun Lee,Teokkyu Suh,Joo-Young Kim*

Main category: cs.AR

TL;DR: PATTON 是一个将生产级 LLM 推理引擎与商用 PIM 集成的运行时，通过分层粒度分配、Commit Zone 暂存和布局追踪，解决 PIM 上 KV cache 生命周期管理与 GEMV 效率/单 token 写入/容量的三方冲突，平均加速 1.95x、能效提升 4.83x。


<details>
  <summary>Details</summary>
Motivation: PIM 适合加速 memory-bound 的 decode attention，但仅加速 attention 算子不足以支撑生产级 LLM serving：真实引擎需要对逻辑 KV cache block 进行动态分配、填充、共享、缓存与回收。要在商用 PIM 上支持这一生命周期，必须解决物理内存分配、block-to-address 映射与命令生成问题。对 Value cache 而言，这些需求导致 GEMV 效率、单 token 写入效率与内存容量之间产生根本性冲突：GEMV 优化布局会把新生成的 Value 向量分散到不同 row，导致写入开销高；而更细粒度的内存共享虽提升容量利用率，却会碎片化 GEMV 归约。

Method: PATTON 提出分层 granule 分配：block 粒度的 Key/Value granule 与逻辑 token block 一一对应，从而固定其物理位置与命令；更粗粒度的 granule 将多个 block 聚合，以获得高效 GEMV 执行与内存利用率。引入 Commit Zone 暂存部分填充的 Value block，实现高效单 token 写入，随后再提交到 GEMV 优化位置。运行时追踪这些布局信息，以生成 KV cache 写入及 QK-transpose/SV 命令。关键点是无需修改 PIM 处理单元。

Result: 在 attention 执行与运行时引发的 prefill 重计算两方面，PATTON 相比基线平均取得 1.95x 加速与 4.83x 能效提升；无需对 PIM 处理单元做任何修改；其 KV cache 命中率与 vLLM 原生 GPU KV cache 相当。

Conclusion: PATTON 表明 PIM 加速 LLM serving 的瓶颈不在单个 attention kernel，而在与生产引擎对接的 KV cache 生命周期管理与地址/命令生成。通过分层 granule、Commit Zone 与布局追踪，它在不改动 PIM 硬件的前提下化解了 GEMV 效率、单 token 写入效率与容量之间的矛盾。工程上其价值在于提供了一条低成本迁移路径：保留 vLLM 类引擎的 block 抽象与命中率特性，把 PIM 作为后端，并避免 prefill 重计算等运行时开销。

Abstract: Processing-in-Memory (PIM) is promising for accelerating memory-bound decode attention, but attention acceleration alone is insufficient for production LLM serving, where engines dynamically allocate, populate, share, cache, and reclaim logical KV cache blocks. Supporting this lifecycle on commodity PIM requires efficient physical memory allocation, block-to-address mapping, and command generation. For the Value cache, these requirements create a fundamental conflict among GEMV efficiency, single-token write efficiency, and memory capacity: GEMV-optimized layouts scatter newly generated Value vectors across rows, making writes costly, while finer-grained memory sharing improves capacity utilization but fragments GEMV reductions. We present PATTON, a PIM runtime that integrates production LLM serving engines with commodity PIM. PATTON introduces hierarchical granule allocation: block-sized Key and Value granules map one-to-one to logical token blocks, fixing their physical placements and commands, while coarser granules group blocks for efficient GEMV execution and memory utilization. A Commit Zone stages partial Value blocks for efficient single-token writes before committing them to GEMV-optimized locations. PATTON tracks these placements to generate KV cache writes and QK-transpose/SV commands. Across attention execution and runtime-induced prefill recomputation, PATTON achieves an average 1.95x speedup and 4.83x higher energy efficiency over evaluated baselines, requires no PIM processing-unit modifications, and maintains a KV cache hit rate comparable to the native GPU KV cache in vLLM.

</details>


### [22] [CHERI-D Reincarnate: efficient multicore CHERI temporal memory safety through allocation reincarnation (draft version)](https://arxiv.org/abs/2609.11590)
*Yuecheng Wang,Jonathan Woodruff,Simon W. Moore*

Main category: cs.AR

TL;DR: Reinc 是对 CHERI-D 的架构扩展，通过"隔离 ID 而非内存"实现可扩展且高效的时序内存安全（use-after-free 缓解），在常见情况下允许已释放内存立即复用。


<details>
  <summary>Details</summary>
Motivation: 此前工作 CHERI-D 将有限宽度的 generation ID 存放在固定位置，当 ID 耗尽时必须隔离整个对象（allocation slot），导致内存扫描（memory-sweep）频繁、隔离内存开销大。

Method: 核心机制为 allocation reincarnation：当某分配槽的当前 ID 耗尽时，不隔离该内存槽，而是动态为其分配新 ID，被耗尽的是 ID 并被隔离、之后回收，而底层内存保持可立即复用。同时引入一致性 ID 缓存（coherent ID caching），保持完全去中心化的 ID 组织，时序元数据与被保护内存同址存放以保留局部性；为支持多核，用轻量级 reverse-map 和 filter 机制将物理一致性事件连接到虚拟寻址的 ObjID buffer。实现覆盖 CHERI-Toooba（超标量 FPGA 软核）、QEMU、LLVM/Clang 与 CheriBSD 的软硬件协同设计。

Result: 在所评估的工作负载上，Reinc 大幅降低了内存扫描频率与内存隔离开销，同时仅带来较低的性能与硬件开销。

Conclusion: 将隔离粒度从物理内存转移到 ID，可在保持去中心化元数据局部性的同时实现内存即时复用，是一种低开销、可扩展的时序内存安全方案。

Abstract: We propose CHERI-D Reincarnate (Reinc), an architectural extension to CHERI for scalable and efficient temporal memory safety. Prior work CHERI-D has a finite-width generation ID stored at a fixed location, requiring an object to be quarantined when its ID is exhausted. Reinc further provides use-after-free mitigation while permitting immediate freed memory reuse for objects through allocation reincarnation: rather than quarantining an allocation slot upon ID exhaustion, Reinc dynamically assigns a new ID to that slot when its current ID is exhausted. Exhausted IDs are quarantined and later reclaimed, while the underlying memory remains available for immediate reuse. By quarantining IDs rather than memory, Reinc enables continuous reuse of memory in the common case, substantially reducing both memory-sweep frequency and quarantine memory overhead.
  Reinc further introduces coherent ID caching while retaining a fully decentralized ID organization. Temporal metadata remains colocated with the memory it protects, preserving locality while avoiding centralized metadata structures. To support multicore execution, Reinc connects physical coherence events to the virtually addressed ObjID buffer using lightweight reverse-map and filter-based mechanisms.
  We implement Reinc as a hardware-software co-design spanning CHERI-Toooba (superscalar FPGA softcore), QEMU, LLVM/Clang and CheriBSD. Across our evaluated workloads, Reinc substantially reduces memory-sweep frequency and memory quarantine while incurring low performance and hardware overhead.

</details>


### [23] [AccelForge: Comprehensive Modeling and Co-Design Framework for AI Accelerators](https://arxiv.org/abs/2609.11906)
*Tanner Andrulis,Michael Gilbert,Vivienne Sze,Joel S. Emer*

Main category: cs.AR

TL;DR: AccelForge 是一个统一的张量代数加速器建模框架，将设备、电路、架构、负载建模与映射优化整合到一套可组合、可扩展的高性能 Python 实现中，大幅缩短评估时间。


<details>
  <summary>Details</summary>
Motivation: 张量代数负载（如深度神经网络）在数据中心与边缘部署中能耗极高，必须依赖加速器实现高能效与高吞吐。为快速评估和迭代加速器设计，业界需要能同时刻画设备、电路、架构、负载属性并优化负载到硬件映射的建模框架。

Method: AccelForge 将多项已有工作统一到一个框架，提供三类能力：(1) 可组合、用户自定义且用户可修改的设备/电路/架构模型；(2) 快速 mapper，实现高精度评估并将所需计算时间与人力时间降低数个数量级；(3) 模型与 mapper 均采用易用、易扩展且高性能的 Python 实现，便于快速研究与面向新优化的扩展。

Result: 论文定位为框架性贡献，重点在于能力、速度与易用性上的改进：通过统一整合与可组合建模，把原先分散的工作收敛为一个框架，并借助快速 mapper 将评估时间缩短数个数量级。

Conclusion: AccelForge 面向加速器设计空间探索的迭代效率问题，兼顾建模精度、评估速度与工程可扩展性，为张量代数加速器的快速研究与新优化扩展提供统一基础设施。

Abstract: Tensor algebra workloads, of which deep neural networks are prominent examples, are energy-intensive workloads in modern datacenter and edge deployments, making accelerators necessary to achieve energy efficiency and high throughput. To quickly evaluate and iterate on accelerator designs, we need an accelerator modeling framework that captures salient attributes of devices, circuits, architectures, workloads, as well as optimizing the mapping of the workload onto the hardware.
  In this paper, we introduce AccelForge, which improves upon existing accelerator modeling frameworks in capabilities, speed, and ease-of-use. AccelForge unifies and multiple works into one framework, and it includes (1) composable user-defined and user-modifiable models of devices, circuits, and architectures, (2) fast mappers that enable accurate evaluation in orders of magnitude less (computer and human) time, and (3) easy-to-use and easy-to-extend, yet still high performance, Python implementations of both the model and mapper to enable rapid research and extension to novel optimizations.

</details>


<div id='cs.PF'></div>

# cs.PF [[Back]](#toc)

### [24] [Compass: Dissecting Communication and Computation Operators for Efficient LLM Training](https://arxiv.org/abs/2609.10549)
*Guangyu Xiang,Lin Zhang,Haoxuan Yu,Xinglin Pan,Shaohuai Shi,Xiaowen Chu*

Main category: cs.PF

TL;DR: Compass 通过双环通信的 IntraFusion 算法、张量分解度数学建模与统一策略选择框架，在多 GPU（NVLink-PCIe 混合拓扑）上最大化通信/计算重叠的带宽利用率，端到端较 Megatron-LM 最高加速 1.42x。


<details>
  <summary>Details</summary>
Motivation: LLM 分布式训练中，重叠通信与计算是隐藏通信开销的关键手段。现有两类做法各有缺陷：IntraFusion（算子内融合，把多个算子打包成单个大 kernel）没有充分利用网络拓扑，在异构多 GPU 系统上带宽利用率不足；InterDecom（算子间分解，把张量切分并流水化）缺乏确定最优分解份数的依据，难以达到峰值性能。二者之间也缺少统一的选择准则。

Method: 1) 提出基于双环（double-ring）通信的 IntraFusion 算法，针对 NVLink-PCIe 混合拓扑提升带宽利用率；2) 建立张量分解模型，从数学上推导 InterDecom 的最优分解度；3) 构建统一性能框架，对不同场景自动判定最优策略（IntraFusion vs. InterDecom 及其参数）。

Result: 双环 IntraFusion 相比已有融合方案取得 1.5x–2.5x 加速；分解模型对 InterDecom 最高带来 1.3x 提升；在 288 种配置及端到端真实应用上验证，Compass 能稳定选中最优策略，端到端较 Megatron-LM baseline 最高 1.42x 加速。

Conclusion: Compass 将“通信-计算重叠”从启发式经验选择转为可建模、可推导的优化问题：拓扑感知的融合算法解决带宽瓶颈，解析模型解决分解度调参，统一框架完成策略自动择取。对训练系统工程的直接价值在于：在混合互联集群上减少通信暴露时间、降低人工调参成本，且作为策略层可叠加在现有并行框架之上。

Abstract: Overlapping communication and computation operators is a common practice to hide communication overheads, accelerating large language models (LLMs) training on GPU clusters. Existing systems achieve this through either intra-operator fusion (IntraFusion), which packs operators into a single large kernel, or inter-operator decomposition (InterDecom), which splits a tensor into multiple parts for pipelined execution. However, current IntraFusion methods underutilize network topology, causing suboptimal bandwidth usage on multi-GPU systems, while InterDecom struggles to determine the optimal number of decomposed parts for peak performance. To address these issues, we introduce Compass, which employs systematic optimization and comprehensive modeling. First, we design a novel IntraFusion algorithm leveraging double-ring communications to maximize bandwidth utilization in hybrid NVLink-PCIe systems, achieving 1.5x-2.5x speedups. Second, we develop a decomposition model that mathematically derives the optimal tensor decomposition degree for InterDecom, improving performance by up to 1.3x. Finally, we develop a unified performance framework that accurately determines the best strategy for different scenarios. We validate Compass through extensive evaluation across 288 configurations and end-to-end experiments on real-world applications. The results demonstrate that Compass consistently selects the optimal strategy, achieving up to a 1.42x end-to-end speedup compared to the Megatron-LM baseline.

</details>


<div id='cs.NI'></div>

# cs.NI [[Back]](#toc)

### [25] [Message-Level Scheduling for RLNC-Coded Multi-Source Traffic](https://arxiv.org/abs/2609.10940)
*Zhaohong Lu,Qingyu Liu,Haibo Zeng*

Main category: cs.NI

TL;DR: 本文研究目的端有限处理能力下、多条RLNC编码消息流的加权解码时延最小化问题。调度器仅决定已到达数据包的在线处理顺序，问题刻画为trace-conditioned离线调度，其中单处理单元的batch-release子类被证明为强NP-hard。作者提出MAIDS（Message-Aware Innovation-Deficit Scheduling），按权重与剩余解码赤字对可服务消息排序，在单处理单元下对nonblocking渐进到达且等权、以及common activation任意正权两种情形证明精确最优，同时指出无约束加权在线问题不存在通用确定性O(1)竞争比。仿真显示MAIDS稳定降低加权解码时延、平均接近离线最优，并复现理论边界。


<details>
  <summary>Details</summary>
Motivation: RLNC（随机线性网络编码）将一条消息编码为多个包，接收端需累积足够多的创新包才能解码，因此"解码时延"由到达与处理共同决定。多流竞争有限处理能力时，如何在线调度解码处理顺序以最小化加权解码时延，是网络编码与实时流媒体/低时延传输中的核心问题。既有调度研究多关注传输侧或吞吐，缺少对"解码赤字"这一创新维度感知的调度设计与理论保证，且该在线问题的复杂度与竞争比边界并不清晰。

Method: 1) 建立trace-conditioned离线调度模型：包到达外生给定，调度仅决定已到达包的处理顺序，目标为加权解码时延最小。2) 复杂度分析：证明单处理单元的batch-release子类为强NP-hard。3) 提出MAIDS算法：对每个可服务消息，依据其权重和剩余解码赤字（还需多少创新包才能解码）计算优先级进行服务。4) 理论分析：单处理单元下，在nonblocking渐进到达且权重相等、以及common activation且任意正权重两种受限情形证明MAIDS精确最优；并证明无约束加权在线问题不存在通用确定性O(1)竞争比。5) 用streaming与batch基准做仿真，与基线及离线最优对比。

Result: - 复杂性：单处理单元batch-release子类强NP-hard。- 最优性：单处理单元下，nonblocking渐进到达且等权、common activation任意正权两种情形MAIDS精确最优。- 不可近似性：无约束加权在线问题无通用确定性O(1)竞争比。- 仿真：MAIDS在streaming和batch基准上持续降低加权解码时延，相对基线有稳定增益，平均接近离线最优，并实证复现了理论预测的精确性能边界。

Conclusion: MAIDS把"创新赤字"嵌入在线调度优先级，在特定到达结构下取得可证明的精确最优，并给出在线加权问题竞争比的负面边界。其价值在于为RLNC解码端处理调度提供了低开销、易实现且有理论支撑的策略，可作为网络编码低时延传输与目的端处理资源分配的基础组件。

Abstract: This paper studies weighted decoding-delay minimization for multiple RLNC-coded message streams that compete for finite processing capacity at a destination. Packet arrivals are exogenous, while the scheduler only determines the processing order of packets already available at the destination. A trace-conditioned offline scheduling formulation shows that a batch-release subclass is strongly NP-hard even with a single processing unit. Message-Aware Innovation-Deficit Scheduling (MAIDS) is then developed to prioritize each serviceable message according to its weight and remaining decoding deficit. For a single processing unit, MAIDS is shown to be exactly optimal under nonblocking progressive arrivals with equal weights and under common activation with arbitrary positive weights, while the unrestricted weighted online problem admits no universal deterministic $O(1)$ competitive ratio. Simulation results on streaming and batch benchmarks show that MAIDS consistently reduces weighted decoding delay relative to the tested baselines, remains close to the offline optimum on average, and recovers the predicted exact performance boundaries.

</details>


### [26] [The Towers Were Standing: A Cause Decomposition of Cellular Outages During Hurricane Helene](https://arxiv.org/abs/2609.10944)
*Oluseyi Olukola,Oare Danielle Addeh,Esther Abiodun Konan,Nick Rahimi*

Main category: cs.NI

TL;DR: 该论文对FCC灾难信息报告系统（DIRS）数据进行首次"按成因分解"研究，重建80个州-日与580个县-日样本，证明飓风Helene造成的蜂窝基站中断主因并非物理损毁（仅占1.1%），而是电力（汇总63.2%）与回传传输中断（山区北卡52.2%、田纳西69.9%）。


<details>
  <summary>Details</summary>
Motivation: 传统认知将大规模基站退服归因于铁塔物理损毁，且行业韧性投资普遍集中在备用电源。作者希望通过因果分解验证这一假设，判断灾后蜂窝网络中断的真实主导因素，从而纠正韧性投资的优先级错配。

Method: 基于FCC DIRS的24份每日申报，采用两套独立抽取流程并做交叉核验（reconciled），重建80个州-日、580个县-日的样本；对每个中断基站-日进行成因归类（电力/传输回传/物理损毁），并按地形与州维度分层统计；辅以独立主动探测（active-probe）测量：观测响应式/24前缀数量变化作为旁证。

Result: 基站物理损毁可忽略：六州合计仅占归因基站-日的1.1%，单点最高3.8%，即"站点是站着的"。电力在汇总口径占63.2%；但山区北卡罗来纳传输中断达52.2%（电力47.3%），田纳西达69.9%。北卡传输占比在事件进程中从7.0%升至85.0%（ρ=0.92）。登陆后第17天（10月15日），北卡六个相邻县有47个站点失去传输，且无降雨、无断电、无物理损毁，并在下次报告前恢复。主动探测佐证：响应式/24前缀下降1.02%、持续12小时，而田纳西保持平稳。

Conclusion: 灾后蜂窝中断的主因是电力与回传传输这两类"非物理"依赖，而非铁塔损毁；山区场景下传输回传甚至成为主要瓶颈，且可出现与天气/供电无关的次生传输中断事件。作者公开发布该数据集，并指出备用电源这一主流韧性投资仅覆盖问题中较小的一半，回传多样性与传输韧性应获得同等乃至更高的优先级。

Abstract: Hurricane Helene produced the largest absolute cell-site outage in the public FCC record, peaking at 4562 sites. The conventional model is physical: towers destroyed. Helene did destroy over 1700 miles of fibre, but almost none of it was cell sites. We present the first cause-decomposed study of the FCC's Disaster Information Reporting System, reconstructing 80 state-days and 580 county-days from 24 daily filings by two reconciled independent extractions. Damage to cell sites is negligible: 1.1% of attributed cell-site-days across six states, at most 3.8% anywhere. The sites were standing. What took them out divides by terrain: pooled, power dominates at 63.2%, but in mountainous North Carolina severed transport (backhaul) reaches 52.2% against 47.3%, and in Tennessee 69.9%. North Carolina's transport share rises from 7.0% to 85.0% across the event (\r{ho} = 0.92). Seventeen days after landfall, on 15 October, 47 sites lost transport across six contiguous North Carolina counties with no rainfall, no power loss, no damage, and recovery by the next report. Independent active-probe measurement corroborates it: responsive /24s fall 1.02% for twelve hours while Tennessee stays flat. We release the dataset. Backup power is the standard resilience investment; here it addresses the smaller half of the problem.

</details>


### [27] [terms.txt: A Consent and Compensation Protocol for Agentic Web Access](https://arxiv.org/abs/2609.11152)
*Rajarshi Chowdhury*

Main category: cs.NI

TL;DR: 论文指出"爬虫换流量"的开放网络默契正被 AI 爬虫与 agent 打破，robots.txt 已无法表达身份、目的、条款与价格，因此提出 terms.txt（面向"按路径 × 按用途"的机器访问条款）与基于源站强制的交换协议（Web Bot Auth 签名、签名意图、委托令牌、HTTP 402 协商、签名回执），并给出零依赖实现：单 vCPU 上每请求增加 0.20–0.65 ms。


<details>
  <summary>Details</summary>
Motivation: 开放网络长期依赖一个不成文契约：站点允许爬虫抓取，搜索引擎回馈访问流量。实测数据显示该契约在 AI 爬虫与自动化 agent 下已失效——自动客户端占多数请求，训练类抓取在 Cloudflare 分类中占主导，且头部 AI 平台为每个回馈访客要抓取数千页面。现有治理手段失效：robots.txt 只是"请求规则"，无法表达抓取者身份、用途、条款或价格，且可被绕过；较新的替代方案多为专有 CDN 功能，不具开放性与可移植性。因此需要一个开放、可表达细粒度访问条款、且可在源站侧强制执行的标准机制。

Method: 1) terms.txt：robots.txt 风格的文件，按路径（per-path）与用途（per-purpose）声明机器访问条款；2) 源站强制的交换协议（origin-enforced exchange），组合 Web Bot Auth 签名做客户端身份认证、携带签名意图（signed intent）、委托令牌（delegation tokens）传递代理/链式权限、HTTP 402 进行条款与价格协商、签名回执（signed receipts）做事后审计；3) 明确划分交换层可强制执行、可审计、与留待法律合同的部分；4) 提供零依赖（dependency-free）参考实现并测量开销。

Result: 性能开销：零依赖实现在单个 vCPU 上每请求增加 0.20–0.65 ms，属于可接受的源站侧开销量级。协议层面给出可强制执行/可审计/依赖合同的边界定义（摘要未给出更多定量评测，如大规模部署、抗伪造或协商成功率数据）。

Conclusion: 该工作把"爬虫访问治理"从不可执行的礼貌约定（robots.txt）推进为可机器协商、可密码学验证、可审计的开放式访问条款协议，代价是每请求亚毫秒级开销，为 AI 时代的内容供给与抓取提供开放替代品，与专有 CDN 方案形成竞争。

Abstract: The open web ran on an unwritten bargain: sites admitted crawlers, and search engines sent visitors back. Public measurements show that bargain breaking under AI crawlers and agents. Automated clients now make up most requests, training dominates Cloudflare-classified crawling, and the largest AI platforms fetch thousands of pages for each visitor they return. The web's common control, robots.txt, cannot express identity, purpose, terms, or price, can be circumvented, and newer alternatives are largely proprietary CDN features. We specify terms.txt, a robots.txt-style file for per-path, per-purpose machine-access terms, plus an origin-enforced exchange using Web Bot Auth signatures, signed intent, delegation tokens, HTTP 402 negotiation, and signed receipts. We define what the exchange can enforce, audit, and leave to contract. A dependency-free implementation adds 0.20 to 0.65 ms per request on one vCPU.

</details>


### [28] [IPv6 Hitlist Service: Lessons Learned From 10 Years of Operation](https://arxiv.org/abs/2609.11475)
*Oliver Gasser,Lion Steger,Patrick Sattler,Johannes Zirngibl*

Main category: cs.NI

TL;DR: 该论文总结 IPv6 Hitlist Service 十年运维经验：梳理服务演进、用真实 ISP/Tier-1 流量做覆盖率评估，并结合用户调研与访问日志给出使用最佳实践。


<details>
  <summary>Details</summary>
Motivation: IPv6 地址空间巨大（/64 子网海量、扫描不可行），传统的暴力扫描与 IPv4 时代的测量方法失效，因此需要「hitlist」（已知活跃/可达地址列表）作为测量目标源。IPv6 Hitlist Service 是最主流的公共 hitlist 提供方，但此前缺乏对其代表性、数据质量和长期运维取舍的系统性评估，使用者难以判断其偏差与适用范围。

Method: 1) 纵向运维复盘：记录服务十年间的架构与数据源演进（如输入来源、主动探测/响应式地址流程、格式与发布方式的变化）。2) 覆盖率分析：以某中欧大型 ISP 与 Tier-1 网络的真实 IPv6 流量为 ground truth，统计 hitlist 对其来源 AS、/48 前缀的命中比例，量化代表性与盲区。3) 用户调研 + 访问日志分析：通过问卷了解使用场景，结合下载日志刻画访问模式（一次性 vs 持续、全量 vs 仅响应式地址）。4) 归纳最佳实践与经验教训。

Result: 覆盖率：来自上述网络的 IPv6 流量中，87.1% 的 AS 和 56.5% 的 /48 前缀至少有 1 个地址被服务收录，说明 AS 级别覆盖较高、但前缀级别仍有明显缺口（约 43.5% 的 /48 未被覆盖）。用户方面呈现多样化场景与差异化的抓取模式（一次性/持续、全量/响应式子集）。论文同时给出面向使用者的实践建议与十年运维教训。

Conclusion: hitlist 是 IPv6 大规模测量中不可替代的目标源，但其前缀级覆盖存在系统性偏差，使用时需结合覆盖率边界做结果外推与置信度校正，并遵循推荐的数据选取与刷新策略。该工作为 IPv6 测量基础设施提供了长期、可复用的工程经验基线。

Abstract: After becoming an Internet Draft more than 30 years ago, IPv6 has seen an increase in deployment and use in the past years. As measurements in the IPv6 Internet require new approaches due to the vastly larger address space, hitlists have come along as one possible source for finding IPv6 targets. One of the most prominent hitlists is provided by the IPv6 Hitlist Service.
  In this paper, we share insights from 10 years of operations of the IPv6 Hitlist Service: We show different evolutions of the service, highlighting important changes along the way. To better understand the representativeness of the hitlist, we perform a coverage analysis using real-world traffic data from a major central European ISP and Tier-1 network, finding that at least one address is known to the IPv6 Hitlist Service for 87.1 % of ASes and 56.5 % of /48 prefixes originating IPv6 traffic. We also share results from a conducted user survey and analyze users accessing the IPv6 Hitlist Service, finding diverse use cases and access patterns across time (e.g., one-off vs. continuous downloads) and available data (e.g., all vs. responsive addresses). Finally, we provide best practice recommendations when working with the hitlist and share lessons learned during its 10-year operation.

</details>


### [29] [Comparative Performance Analysis of OTFS and OFDM Modulations for Mobile Wireless Communications](https://arxiv.org/abs/2609.11623)
*J. Marcos Leal B. Filho,Danilo de S. Pena,Marília C. Muniz,Álvaro A. M. de Medeiros,Vicente A. de Sousa*

Main category: cs.NI

TL;DR: 该文对OTFS与OFDM在移动无线场景下做了系统性的量化性能对比,覆盖移动性、调制阶数、多径环境和均衡器配置;结果显示OTFS在高移动性及多径环境下稳定优于OFDM,增益幅度随调制阶数从接近到数个数量级不等,且在低/中SNR区间OTFS的非线性均衡器优于线性均衡器。


<details>
  <summary>Details</summary>
Motivation: 高移动性场景下OFDM因多普勒扩展导致子载波间干扰(ICI)和信道估计快速失效,性能显著退化;而OTFS在延迟-多普勒域具有稀疏、准静态的信道表示,理论上有望在高速移动与强多径下更鲁棒。但已有工作多为零散的单点对比,缺乏在统一仿真框架下跨移动性、调制阶数、多径与均衡器维度的系统性量化评估,难以指导实际选定调制方案。

Method: 采用链路级仿真,在同一框架下对OTFS与OFDM进行控制变量对比。扫描维度包括:(1)移动性/多普勒水平;(2)调制阶数(如QPSK、16QAM、64QAM等);(3)多径信道条件;(4)接收端均衡器类型——对OTFS对比线性均衡器(如LMMSE/迫零)与非线性均衡器,并分析低/中SNR区间的差异。以BER/误码性能为主要指标,量化两者差距随配置变化的趋势。

Result: OTFS在高移动性与多径环境下持续优于OFDM;性能差距对调制阶数高度敏感——低阶调制时两者接近,高阶调制时可达数个数量级。在低与中等SNR区间,OTFS采用非线性均衡器的性能优于传统线性均衡器。

Conclusion: OTFS更适合高移动性、强多径的无线链路,但其优势高度依赖调制阶数与均衡器设计,需配套非线性均衡才能在中低SNR下充分释放。工程上应依据移动性等级与调制配置选择调制方案及均衡策略,而非一概而论。

Abstract: This paper provides a quantitative performance comparison between Orthogonal Time Frequency Space (OTFS) and Orthogonal Frequency-Division Multiplexing (OFDM) modulation schemes, focusing on mobile wireless communication scenarios. We evaluate and compare both schemes based on critical communication scenarios and configurations such as mobility levels, modulation orders, multipath environments, and equalizers. The study systematically identifies conditions where OTFS and OFDM each exhibit optimal performance. Results from simulations demonstrate that OTFS outperforms OFDM consistently for high mobility scenarios and multipath environments. Depending on the modulation order, the performance gap between OTFS and OFDM might be very close or many orders of magnitude. Moreover, at low and mid values of SNR, the non-linear equalizer performs better than traditional linear equalizers for OTFS.

</details>


### [30] [Unmanned Aerial Vehicle Propagation Channel over Vegetation and Lake Areas: First- and Second-Order Statistical Analysis](https://arxiv.org/abs/2609.11672)
*Deyvid L. Leite,Pablo Javier Alsina,Millena M. de M. Campos,Vicente A. de Sousa Junior,Alvaro A. M. de Medeiros*

Main category: cs.NI

TL;DR: 本文提出一套基于无人机的射频信道测量系统，在含Caatinga植被与湖泊的传播环境中采集数据，识别最佳信道模型，并完成大尺度衰落（路损、对数正态阴影）与小尺度衰落（多径、多普勒）表征，同时由多普勒频移反推飞行器速度。


<details>
  <summary>Details</summary>
Motivation: 无人机承载互联网接入、货物投递、空中出租车等服务，其控制站与飞行器之间需要可靠安全通信，而链路设计依赖对具体传播环境的信道特性刻画。植被与水面并存的复杂环境缺乏实测信道模型，因此需要一套可复现的机载测量方案来获取真实数据。

Method: 搭建以无人机为载体的射频信道测量平台，在包含Caatinga植被与湖泊的传播场景中采集信号数据；对实测数据拟合多种候选信道模型并择优；基于大尺度统计提取路损指数与对数正态阴影参数；基于小尺度分析提取多径与多普勒特性，并由多普勒频移估计飞行器速度。

Result: 识别出最贴合该环境通信特征的信道模型；给出路损与对数正态阴影的大尺度衰落参数；给出多径与多普勒小尺度衰落特性；并由多普勒频率估算出无人机飞行速度。

Conclusion: 该工作为植被—水面混合环境下的无人机通信链路提供了实测信道模型与参数依据，测量方法与参数化结果可直接支撑链路预算、波形与抗衰落设计，并为后续无人机通信系统的仿真与部署提供参考基线。

Abstract: The use of unmanned aerial vehicles (UAV) to provide services such as the Internet, goods delivery, and air taxis has become a reality in recent years. The use of these aircraft requires a secure communication between the control station and the UAV, which demands the characterization of the communication channel. This paper aims to present a measurement setup using an unmanned aircraft to acquire data for the characterization of the radio frequency channel in a propagation environment with particular vegetation (Caatinga) and a lake. This paper presents the following contributions: identification of the communication channel model that best describes the characteristics of communication; characterization of the effects of large-scale fading, such as path loss and log-normal shadowing; characterization of small-scale fading (multipath and Doppler); and estimation of the aircraft speed from the identified Doppler frequency.

</details>


### [31] [Electroencephalography Signal Analysis for Human Activities Classification: A Solution Based on Machine Learning and Motor Imagery](https://arxiv.org/abs/2609.11695)
*Tarciana C de Brito Guerra,Taline Nóbrega,Edgard Morya,Allan de M. Martins,Vicente A de Sousa*

Main category: cs.NI

TL;DR: 该工作用随机森林(Random Forest)对EEG信号做"真实运动 vs. 想象运动"及对应肢体的分类,并分别在消费级与研究级EEG采集系统上验证。结论是随机森林即使在消费级EEG上也能有效区分想象/真实活动并识别相关身体部位,但被试间差异(inter-subject variability)显著拖累分类性能。


<details>
  <summary>Details</summary>
Motivation: EEG是理解与运动相关脑电活动的基础工具,BCI借此为运动功能障碍人群构建辅助技术。但EEG信号的特征与模式提取仍然困难,常需依赖机器学习;同时,现有研究多基于研究级设备,消费级EEG能否支撑同类任务缺乏系统对比,且被试间信号差异对分类的影响需要量化。

Method: 基于Random Forest算法构建ML分类器,对受试者在真实运动与运动想象(imagery)任务下的EEG信号进行分类,并进一步判别所关联的身体部位。评估采用两套采集系统:一套消费级EEG、一套研究级EEG,以对比不同信号质量条件下的分类表现。

Result: Random Forest能够有效区分想象活动与真实活动,并识别相关身体部位,在消费级EEG上同样成立;但受试者间的EEG信号差异对分类过程产生负面影响。

Conclusion: 随机森林为基于EEG的运动意图分类提供了一种低复杂度、跨设备可用的可行方案,具备支撑认知过程控制类工具(BCI辅助设备)的潜力;后续工作需针对跨被试泛化问题,引入被试自适应/归一化或迁移学习等手段以缓解个体差异带来的性能损失。

Abstract: Electroencephalography (EEG) is a fundamental tool for understanding the brain's electrical activity related to human motor activities. Brain-Computer Interface (BCI) uses such electrical activity to develop assistive technologies, especially those directed at people with physical disabilities. However, extracting signal features and patterns is still complex, sometimes delegated to machine learning (ML) algorithms. Therefore, this work aims to develop a ML based on the Random Forest algorithm to classify EEG signals from subjects performing real and imagery motor activities. The interpretation and correct classification of EEG signals allow the development of tools controlled by cognitive processes. We evaluated our ML Random Forest algorithm using a consumer and a research-grade EEG system. Random Forest efficiently distinguishes imagery and real activities and defines the related body part, even with consumer-grade EEG. However, interpersonal variability of the EEG signals negatively affects the classification process.

</details>


### [32] [From Open RAN to Open Spectrum: A Programmable, Intelligent Architecture for Multi-Service Spectrum Coexistence](https://arxiv.org/abs/2609.11843)
*Michele Polese,Minh Dat Nguyen,Paolo Testolina,Tommaso Melodia*

Main category: cs.NI

TL;DR: 本文提出 Open Spectrum 架构，将软件化、可编程性和开放接口引入异构频谱服务，把频谱、服务与基础设施纳入统一资源池，由 Spectrum Intelligent Controller (SIC) 编排，支持插件式 Spectrum Applications (sApps) 与基于数字孪生(DT)的数据驱动 RFI 建模。基于 BostonTwin 城市 DT 与 Sionna 光线追踪的系统级仿真表明，跨服务共享基础设施存在性能驱动的激励，可将中位 SINR 提升最多 12 dB。


<details>
  <summary>Details</summary>
Motivation: 现有频谱共存/共享框架（如 CBRS）仅从频谱这一单一维度优化资源分配，忽略了任何频谱使能服务都同时依赖 (i) 射频与处理基础设施和 (ii) 协议栈（波形与信号处理流水线）。因此在面对感知、通信、导航、定位等需求与运行模式高度异构的多类频谱服务时，单维优化效率受限，如何突破这一局限仍是开放挑战。

Method: 将开放 RAN 原则扩展到无线网络之外：把频谱、服务与基础设施合并为一个公共资源池；由 Spectrum Intelligent Controller (SIC) 统一编排共享资源；采用插件式 sApps 实现即插即用；使用数字孪生进行数据驱动的 RFI 建模。文中给出 Open Spectrum 架构、共享基础设施池设计，以及租户入驻与激励、冲突解决、跨感知/无线电导航/无线电定位/蜂窝系统的服务共享等工作流。评估采用 BostonTwin 城市 DT 与 Sionna 光线追踪进行系统级仿真。

Result: 仿真显示共享基础设施与跨多服务共享存在性能驱动的激励机制，可提升频谱接入机会，并使中位 SINR 最高改善 12 dB。

Conclusion: Open Spectrum 通过软件化、可编程、开放接口与统一资源池编排，把频谱共存从单维分配问题升级为频谱-服务-基础设施的联合优化问题，为异构频谱服务的共享与共存提供了可操作架构与初步量化收益证据。

Abstract: Considering sharing or coexistence from the perspective of spectrum alone fails to recognize that any spectrum-enabled service also requires (i) radio and processing infrastructure and (ii) a protocol stack, including waveforms and signal processing pipelines. The efficiency of spectrum coexistence frameworks such as Citizen Broadband Radio Service (CBRS) is thus limited to optimizing resource allocation across a single dimension. How to address this limitation, however, remains an open challenge, especially considering the diversity of requirements and operational modes across spectrum services (e.g., sensing, communications, navigation, or positioning). This article introduces Open Spectrum, an architecture that brings softwarization, programmability, and open interfaces to heterogeneous spectrum services, extending the open Radio Access Network (RAN) principles beyond wireless networking. We propose to combine spectrum, services, and infrastructure in a common pool. Its resources are shared and orchestrated by a Spectrum Intelligent Controller (SIC), with plug-and-play spectrum applications, i.e., Spectrum Applications (sApps), and data-driven Radio-Frequency Interference (RFI) modeling using Digital Twins (DTs). We describe the Open Spectrum architecture, shared infrastructure pool, and operational workflows for tenant onboarding and incentives, conflict resolution, and service sharing across sensing, radionavigation, radiolocation, and cellular systems. System-level simulations using the BostonTwin urban DT and Sionna ray tracing show that there exist performance-driven incentives in sharing infrastructure and sharing across multiple services, enabling increased access to spectrum and improvement in median Signal to Interference plus Noise Ratio (SINR) of up to 12 dB.

</details>
