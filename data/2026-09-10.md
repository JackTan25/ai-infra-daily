<div id=toc></div>

# Table of Contents

- [cs.DC](#cs.DC) [Total: 11]
- [cs.AR](#cs.AR) [Total: 11]
- [cs.PF](#cs.PF) [Total: 1]
- [cs.OS](#cs.OS) [Total: 1]
- [cs.NI](#cs.NI) [Total: 10]


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [1] [LBFAST: A Lightweight Moment-Represented Lattice Boltzmann Solver for Multi-GPU Architectures](https://arxiv.org/abs/2609.09160)
*Marco Lauricella,Andrea Montessori,Giorgio Amati,Filippo Spiga,Adriano Tiribocchi,Massimo Bernaschi,Sauro Succi*

Main category: cs.DC

TL;DR: LBFAST：面向GPU的格子玻尔兹曼求解器，用"轻量矩表示"公式，在计算时从一组约化矩即时重建碰撞后分布函数而非显式存储，从而大幅降低显存占用，支持大规模三维模拟。标准单/双组分基准验证了精度与稳定性；多GPU弱扩展在最多512块GPU上接近理想，且在不同速度集下保持吞吐，能效稳定。


<details>
  <summary>Details</summary>
Motivation: 现代加速器（GPU）上显存容量与带宽是关键瓶颈。传统LBM需为每个网格点显式存储全部离散速度的分布函数/碰撞后布居，三维大规模模拟的显存需求巨大，限制问题规模并增加数据搬运（带宽受限）。因此需要一种降低内存足迹、又不牺牲精度的表示方法。

Method: 核心是"轻量矩表示"公式：不显式存储碰撞后布居（post-collision populations），而是只保留一组约化矩（reduced set of moments），并在需要时通过on-the-fly重建即时恢复分布函数。这样显存占用显著下降，同时以计算换内存（recompute-over-store）。实现完全面向GPU，采用多GPU分布式并行，并在单组分与双组分标准基准（含不同离散速度集）上评测。

Result: 单/双组分基准显示良好精度与稳定性；多GPU系统上弱扩展在最多512块GPU时接近理想（near-ideal weak scaling），在不同速度集下保持持续吞吐（sustained performance）；整体能量效率稳定，吞吐具竞争力且显存占用更低。

Conclusion: 通过"约化矩+即时重建"的轻量表示，在显存受限的GPU架构上实现了大规模三维LBM模拟的低内存、可扩展、能效稳定的实用路径，适用于当前及未来HPC平台。对工程实践的启示：在显存/带宽受限的CFD/HPC内核中，"以重算换存储"的表示变换是提升可扩展性与成本效率的有效手段。

Abstract: We present LBFAST, a GPU-oriented lattice Boltzmann solver based on a lightweight moment-represented formulation, in which post-collision populations are reconstructed on the fly from a reduced set of moments rather than stored explicitly. This approach significantly lowers the memory footprint, enabling large three-dimensional simulations within the constraints of modern accelerator architectures, where VRAM capacity and bandwidth are critical resources. The method is assessed through standard single- and two-component benchmarks demonstrating good accuracy and stability. Extensive scaling experiments on multi-GPU systems show near-ideal weak scaling up to 512 GPUs and sustained performance across different velocity sets. The combination of reduced memory usage, competitive throughput, and stable energy efficiency makes the proposed formulation a practical route for large-scale lattice Boltzmann simulations on current and emerging HPC platforms.

</details>


### [2] [SMCC-Empowered Digital Twins for Sensorless Monitoring in Large-Scale AI-Driven IoT Systems](https://arxiv.org/abs/2609.09161)
*Vincenzo Sammartino*

Main category: cs.DC

TL;DR: 提出SMCC-DT框架，用ISAC波形在6G边缘实现无传感器的数字孪生监测，将DT同步建模为跨层资源优化问题，并用PPO-based DRL在线求解，仿真中延迟降38.7%、能耗降27.4%。


<details>
  <summary>Details</summary>
Motivation: 大规模IoT场景下的AI数字孪生需要物理环境与虚拟副本之间持续高保真同步，但传统方案依赖密集传感器部署，在硬件、能耗和网络带宽上成本过高，难以规模化落地。

Method: 提出SMCC-DT统一框架，把Sensing-Memory-Communication-Computation四层耦合起来：单个ISAC射频信号同时完成环境遥测感知与向边缘服务器回传，边缘侧将大模型载入受限内存并执行推理以更新DT状态。将DT同步问题形式化为跨层优化，联合分配发射功率、波束成形向量、内存分区与CPU频率，在感知精度、吞吐、内存容量和算力预算约束下最小化端到端同步延迟；由于该MINLP为NP-hard，设计PPO-based DRL智能体SMCCAGENT在线学习近优资源分配策略。

Result: 在500节点工业IoT测试平台的仿真中，相比SOTA的正交方案和纯计算基线，DT同步延迟降低38.7%，总能耗降低27.4%，同时保持感知精度>95%、模型推理吞吐>30 FPS。

Conclusion: 该工作表明ISAC+边缘AI可将感知与通信融合，以跨层资源调度替代密集传感部署，为6G边缘数字孪生提供低成本高保真的同步路径；但目前为仿真验证，工程落地仍需真实ISAC硬件与在线训练开销评估。

Abstract: The deployment of AI-driven Digital Twins (DTs) in large-scale Internet-of-Things (IoT) ecosystems demands continuous, high-fidelity synchronization between the physical environment and its virtual replica. Conventional approaches rely on dense sensor deployments, which introduce prohibitive costs in terms of hardware, energy, and network bandwidth. In this paper, we propose SMCC-DT, an integrated Sensing-Memory-Communication-Computation (SMCC) framework that enables sensorless monitoring of physical assets by exploiting Integrated Sensing and Communication (ISAC) waveforms at the 6G Edge. Under the SMCC-DT paradigm, a single radio signal simultaneously extracts environmental telemetry (Sensing) and delivers it to an Edge server (Communication), where a large-scale AI model is loaded into constrained memory (Memory) and executed (Computation) to update the DT state. We formulate the DT synchronization problem as a cross-layer optimization that jointly allocates transmit power, beamforming vectors, memory partitions, and CPU frequency to minimize the end-to-end synchronization latency subject to sensing accuracy, throughput, memory capacity, and computational budget constraints. Because the resulting mixed-integer nonlinear program is NP-hard, we design a Proximal Policy Optimization (PPO)-based Deep Reinforcement Learning (DRL) agent, termed SMCCAGENT, that learns near-optimal resource allocation policies online. Extensive simulations over a 500-node industrial IoT testbed demonstrate that SMCC-DT reduces DT synchronization latency by 38.7% and total energy consumption by 27.4% compared to state-of-the-art orthogonal and compute-only baselines, while sustaining sensing accuracy above 95% and model inference throughput above 30 frames per second.

</details>


### [3] [HELIOS: Guardrailed LLM-Driven Evolution of Autonomous Resource Orchestration Policies for Multi-Cloud Distributed Systems](https://arxiv.org/abs/2609.09164)
*Guanyu Ding,Ying Wang*

Main category: cs.DC

TL;DR: HELIOS 把 LLM 移出在线控制回路：LLM 离线在 trace 驱动的多云模拟器中进化出可执行的 Python 编排策略，生产环境只运行冠军策略并套上容量可行性、SLO 分级放置与 churn 预算三类 guardrail。真实负载 + AWS/Azure/GCP 2026 价格与抢占数据下，罚没运营成本比单云 best-fit 低 45%、比校准多云启发式低 9–19%，并以低 40% 成本追平 oracle 信息 MILP 的高 SLO 表现，优于 2.6 倍交互量的 DQN 元控制器。guardrail 缺失时 10x spot 风险压力测试下高阶 SLO 停机率从 0.17% 恶化到 97–98%。


<details>
  <summary>Details</summary>
Motivation: 跨多云部署延迟敏感服务时存在单云 autoscaler 看不到的优化面：各云 on-demand vCPU 价差、spot 折扣与中断风险随云和实例型变化、egress 费用惩罚状态迁移、单云故障会整体不可用。LLM 擅长从自然语言环境描述综合复杂决策逻辑，但把 LLM 放在每个调度决策的关键路径上不可行——200 服务规模下，逐决策推理成本高于其管理的云容量，并给毫秒级控制回路引入数秒延迟。

Method: LLM 离线进化编排策略：策略是固定特征接口上的一小段 Python 程序，在 trace 驱动的多云模拟器中被逐代评估与选择；仅冠军程序上生产，外层 guardrail 独立于进化代码的提议，强制容量可行性、SLO 分级放置规则与 churn 预算。训练/评测数据为 PlanetLab、Bitbrains、Azure 真实负载 trace 叠加 AWS/Azure/GCP 2026 价格与中断频率数据。对比对象包括单云 best-fit、校准多云启发式、oracle 信息 MILP 规划器与 DQN 元控制器。

Result: 罚没运营成本相比单云 best-fit 基线降低 45%，相比校准多云启发式降低 9–19%。以低 40% 成本追平 oracle 信息 MILP 的高阶 SLO 表现，并优于用 2.6 倍环境交互训练的 DQN 元控制器。guardrail 关键：10x spot 风险压力测试下，无 guardrail 的同族策略高阶 SLO 停机率达 97–98%，有 guardrail 时为 0.17%。已开源模拟器、策略、LLM prompt/response 存档与逐代 fitness 日志。

Conclusion: 核心工程范式是"LLM 做离线策略合成、在线只跑已验证的小程序"，把昂贵的非确定性推理与毫秒级控制回路解耦；这比 DQN 类 RL 元控制器样本效率更高，也规避了 LLM 在线延迟与成本瓶颈。对多云调度、成本/中断优化、SLO 保障与 LLM-for-systems 落地均有直接参考价值，开源制品（模拟器 + 逐代日志 + prompt 存档）显著降低复现与二次开发门槛。需注意：结论依赖 2026 价格/中断假设与 trace 回放，跨云真实 egress 计费、spot 回收动态与 guardrail 覆盖不到的长尾故障仍需实测验证。

Abstract: Operating latency-sensitive services across multiple public clouds creates an optimization surface no single provider autoscaler can see: on-demand vCPU prices differ by provider, spot discounts and interruption risks vary by provider and instance type, egress fees penalize state movement, and provider-level failures can take down single-cloud deployments. Large language models (LLMs) are appealing orchestrators because they can synthesize non-trivial decision logic from a natural-language environment description. However, placing an LLM on the critical path of every scheduling decision is impractical: for a 200-service fleet, per-decision inference would cost more than the cloud capacity it manages and add multi-second latency to a millisecond-scale control loop. HELIOS moves the LLM off the critical path. The LLM evolves executable orchestration policies, small Python programs over a fixed feature interface, against a trace-driven multi-cloud simulator. Only the champion program runs in production, wrapped in guardrails that enforce capacity feasibility, SLO-class placement rules, and churn budgets regardless of evolved code's proposal. On real workload traces (PlanetLab, Bitbrains, Azure) combined with 2026 price and interruption-frequency data from AWS, Azure, and GCP, the evolved policy reduces penalized operating cost by 45% versus a single-cloud best-fit baseline and by 9-19% versus calibrated multi-cloud heuristics. It matches an oracle-informed MILP planner's premium-SLO performance at 40% lower cost and outperforms a DQN meta-controller trained with 2.6x more environment interactions. Guardrails are essential: without them, the same policy family degrades to 97-98% premium-tier downtime under a 10x spot-hazard stress test, versus 0.17% when guarded. We release the simulator, policies, LLM prompt/response archive, and per-generation fitness logs for full reproducibility.

</details>


### [4] [TFR-GNN: Topology- and Fault-Aware Graph Neural Scheduling for Heterogeneous Distributed Computing Systems](https://arxiv.org/abs/2609.09165)
*Shiyu Yang,Ziyang Zeng,Jie-Si Yang*

Main category: cs.DC

TL;DR: TFR-GNN 是一个基于图神经网络的 DAG 工作流调度器，用双向依赖注意力、机器图拓扑注意力和交叉注意力放置头（含故障门控可靠性偏置与复制门），通过将故障容忍的最优组合策略蒸馏为单次前向策略。在无故障时精确匹配 HEFT，故障下期望 makespan 平均降低 14.8%（最高 47%），优于 R-HEFT 11%，匹配事后最优 oracle，可泛化到未见应用及大 10 倍图，近 5000 任务图亚秒级出解。


<details>
  <summary>Details</summary>
Motivation: DAG 调度在异构分布式系统上是 NP-hard，HEFT 因其低复杂度和强 makespan 成为事实标准。但真实部署中机器会失败（商品节点、可抢占节点可靠性远低于专用节点），一个 makespan 最优但忽略可靠性的放置会被节点故障严重拖慢。作者在 WfCommons/Pegasus 真实工作流上实证表明：在故障强度与集群负载的联合空间中不存在单一固定启发式最优——无故障时 HEFT 最优，有故障且存在空闲容量时，可靠性感知的放置最多可将期望 makespan 降低 52%。因此需要一个能随故障与负载自适应的一次性策略。

Method: TFR-GNN 的架构包含三部分：(1) 对任务 DAG 的双向依赖注意力；(2) 对带宽加权机器图的拓扑注意力；(3) 交叉注意力放置头，附带故障门控的可靠性偏置（failure-gated reliability tilt）和可选的复制门（replication gate）。训练方式为将“best-of-portfolio 故障容忍 oracle”蒸馏为单一 one-shot 策略，避免部署时按场景调参。评估采用经过验证的事件级模拟器，在真实工作流数据和双峰可靠性集群模型上进行，作者声明所有实验数字均非合成。

Result: 在真实工作流 + 双峰可靠性集群模型下：无故障时 TFR-GNN 与 HEFT 完全一致；故障下期望 makespan 相对 HEFT 平均降低 14.8%（最高 47%）；比固定可靠性感知基线 R-HEFT 优 11%；作为单一策略匹配逐场景事后 oracle，且无需任何部署时调优；可泛化到未见应用以及比训练集大一个数量级的工作流；对近 5000 任务的图仍能在远低于 1 秒内生成调度。

Conclusion: 论文的核心贡献是把“可靠性感知 DAG 调度”从人工固定启发式的二选一问题，转化为可用 GNN 学习的单次策略问题，并与 HEFT 保持严格兼容（无故障即退化为 HEFT）。工程上意义在于：在存在非专用/可抢占节点的异构集群中，调度器可自适应故障强度与负载而无需部署时调参，亚秒级近 5000 任务的可扩展性使其具备在线放置的可行性；其主要局限是结果仍基于模拟器而非真实生产集群的端到端验证。

Abstract: Scheduling workflow directed acyclic graphs (DAGs) on heterogeneous distributed systems is a classical NP-hard problem, and the list-scheduling heuristic HEFT remains the defacto standard because of its low complexity and strong makespan. In real deployments, however, machines fail: commodity and pre-emptible nodes are far less reliable than dedicated ones, and a makespan-optimal but reliability-agnostic placement can be dramatically slowed by node failures. We show empirically, on real workflow structures from the WfCommons/Pegasus corpus, that no single fixed heuristic is best across the joint space of failure intensity and cluster load: with no failures HEFT is optimal, whereas under failures a reliability-aware placement can reduce the expected makespan by up to 52% when spare capacity exists. Motivated by this, we present TFR-GNN, a graph neural scheduler that combines bidirectional dependency attention over the task DAG, topology attention over the (bandwidth-weighted) machine graph, and a cross-attention placement head augmented with a failure-gated reliability tilt and an optional replication gate. We train TFR-GNN by distilling a best-of-portfolio fault-tolerant oracle into a single one-shot policy. On real workflows and a bimodal-reliability cluster model, TFR-GNN matches HEFT exactly when there are no failures, reduces the expected makespan under failures by 14.8% on average (up to 47%) over HEFT, beats a fixed reliability-aware baseline (R-HEFT) by 11% and matches a per-scenario hindsight oracle as a single policy without any deployment-time tuning, and generalises to unseen applications and to workflows an order of magnitude larger than those seen in training, while producing schedules in well under a second for graphs of nearly 5,000 tasks. All results are produced by a verified event-level simulator on real workflow data; no experimental numbers are synthetic.

</details>


### [5] [Smart Adaptive Computing Across the Continuum: LLMs in IoT-Edge-Cloud Resource Management](https://arxiv.org/abs/2609.09348)
*Antonino Vaccarella,Lanpei Li,Vincenzo Lomonaco,Massimo Coppola*

Main category: cs.DC

TL;DR: 该论文是对 LLM+DRL 云连续体编排系统的分类学综述与扩展：在 Wang 等人的分类法上新增 "AI Augmentation Paradigm" 与 "Feedback channel" 两个维度，用其分析六套近期系统架构，发现无人同时实现完整的 LLM 编排与完整的智能体层反馈，进而指出缺失的是跨层反馈抽象。


<details>
  <summary>Details</summary>
Motivation: IoT、edge、cloud 三层资源管理的约束持续变化，需要上下文感知的连续决策；DRL 适合此类问题，LLM 也越来越多被用于增强 DRL 流水线，但两者之间的架构关系（LLM 究竟以何种方式介入、执行反馈如何回流）在已有文献中很少被显式刻画，导致系统设计缺乏统一的比较与对照框架。

Method: 以 Wang 等人的 "Continuum Orchestration Systems employing DRL" 分类法为基线，扩展出两个新维度：(1) AI Augmentation Paradigm——刻画 LLM 被利用的方式（如作为策略、作为奖励/目标生成器、作为状态抽象层等）；(2) Feedback channel——刻画执行反馈是否、以及经由哪条系统路径返回 LLM，从而在 LLM Orchestration 层闭合 MAPE 控制回路。随后用该分类法对六套近期系统架构进行逐个标注与横向对比，并归纳共性缺口。

Result: 六套系统均未同时在 Cloud Continuum 场景下做到"完整 LLM 编排 + 完整智能体层反馈"。这一缺口被归因于缺少一种跨层反馈抽象：各层（IoT/edge/cloud）信号量纲与语义不可通约，无法直接喂给 LLM Orchestrator，因此 MAPE 回路实际上在编排层断开。

Conclusion: 论文的主要贡献不是新系统，而是分类学与问题定位：把 LLM 与 DRL 的耦合方式、反馈路径显式化，指出跨层反馈抽象是 Cloud Continuum 中 LLM 编排落地的关键缺失组件，为后续系统设计给出明确的架构方向。

Abstract: Managing resources across IoT, edge, and cloud layers calls for continuous, context-aware decisions under constraints that rarely stay fixed. Deep reinforcement learning (DRL) handles this class of problems well, and large language models (LLMs) are increasingly used to augment DRL pipelines, yet the architectural relationship between the two is seldom made explicit. We build on Wang et al.'s taxonomy of Continuum Orchestration Systems employing DRL techniques and extend it with two further dimensions. The AI Augmentation Paradigm measures how LLMs are exploited, while the Feedback channel captures whether and through which system path the execution feedback returns to the LLM in order to close the MAPE control loop at the LLM Orchestration layer. We apply this taxonomy to six recent system architectures and find a common gap, as none combines full LLM orchestration with full agent-layer feedback in a Cloud Continuum setting. We relate this gap to a missing cross-tier feedback abstraction, bridging the incommensurable per-tier signals and the LLM Orchestrator.

</details>


### [6] [Breaking Fault Lines: Unifying TEE-Assisted BFT Consensus in Partially Trusted Worlds](https://arxiv.org/abs/2609.09742)
*Xiaoqing Wen,Tong Liu,Jianyu Niu,Jialin Li,Cong Wang,Yinqian Zhang,Chen Feng*

Main category: cs.DC

TL;DR: 首个面向任意部分TEE部署的HotStuff风格BFT协议Raftel：给出紧致容错界f < max{n/3, m/2}，揭示TEE占比需超2/3才带来容错收益的阈值现象，并用双法定人数与TEE-leader快路径实现。


<details>
  <summary>Details</summary>
Motivation: 现有TEE辅助BFT协议多假设全部或固定比例的副本具备TEE，而实际部署中硬件可信节点常是任意子集，异构信任会改变法定人数（quorum）构成与容错边界，已有协议在该模型下不再适用，需重新刻画容错极限并设计相应协议。

Method: 在通用部分TEE模型下重新推导容错界，得到f < max{n/3, m/2}（n为总副本数，m为TEE副本数）；据此提出两条协议原则：(1)双法定人数构造，安全组合纯TEE quorum与混合quorum；(2)TEE-leader快路径，利用硬件强制的不可抵赖性（non-equivocation）降低共识与视图切换延迟。基于Intel SGX实现Raftel（HotStuff风格）及流水线变体chained-Raftel，并在LAN/WAN环境评测。

Result: Raftel在WAN下最高达625 TPS、延迟低于670 ms；吞吐较HotStuff最高高出308 TPS，性能接近全TEE辅助协议。

Conclusion: 异构信任下TEE对BFT的容错增益存在尖锐阈值：TEE副本需超过2/3才产生实质收益；Raftel证明在任意部分TEE部署中可兼顾安全性与接近全TEE方案的性能，为混合可信共识提供了可落地的工程路径。

Abstract: This paper revisits TEE-assisted BFT under a universal partial-TEE model, where an arbitrary subset of replicas execute inside TEEs while the remaining replicas operate without hardware trust guarantees. We show that heterogeneous trust changes the structure of quorum formation and fault tolerance. In particular, we derive a tight resilience bound f < max {n/3, m/2}, where n is the total number of replicas and m is the number of TEE-enabled replicas. The result reveals a sharp threshold phenomenon: TEEs improve fault tolerance only once they exceed two-thirds of the deployment.
  Guided by this characterization, we introduce two protocol principles: (1) a dual-quorum construction that safely combines TEE-only and mixed quorums, and (2) a TEE-leader fast path that leverages hardware-enforced non-equivocation to reduce both consensus and view-change latency. We realize these ideas in Raftel, which is, to our knowledge, the first HotStuff-style BFT protocol designed explicitly for arbitrary partial-TEE deployments, and in chained-Raftel, a pipelined variant that further accelerates mixed-trust execution. We implement both protocols atop Intel SGX and evaluate them in LAN and WAN environments. Our results show that Raftel achieves up to 625 TPS with sub-670 ms latency in WAN settings, outperforming HotStuff by up to 308 TPS in throughput while approaching the performance of fully TEE-assisted protocols.

</details>


### [7] [Epoch: Compiling Diffusion Blocks for Sparse MoE Serving](https://arxiv.org/abs/2609.09748)
*Jianian Zhu,Hang Wu,Yinghui Li,Haojie Wang,Ruixuan Li,Jidong Zhai*

Main category: cs.DC

TL;DR: 本文提出 \sys{}，一个面向块扩散（block-diffusion）MoE 语言模型的推理服务系统，将"扩散块"视为编译单元，通过 block plan 解耦块时钟与逐 forward 的执行时钟，在 MoE forward 的三个稠密轴上分别用 \atlas{}（覆盖驱动的活跃专家集合）、\lsp{}（仅对活跃/新解码/需刷新位置做路由专家计算）、\freshlane{}（贯穿 dispatch/kernel/combine 的新鲜 token–expert 工作列表）实现稀疏化执行，在 8×H100 上端到端提速最高 2.7×。


<details>
  <summary>Details</summary>
Motivation: 扩散语言模型（dLLM）的生成范式是：在一个固定大小的 token 位置块上，通过多次 forward pass 迭代精炼（refinement），直到收敛。这导致其执行单元是"块级迭代时钟"，而当前主流 LLM serving 系统的执行单元是"单次 forward"。若直接把稠密 MoE runtime 套用到 dLLM 上，会产生三类系统性浪费：(1) 每次 forward 都重新构建高度相似的 routing 结构（专家路由在迭代间变化很小），产生重复的 gate 计算与专家选择开销；(2) 对 logits 已经"死亡"（已确定、不再变化）的位置仍然重复计算专家输出；(3) 这些无效位置仍然要参与 dense expert-parallel 通信（all-to-all dispatch/combine），把通信量按稠密规模放大。本质矛盾是：稠密 MoE runtime 的调度/通信粒度（按 forward、按全序列）与 dLLM 的真实工作量分布（按 block clock、按活跃位置）不匹配。

Method: \sys{} 的核心设计是把一个扩散块的块时钟结构编译成小的 block plan，并在每个迭代时钟上刷新所有可能影响"活跃解码决策"的值。它在 MoE forward 的三条稠密执行轴上分别做压缩：1) \atlas{}（专家维）：按覆盖率编译每层的"活跃专家支撑集"（coverage-driven active expert support），即在保证块解码正确性的前提下，预先确定每层需要参与计算的最小专家集合，同时仍然逐迭代重算 gate logits，以捕捉路由变化，避免因静态裁剪专家而损失质量。2) \lsp{}（序列维）：模型状态仍保留完整的 sequence shard（稠密 state 不被切碎，保持 layer boundary 处的一致性），但只把"活跃的、刚解码的、需要刷新的"位置送入新一次的路由专家计算，其余死位置跳过 routed-expert GEMM。3) \freshlane{}（通信/内核维）：把上述"新鲜 token–expert 工作列表"贯穿 expert-parallel 的 dispatch、专家 kernel、以及 combine 三个阶段，使集合通信只承载真实活跃的工作量，并在层边界处恢复成稠密的逻辑 shard，从而不破坏原有 EP/TP 布局。实现于 8×NVIDIA H100，评测 LLaDA-MoE、LLaDA2.0-mini、LLaDA2.0-Flash 三个开源块扩散 MoE 模型（总参数 7B–100B），任务为 GSM8K、HumanEval、MGSM、MT-Bench。

Result: 在相同 8-GPU 部署下，\sys{} 相对最强存活基线（strongest surviving baseline）端到端执行时间最高提升 2.7×；在多个基线因显存不足（OOM）而无法运行的最大 batch size 下仍然可行，说明其不仅降低计算/通信开销，也显著缓解了 KV/激活显存压力；同时任务质量（GSM8K/HumanEval/MGSM/MT-Bench）相对稠密参考实现保持一致，即稀疏化执行没有引入可观测的质量退化。

Conclusion: 该工作的系统贡献在于首次把 dLLM 的"块级精炼时钟"确立为 MoE serving 的一等编译/调度单元，用 block plan 将迭代间不变的 routing 结构与迭代间变化的值（gate logits、活跃位置集合、新鲜 token–expert 工作列表）显式分离，并在专家维（\atlas{}）、序列维（\lsp{}）、通信维（\freshlane{}）三轴协同稀疏化，且以"层边界恢复稠密逻辑 shard"的方式保持与既有 EP/TP 并行布局的兼容性。工程意义：为扩散式 LLM（尤其大规模 MoE 变体）的 serving 提供了一条不依赖专用硬件、可复用到现有 MoE EP runtime 的优化路径；其"块计划 + 新鲜工作列表驱动集合通信"的思路可推广到其他迭代式解码/多步推理负载（如投机解码 draft 验证、迭代式修订生成），对降低 serving 通信放大和显存占用尤其有参考价值。

Abstract: Diffusion language models generate text by refining a fixed-size block of token positions through many forward passes, a loop that does not match the per-forward execution unit used by most LLM serving systems. A dense MoE runtime binds all work to the refinement-iteration clock: it rebuilds similar routing structure on every forward, recomputes expert outputs for positions whose logits are already dead, and sends those positions through dense expert-parallel collectives. This paper presents \sys{}, a serving system that treats the diffusion block as a compilation unit. \sys{} compiles a small \emph{block plan} for the block-clock structure of one diffusion block and refreshes every value that can affect a live decode decision on the iteration clock.
  \sys{} realizes this plan along three dense axes of an MoE forward: \atlas{} compiles a coverage-driven active expert support per layer while recomputing gate logits every iteration; \lsp{} keeps full sequence shards as model state but routes only live, newly decoded, and refresh-required positions through fresh routed-expert computation; \freshlane{} carries this fresh token--expert worklist through expert-parallel dispatch, kernels, and combine, then restores the dense logical shard at the layer boundary. We implement \sys{} on 8 NVIDIA H100 GPUs and evaluate it on three open-weight block-diffusion MoE models (LLaDA-MoE, LLaDA2.0-mini, and LLaDA2.0-Flash, spanning 7B to 100B total parameters) across GSM8K, HumanEval, MGSM, and MT-Bench. \sys{} improves end-to-end execution time by up to 2.7$\times$ over the strongest surviving baseline under the same 8-GPU placement and remains feasible at the largest batch sizes where multiple baselines run out of memory, while preserving task quality relative to the dense reference.

</details>


### [8] [Introvert Clustering for Distributed Graph Algorithms](https://arxiv.org/abs/2609.10044)
*Yi-Jun Chang,Nima Dolatabadi*

Main category: cs.DC

TL;DR: 论文提出 introvert clustering 这一图分解原语，强化低直径聚类使每个顶点保证至少 (1/2-ε) 比例的相关邻居留在本簇内，迭代得到 O(log n) 层、弱直径 O(log n) 的层次分解，并据此在 LOCAL 模型下给出边染色与局部平衡割的确定性算法。


<details>
  <summary>Details</summary>
Motivation: 标准网络分解（network decomposition）的常规贪心流程受限：聚簇后簇间边与割的处理往往只能走贪心，难以扩展到边染色、局部平衡割等更强的对称性破缺/割问题。作者希望构造一种更强的聚类保证，使簇内问题可以独立、非贪心地求解。

Method: 核心是 introvert clustering：要求每个被聚类顶点至少保留 (1/2-ε) 比例的相关邻居在同一簇内。迭代应用得到 O(log n) 层、弱直径 O(log n) 的分层分解。构造上，随机版本用 Miller–Peng–Xu 低直径聚类加简单修剪，O(log^2 n) 轮；确定性版本白盒改造 Ghaffari–Grunau [FOCS 2024] 的递归网络分解，做到 Õ(log^2 n) 轮。应用算法极简：边染色逆序处理层并在簇内染色，局部平衡割正序处理层并在簇内求局部最大割。

Result: 在 LOCAL 模型下：(1) 对任意常数 ε>0，得到 Õ(log^2 n) 轮确定性 ((3/2+ε)Δ)-边染色，适用于 Δ≥Δ0(ε)，二部图对所有 Δ 成立；(2) 对任意常数 0<ε<1/4，得到 Õ(log^2 n) 轮确定性 (1/4-ε)-局部平衡割，即每个顶点至少 (1/4-ε) 比例邻居在对侧。

Conclusion: introvert 保证突破了网络分解的贪心局限，把边染色与局部平衡割等问题的复杂度压到 Õ(log^2 n) 轮的确定性并行时间，且算法结构异常简单，是网络分解工具的实质性推广。

Abstract: We introduce a graph decomposition primitive called introvert clustering, which strengthens standard low-diameter clustering by guaranteeing that every clustered vertex keeps at least a $\left(\frac12-\varepsilon\right)$-fraction of its relevant neighbors in its own cluster. Repeatedly applying this primitive yields a layered introvert network decomposition with $O(\log n)$ layers and weak diameter $O(\log n)$.
  We give two applications in the $\mathsf{LOCAL}$ model. For every constant $\varepsilon>0$, we obtain a $\widetilde O(\log^2 n)$-round deterministic algorithm for list $\left(\frac32+\varepsilon\right)Δ$-edge coloring on graphs of maximum degree $Δ\geqΔ_0(\varepsilon)$; for bipartite graphs, the result holds for all $Δ$. For every constant $0<\varepsilon<1/4$, we also obtain a $\widetilde O(\log^2 n)$-round deterministic algorithm for a $\left(\frac14-\varepsilon\right)$-locally balanced cut, where every vertex has at least a $\left(\frac14-\varepsilon\right)$-fraction of its neighbors on the opposite side.
  The resulting algorithms are remarkably simple: edge coloring processes the layers in reverse order and colors each cluster, while locally balanced cut processes them forward and computes a locally maximum cut within each cluster. The introvert guarantee enables these procedures beyond the usual greedy regime of network decomposition.
  We construct the decomposition in $O(\log^2 n)$ randomized rounds using Miller--Peng--Xu low-diameter clustering and a simple trimming procedure, and deterministically in $\widetilde O(\log^2 n)$ rounds via a white-box adaptation of the recursive network decomposition algorithm of Ghaffari and Grunau [FOCS 2024].

</details>


### [9] [CEDD-optimizer: Enabling Cost-Efficient Dataset Distillation on Geographically Distributed Edge Systems](https://arxiv.org/abs/2609.10151)
*Dai Liu,Eishi Arima,Martin Schulz*

Main category: cs.DC

TL;DR: 提出 CEDD-optimizer，一个面向非均匀边缘环境的分布式数据集蒸馏（DD）超参调优框架，在满足训练质量约束下最小化总成本（能耗+数据传输），含 CEDD-calibrator 与 CEDD-solver 两模块，实验较基线 DD 最高提升 20.8（成本效率）。


<details>
  <summary>Details</summary>
Motivation: 集中式学习需从边缘设备汇聚数据，通信开销成为瓶颈。数据集蒸馏（DD）压缩比高，适合分布式数据的集中式训练，但其在非均匀边缘环境中的成本效率尚未被探索：地理分布设备的电价与传输价格各异，而 DD 关键超参（目标压缩比、蒸馏步数）同时影响能耗、传输开销与下游测试精度，需在局部与全局两个层面联合调优。

Method: CEDD-optimizer 将超参调优建模为"训练质量约束下的总成本最小化"问题，并显式建模环境非均匀性。1) CEDD-calibrator：标定能耗模型与训练质量模型的参数——能耗模型通过离线标定获得，质量模型通过三步调优方案在线估计；2) CEDD-solver：基于上述模型求解成本最小化问题，引导并改进分布式 DD 工作流（压缩比、蒸馏步数等超参的跨设备配置）。

Result: 在多个图像数据集上的实验显示，在相同质量约束下，该方法相较基线 DD 方法成本效率最高提升 20.8。

Conclusion: 该工作将 DD 超参调优与边缘环境的异构能耗/传输定价联合优化，为分布式数据下的集中式训练提供成本感知的系统级调优框架；其工程启示在于：在异构边缘集群上做数据压缩/蒸馏流水线时，压缩比与计算量应与设备侧电价、带宽成本协同调度，而非采用统一静态配置。

Abstract: Centralized learning is a fundamental paradigm in modern AI, in which data are collected from distributed edge devices and aggregated at a central host for model training. However, this pipeline is often bottlenecked by the substantial communication overhead of data collection. Dataset Distillation (DD), with its high compression ratio, is therefore attractive for centralized learning on distributed data. Yet, the cost efficiency of DD in non-uniform edge environments remains largely unexplored. Geographically distributed edge devices face different energy and data-transfer prices, while key DD hyperparameters, such as the target compression ratio and number of distillation steps, substantially affect energy use, transfer overhead, and downstream test accuracy. These hyperparameters must therefore be tuned both locally and globally. We propose the Cost-Efficient Dataset Distillation optimizer (CEDD-optimizer), a hyperparameter-tuning framework for cost-efficient distributed DD. It minimizes total cost under a training-quality constraint by optimizing hyperparameter settings across edge devices while accounting for environmental non-uniformity. The framework comprises two modules: CEDD-calibrator and CEDD-solver. CEDD-calibrator identifies parameters for our energy and training-quality models: the former is determined through offline calibration, whereas the latter is estimated online through a three-step tuning scheme. Based on these models, CEDD-solver solves the cost-minimization problem to steer and improve the distributed DD workflow. Experiments across various image datasets show that our approach achieves up to a 20.8 improvement over the baseline DD method under the same quality constraint.

</details>


### [10] [Stencil Computation at the Intersection of AI and HPC](https://arxiv.org/abs/2609.10368)
*Timothee Ewart,Mauricio Araya-Polo*

Main category: cs.DC

TL;DR: 该论文把面向AI的Tensor编译器(TinyTC、OpenAI Triton)的tiling与内存抽象迁移到高阶stencil计算，以8阶25点声学stencil(含边界条件)为案例，在Intel、NVIDIA、AMD多代GPGPU上对比硬件专用TinyTC与可移植PyTorch/Triton实现，并用roofline与内存层次profiling证明该类负载受带宽限制、编译器管理的L1/LSC缓存可替代程序员手写shared memory staging。


<details>
  <summary>Details</summary>
Motivation: AI张量编译器已具备成熟的tiling与内存层次抽象，但这些能力此前主要用于稠密线性代数/深度学习算子，尚未被系统地验证能否覆盖科学计算中的高阶stencil(尤其带边界条件)负载。同时HPC stencil开发长期面临"专用kernel性能高但不可移植 vs 可移植框架性能损失"的取舍，需要实证数据判断Tensor编译器路线的工程价值。

Method: 以8阶、25点声学stencil(带边界条件、大规模网格)为统一基准；对比两类实现：(1)硬件专用TinyTC，(2)PyTorch/Triton可移植实现；评测平台覆盖Intel B70/B580/GPU MAX 1550、NVIDIA A100/RTX6000 Blackwell/H100、AMD MI325x；采用roofline模型与内存层次profiling(含L1/LSC缓存行为)进行性能归因，并区分随机初始化与零初始化两种数据分布以观察硬件内存压缩的影响。

Result: Battlemage B580上随机初始化时TinyTC达15.6 Gpts/s，PyTorch/Triton为13.5 Gpts/s；零初始化下因硬件内存压缩可达35.8 Gpts/s。roofline与内存层次分析表明性能主导因素是带宽，编译器管理的L1/LSC缓存可有效替代手工shared memory staging。总体结论：Intel硬件上TinyTC为性能优先路径，PyTorch/Triton为跨厂商HPC stencil开发的强可移植性/生产力基线。

Conclusion: Tensor编译器的tiling与内存抽象可成功复用于高阶stencil类科学计算负载，工程上意味着AI编译器栈与HPC stencil kernel之间的技术栈可收敛；短期Intel平台优先选TinyTC，跨厂商部署与快速迭代优先选Triton；但需注意零初始化带来的压缩加速属于数据分布红利而非算法优化，且结论受限于单一stencil与受带宽约束的场景。

Abstract: Tensor compilers such as TinyTC and OpenAI Triton were originally developed for AI workloads, but the same tiling and memory abstractions can be applied to implement efficient high-order stencils for scientific and industrial applications. We demonstrate this for an 8th-order, 25-point acoustic stencil with boundary conditions over an a demanding-sized grid, targeting GPGPUs, where we compare the hardware-specialized TinyTC implementation with a portable PyTorch/Triton implementation. The target platforms for evaluation include Intel B70, B580, GPU MAX 1550, NVIDIA A100/RTX6000 Blackwell/H100, and AMD MI325x. For instance, on Battlemage B580 TinyTC reaches 15.6 Gpts/s versus 13.5 Gpts/s for PT/Triton under random initialization, while zero-initialized runs reach up to 35.8 Gpts/s due to hardware memory compression. Using roofline and memory-hierarchy profiling, we show that -as expected- performance is predominantly bandwidth-limited and that compiler-managed L1/LSC caching can effectively replace programmer-managed shared-memory staging for this stencil class. Overall, the results position TinyTC as the performance-oriented path on Intel hardware and PyTorch/Triton as a strong portability/productivity baseline for cross-vendor HPC stencil development.

</details>


### [11] [Avatar: Toward Autonomous End-to-End Orchestration of Scientific Workflows using LLMs](https://arxiv.org/abs/2609.10509)
*Suman Raj,Hai Duc Nguyen,Haochen Pan,Ryan Chard,Kyle Chard,Ian Foster*

Main category: cs.DC

TL;DR: Avatar 用基于 actor 的架构（orchestrator / executor / provenance monitor）把 WMS 的编排决策策略抽象为可插拔（规则或 LLM 驱动）的 action catalog，使传统编排与 agentic 编排共用同一内核；在三个负载上规则模式复现原生执行，LLM 模式将计算浪费降低 55%、GPU 忙碌时间减少 40%。


<details>
  <summary>Details</summary>
Motivation: 现有科学工作流管理系统（如原生的 DAG 调度器）依赖人工固定规则完成编排，难以适应运行时动态变化；LLM agent 被认为可能带来更自主的编排，但工程上缺乏明确答案：agentic 推理应插入到哪个环节、如何约束其风险、以及在什么条件下才真正带来收益。论文正是针对这一"插入点—风险边界—收益条件"三问展开。

Method: 提出 Avatar 的 actor 架构，由三类 actor 组成：orchestrator（编排决策）、executor（任务执行）、provenance monitor（溯源/状态观测，为决策提供上下文与反馈）。关键设计是每个 actor 的决策策略可插拔：规则实现或 LLM 后端实现均通过一个经 adapter 校验的 action catalog 暴露，使常规控制与 agentic 控制运行在同一内核之上，从而可跨不同 WMS 复用。基于 Academy 框架给出实现，并在三类工作负载上与原生 WMS 对比评测；通过规则模式作为等价性基线，隔离 LLM 带来的增量收益与风险。

Result: Avatar 的规则模式可复现原生执行行为，且是同一份未修改的内核同时驱动全部三个工作负载，验证了架构的通用性与低迁移成本；LLM 驱动的 Avatar 相比基线将计算浪费降低 55%，GPU 忙碌时间（GPU-busy time）削减 40%。

Conclusion: Avatar 的贡献不止于性能数字，而是给出了将 LLM 控制面引入工作流系统的可落地系统抽象：可插拔策略 + 统一 action catalog + 溯源监控，使 agentic 编排可被约束在受控动作空间内，并支持规则/agentic 双模共存与回退。其工程意义在于为 WMS 的"自推理编排"提供了可替换、可验证的接口层，利于在不重写调度内核的前提下渐进式引入 LLM 决策。

Abstract: Scientific workflow management (WMSs) systems automate execution, yet orchestrate using fixed, hand-tuned rules. LLM agents promise more autonomous orchestration, but it remains unclear where to introduce agentic reasoning, how to bound its risk, and when it actually helps. We present Avatar, an actor-based architecture comprising an orchestrator, an executor, and a provenance monitor. Each actor's decision policy is pluggable (rule-based or LLM-backed) via a single adapter-validated action catalog, so conventional and agentic control run on the same core across different WMSs. We present an implementation using the Academy framework and evaluate Avatar across three workloads. We observe that Avatar's rule mode reproduces native execution, with a single unchanged core running all three. Moreover, LLM-backed Avatar reports a reduction of compute wastage by $55\%$ and cuts GPU-busy time by $40\%$. Overall, we envision Avatar as a step toward workflow systems that reason about their own orchestration rather than follow pre-fixed rules.

</details>


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [12] [Shift-Accumulate Attention: Multiplier-Free Query--Key Products for Transformer Decoding](https://arxiv.org/abs/2609.09208)
*Khubaib Ahmed,Amna Noor,Ahsan Ul haq*

Main category: cs.AR

TL;DR: Summary generation failed


<details>
  <summary>Details</summary>
Motivation: Motivation analysis unavailable

Method: Method extraction failed

Result: Result analysis unavailable

Conclusion: Conclusion extraction failed

Abstract: Power-of-two (PoT) quantisation turns a multiplication into a bit shift, so far only for the post-softmax attention--value product. The earlier and larger product, S=QK^T, has not been reformulated the same way. We quantise the key cache to a signed power-of-two fixed-point code, so that every scalar multiplication in QK^T becomes a sign flip, a bit shift and an integer accumulation. A fixed-point head-room F>= e_max turns every shift index r=F-e into a non-negative left shift, making the accumulation exact in integer arithmetic. We add a sub-power mantissa extension that halves the score error for one extra shift-add, a shift-exact online softmax whose running maximum lives in an integer log2 domain so that every rescaling is itself an exact shift, and a log-quantised AV product.
  Fused CUDA kernels in a 1.1B Llama decoder on an RTX 4090: at B=8, T=32k the 4-bit kernel is 4.60x faster than FP16 scaled dot-product attention with a 2.5x smaller KV cache, and batched decoding crosses over at B=64 to 1.22x FP16 throughput at 11.02 against 13.77 GB peak memory. A matched INT8 multiply--accumulate kernel on the same tiling reaches 5.29x: on a GPU with a hardware 4-way INT8 dot product, shift-accumulate is not faster than the MAC it replaces. An iso-storage control--an 8-bit shift code of exactly one byte per key, run through the identical kernel--prices the arithmetic substitution alone at 2.02x at comparable accuracy. The advantage of the PoT representation is therefore its density and the removal of the multiplier, not raw GPU throughput. A sweep over exponent width, mantissa levels, granularity and rounding gives the design lesson: nearest-PoT relative error is scale-free, so extra exponent bits buy nothing (eps_S=0.1302 at 3, 4 and 5 bits) and accuracy must come from mantissa terms.

</details>


### [13] [Academia x Industry: The Role of Fundamentals for Silicon in an AI Native Era](https://arxiv.org/abs/2609.09344)
*Vincent T. Lee,Armin Alaghi,Carole-Jean Wu,Sai Zhang,Brandon Reagen,Thierry Tambe,Jean Boufarhat,Matheus Trevisan Moreira*

Main category: cs.AR

TL;DR: 这是一篇立场/观点性文章（position paper），由学术界与工业界芯片从业者联合撰写，讨论 Agentic AI 如何推动"AI 原生芯片设计"（AI native silicon design）时代的转型，梳理其挑战、机遇与关键考量，并强调芯片设计基础功底在教学中重新变得重要。


<details>
  <summary>Details</summary>
Motivation: Agentic AI 正快速改变芯片设计与工程实践，转型速度快、范围广，使学术界与工业界从业者都感到难以适应。文章希望通过联合视角，为这一转型提供结构化框架，帮助社区理解并加速进入 AI 原生时代。

Method: 观点综述与立场阐述：以学术界 + 工业界联合视角，归纳转型过程中的挑战、机遇与设计考量；重申核心芯片设计基础（架构、电路、验证、EDA 等）的重要性与在新范式下的再定位；呼吁社区积累多样化观点。

Result: 文章未给出实验数据或量化结果；其"结果"是提出一组结构性观点：AI 原生芯片设计将重塑从架构探索、RTL/验证、物理设计到流片与工程流程的方法论，而扎实的硅设计基础是有效利用 Agentic AI 的前提。

Conclusion: 转型不可避免且正在进行；学术界与工业界需重新重视硅设计基本功，同时以多样化、互补的视角共同催化 AI 原生芯片设计时代；具体技术细节会因利益相关方而异，社区应容纳多元观点。

Abstract: Agentic AI is set to become one of the most transformational technologies in generations and materially change how we approach silicon design and engineering. The impact is being felt in real time amid a rapidly changing landscape, which can make it overwhelming for both silicon practitioners and academics to adapt to the AI native silicon design era. To add structure to how we navigate this transition, we provide a joint view from academia and industry silicon practitioners of the challenges, opportunities, and considerations we expect will catalyze how the community transitions into an AI native silicon future. In particular, we reemphasize the importance of core silicon design fundamentals in academic training and why they have renewed importance in research and industry practice for AI native silicon design. It is our hope that the views provided here will offer valuable and complementary perspectives to those in academia and industry to interpret, inform, and catalyze the transition to the AI native era. We expect that many similar and overlapping views will emerge, but the precise technical details will differ across stakeholders, so it is valuable for the community to amass a diversity of viewpoints.

</details>


### [14] [HLSFactory-Agent: Large-Scale Agentic HLS Dataset Construction from Academic and Open-Source Projects](https://arxiv.org/abs/2609.09519)
*Kaushik Chandana,Jay Imperatori,Tanmay Shukla,Justin Zhou,Stefan Abi-Karam,Callie Hao*

Main category: cs.AR

TL;DR: HLSFactory-Agent：用LLM agent（基于Pi框架，运行于Docker）从GitHub仓库/代码目录中自动抽取可独立综合的HLS设计，替代人工整理数据集；配套论文抓取与索引脚本，服务于HLS QoR建模与LLM基准构建。初步结果显示可从结构化代码库中成功抽取可综合设计。


<details>
  <summary>Details</summary>
Motivation: HLS领域缺乏大规模、多样化、超出常见社区benchmark的设计数据集。而LLM/深度学习驱动的硬件设计（QoR预测模型、HLS任务的LLM评测）对该类数据集需求迫切。现有数据集扩充仍依赖人工：在学术论文与开源代码中定位HLS设计，再从大型代码库中剥离出独立设计，过程易错、需专家知识、反复试综合、且每个仓库工程开销大，难以规模化。

Method: 1) HLSFactory-Agent：LLM agent，源自开源Pi agent框架，在Docker容器内执行（隔离、可复现），对每个抽取出的设计进行构建与评估（实际跑综合/HLS工具链）形成闭环；用户只输入GitHub链接或代码目录，输出为可直接接入HLSFactory数据集框架的独立HLS设计目录（turnkey）。2) 索引侧：开源脚本抓取并索引计算机体系结构、EDA、FPGA会议论文，标记可能实现或使用HLS设计的论文/仓库，加速人工发现与筛选，为agent提供输入源。

Result: 在已索引仓库的一个小子集上运行HLSFactory-Agent，初步验证可从结构化代码库中成功抽取出可综合（synthesizable）的设计。论文定位为早期/初步结果，未给出大规模抽取成功率、综合通过率、QoR分布或与人工基线对比的量化数据。

Conclusion: 工作把HLS数据集整理从"人工逐仓库工程"转为"agent化的容器内构建—评估闭环"，是LLM for EDA在数据基础设施层面的务实应用。工程价值主要在：降低per-repo抽取成本、提高大规模语料可扩展性，为QoR模型训练与HLS LLM基准提供数据供给；Docker+综合工具链的组合保证抽取结果可验证而非仅静态代码切割。局限与待验证点：目前规模小、无量化成功率/覆盖率，对非结构化或高度依赖外部IP/约束脚本的代码库鲁棒性未知，综合工具链依赖与许可证、抽取出设计的完整性（约束、testbench、参数化）仍可能需人工介入。代码与索引脚本已开源。

Abstract: Building large, diverse datasets of high-level synthesis (HLS) designs beyond common community benchmarks remains an open challenge. This challenge is made urgent by the rise of deep learning and LLMs for hardware design, which demand such datasets to train QoR models and benchmark LLMs on HLS tasks. Despite ongoing efforts to broaden sources, dataset curation still depends on manual work: locating HLS designs across academic publications and open source, then extracting standalone designs from larger codebases. The process is error-prone and demands expert knowledge, iterative testing, and substantial per-repository engineering.
  To address this, we present HLSFactory-Agent, an LLM agent that automates large-scale HLS dataset curation by extracting standalone designs from larger codebases. HLSFactory-Agent runs the open-source Pi agent framework inside Docker containers to build and evaluate each extracted design. This turnkey automation allows users to pass a GitHub link or code directory to HLSFactory-Agent and receive a folder of extracted HLS designs ready to be integrated into the HLSFactory dataset framework. Additionally, we provide open-source scripts to scrape and index papers from computer architecture, EDA, and FPGA conferences that possibly implement or use HLS designs, allowing for faster human discovery and curation of HLS designs for HLSFactory-Agent.
  We report initial results from running HLSFactory-Agent across a small subset of our indexed repositories, demonstrating successful extraction of synthesizable designs from structured codebases.
  We open source HLSFactory-Agent and indexing scripts at https://github.com/sharc-lab/hlsfactory-agent.

</details>


### [15] [Benchmarking Agentic HLS Design Tasks With HLS-Eval](https://arxiv.org/abs/2609.09526)
*Stefan Abi-Karam,Callie Hao*

Main category: cs.AR

TL;DR: 该论文将开源基准 HLS-Eval 从零样本评测扩展到基于 mini-swe-agent 的 Agent 化评测流程，允许 HLS 设计智能体使用文件编辑工具、调用 C++ 编译器自验证并迭代精修设计，同时记录轨迹以分析成本、token 与迭代次数；初步结果显示开源 LLM 配合 Agent 框架可解掉所有简单 HLS 代码生成任务，说明需要提高基准难度。


<details>
  <summary>Details</summary>
Motivation: 既有 LLM 用于硬件设计的工作多聚焦于 HDL 代码生成与编辑，且作者之前的 HLS-Eval 仅做零样本生成/编辑评测，未回答"智能体如何完成 HLS 设计任务"这一问题；同时简单任务已被轻易解决，亟需可观测、可迭代的 Agent 评测范式。

Method: 基于开源 mini-swe-agent 框架构建 Agent 化评测流：智能体可使用文件编辑工具、调用 C++ 编译器进行自验证，并在推理阶段迭代精修设计；全过程记录 agent trace，用于分析成本、token 用量与迭代次数，在既有 HLS-Eval 基准上给出初步结果。

Result: 开源 LLM 搭配 Agent 化 harness 可解出评测中所有简单 HLS 代码生成任务；通过对比通过/失败运行的轨迹，揭示了模型规模、token 用量、轨迹长度与设计通过率之间的关联。

Conclusion: 结果奠定了 Agent 化 HLS 设计的研究基础，表明需要随模型能力提升而扩展更难基准并发展新的 Agent 工具链。

Abstract: Large language models (LLMs) and AI agents are increasingly explored for hardware design, including high-level digital design. While most work targets code generation and editing for hardware description languages (HDLs), our prior work introduced HLS-Eval, an open-source benchmark for evaluating LLMs on high-level synthesis (HLS) design tasks. Those evaluations, however, focused on zero-shot generation and editing, leaving open how agents achieve HLS design tasks.
  We therefore extend HLS-Eval with an agentic evaluation flow built on the open-source mini-swe-agent framework. The flow lets HLS design agents use file-editing tools, invoke a C++ compiler for self-verification, and iteratively refine designs during inference, while logging agent traces for analysis of cost, token usage, and iteration count. We present initial results on the existing HLS-Eval benchmarks.
  In our initial evaluation, we find open-source LLMs paired with an agentic harness solve every simple HLS code generation task in our evaluation, underscoring the need to expand benchmark difficulty as model capabilities advance. Analyzing traces from passing and failing runs, we show how model size, token usage, and trajectory length relate to design pass rates. These results establish a foundation for agentic HLS design and motivate harder benchmarks and new agentic tooling as model capabilities progress.

</details>


### [16] [Differential Stochastic Simulated Annealing Processor for Fully Connected 2048-Spin Optimization](https://arxiv.org/abs/2609.09559)
*Naoya Onizawa,Md Mohaimenul Alam,Sean Smithson,Duckgyu Shin,Takahiro Hanyu*

Main category: cs.AR

TL;DR: 本文提出一种基于差分随机模拟退火（DSSA）的2048自旋全连接退火处理器架构，TSMC 28nm工艺，后布局3mm×4mm，500MHz时序收敛，集成16Mb SRAM权重存储。通过只对翻转自旋重算交互（活跃前沿）、自旋选择调度、优先级权重读取和跳过空闲步的温度控制，在保持全连接的同时加速稀疏更新，实现2000自旋问题2.7ms TTS、0.86mJ解能量、316mW（0.15mW/自旋）。


<details>
  <summary>Details</summary>
Motivation: 大规模组合优化（如Ising/QUBO求解）需要全连接退火硬件，但全连接导致交互计算与权重访存量随自旋数平方增长，传统串行数据通路退火器存在功耗高、TTS长的问题。作者希望在不牺牲全连接性的前提下，通过算法-架构协同降低有效工作量和能耗。

Method: 1）差分随机模拟退火（DSSA）：保留串行化数据通路以提升密度，仅在自旋翻转时重算其交互，将每步有效工作量收缩到活跃前沿；2）16个自旋共享一组随机数发生器，摊销随机噪声生成开销；3）自旋选择调度（spin-select scheduling）+ 优先级权重读取 + 可跳过空闲步的温度控制器，加速稀疏更新；4）16Mb SRAM权重存储；5）采用后布局仿真（post-layout simulation）评估。

Result: 后布局仿真下，2000自旋问题TTS为2.7ms，解能量0.86mJ，功耗316mW（0.15mW/spin）。相比先前全连接退火器的预期结果，功耗降低1.5倍，TTS能量降低3.5倍。500MHz时序收敛，后布局面积3mm×4mm（28nm CMOS）。

Conclusion: DSSA架构通过差分更新与稀疏活跃前沿调度，在不牺牲全连接性的条件下显著降低功耗与求解能量，展示了面向大规模组合优化硬件的可行性。但结论基于后布局仿真而非流片实测，实际芯片的时钟树、电源噪声与SRAM带宽限制可能进一步影响TTS与能效。

Abstract: A 2,048-spin fully connected annealing processor based on differential stochastic simulated annealing (DSSA) is presented as an architectural design in TSMC 28 nm CMOS with a 3 mm x 4 mm post-layout area. The processor closes timing at 500 MHz, integrates a 16 Mb SRAM weight memory, and amortizes stochastic noise across 16 spins with area-efficient random number generators. DSSA keeps a serialized datapath for density but recomputes interactions only for spins that flip, shrinking the effective workload to the active frontier during each annealing run. Spin-select scheduling, priority-based weight reads, and a temperature controller that skips idle steps accelerate sparse updates without sacrificing full connectivity. Post-layout simulation results show 2.7 ms time-to-solution (TTS) and 0.86 mJ energy-to- solution on 2,000-spin problems at 316 mW (0.15 mW/spin), achieving 1.5x lower power and 3.5x lower TTS energy than projected prior fully connected annealers. These results demonstrate the potential of the proposed DSSA architecture for large-scale combinatorial optimization hardware under post-layout evaluation.

</details>


### [17] [UNISON: A Co-Designed Near-Memory Scheduler of Session KV Residency for LLM Agents](https://arxiv.org/abs/2609.09643)
*Fan He,Yan Li,Xiaoyang Zeng*

Main category: cs.AR

TL;DR: 提出UNISON——面向LLM智能体循环的近内存事件驱动会话级调度器，将KV缓存淘汰（SPEAR）与分层放置（TIDE）统一为同一活跃度排序，用工具等待窗口驱动DMA迁移，在不牺牲系统开销的前提下大幅提升命中率并降低AMAT/TTFT。


<details>
  <summary>Details</summary>
Motivation: 智能体循环（计划—调用工具—等待—恢复）与多轮对话的访存模式存在本质差异：工具等待期间KV前缀仍然"存活"，且大量会话共享同一SRAM/HBM池。现有eviction/placement策略基于近期性、超时或身份（recency/timeout/identity），无法感知循环的机制信息，会把"正在等待"的活跃会话误判为冷数据而丢弃，导致重复prefill与KV重建。作者认为淘汰与分层本质上是同一会话级效率问题的两面，应与compute-mode优化正交处理。

Method: UNISON为近内存事件驱动调度器，核心是两个共享同一live ranking的机制：SPEAR（Survival-Penalty Eviction for Agent Return-gap）基于返回间隔均值与按轮次索引的hazard估计决定淘汰对象；TIDE（Tiering in Idle-window DMA Events）把观测到的工具等待时间当作DMA预算，决定哪些会话留在快层（SRAM）。两者共用live ranking，避免策略冲突。硬件为28nm CMOS调度核，150MHz、13.6mW、0.169mm²，用定点实现复现浮点排序（Kendall tau>0.998）。论文另做结构性必要性分析，论证该统一近内存设计无法拆成独立IP或纯软件实现，否则会重现已记录的失败模式。

Result: 在编码与通用任务基准、3个模型家族、共1415个会话/33596轮上测试：联合策略在每个trace上均为最佳非oracle方案；命中率提升0.3%–23.1%，AMAT降低22%–51%，长时程trace上TTFT降低58%–89%。硬件开销相对其管理的KV层级可忽略。

Conclusion: 论文把智能体工具的"等待期"重新定义为可被调度利用的信息与DMA预算，验证了淘汰与分层必须在会话级统一协同，而非各自独立优化。工程上指向一个可集成于内存侧的低功耗小面积调度核，为agent serving的KV管理提供可落地的软硬件协同路径。

Abstract: Large language models are increasingly composed into agent loops that plan, call tools, and resume the same task after each action. These loops press a shared memory hierarchy harder than conventional multi-turn chat, because they hold a growing key-value (KV) prefix across tool waits and place many sessions on one SRAM/HBM pool, so that eviction and hierarchical placement become a session-level efficiency problem orthogonal to compute-mode optimization. Existing proxies based on recency, timeout, or identity miss the mechanism information of the loop and therefore treat a live wait as a cold, discardable unit. We present Unified Native Inter-turn Session Orchestration Nexus (UNISON), an event-driven near-memory scheduler in which Survival-Penalty Eviction for Agent Return-gap (SPEAR) and Tiering in Idle-window DMA Events (TIDE) share one live ranking. SPEAR selects who leaves from a gap average and a turn-indexed hazard, while TIDE spends the observed wait as a DMA budget for who sits in the fast tier. On coding and general-mission benchmarks with three model families, totaling 1,415 sessions and 33,596 turns, the joint policy is the best non-oracle entry on every trace, raising hit rate by 0.3% to 23.1%, reducing AMAT by 22% to 51%, and lowering TTFT by 58% to 89% on long-horizon traces. A structural necessity analysis shows that the unified near-memory design cannot be decomposed into independent IPs or realized in software without re-introducing documented failure modes. The 28-nm CMOS scheduling core occupies 0.169 mm^2 at 13.6 mW and 150 MHz, a negligible overhead relative to the KV hierarchy it manages, reproducing the floating-point ranking at Kendall tau exceeding 0.998.

</details>


### [18] [Minimal Deadlock-Free Routing for Degree-Six Triangular-Lattice Meshes and Tori with Two Forbidden Turns](https://arxiv.org/abs/2609.09746)
*Zibo Diao,Rongxi Sun*

Main category: cs.AR

TL;DR: 本文研究六度三角格互连网络（有限六边形 mesh 与其周期 torus 商）在虫洞流控下的无死锁最小路由。为 mesh 提出使用 1 个虚通道、仅禁止两个转向的最小部分自适应路由；证明 torus 上同一两转向关系在任意 n≥3 时仍存在环状单 VC 资源 CDG，遂用 2 个 VC 加 Hamiltonian 坐标与分组 dateline 消除周期依赖，并给出全对连通性与资源 CDG 无环性证明。针对单条静态双向链路故障，通过旋转转向规则与同组两跳三角绕行维持连通性与原 VC 数，额外跳数不超过 1。


<details>
  <summary>Details</summary>
Motivation: 六度三角格网络路径多样性高，但六个方向使虫洞流控下的无死锁路由设计困难；同时 torus 的周期性会引入跨 dateline 的残余依赖环，现有方法难以在保持最小路由与低 VC 开销的同时消除死锁，并需兼顾链路故障容错。

Method: 统一六方向坐标系建模有限六边形 mesh 与周期 torus 商；构造仅禁止两个有向转向、使用 1 个 VC 的最小部分自适应路由关系；证明 torus 上每个源-目的对存在唯一最近格提升，但两转向物理路由关系在 n≥3 时单 VC 资源 CDG 成环；采用 2 个 VC 结合 Hamiltonian 坐标与分组专用 dateline（每组段至多穿越 dateline 一次）为 VC 标记的通道资源建立全局序以破除环；对单条预知静态双向链路故障，旋转转向规则并用以同组两跳三角绕行替代故障跳。

Result: 证明了两种物理路由关系的最小全对连通性；证明所提 1-VC mesh 与 2-VC torus 构造的完整资源 CDG 无环，即无死锁；故障场景下受限扩展保持全对连通性与原 VC 数量，相对健康最短路径的额外跳数至多 1。

Conclusion: 该工作给出六度三角格 mesh/torus 上低 VC 开销、最小且可证明无死锁的路由方案，并提供兼具连通性与低开销的单链路容错扩展，适用于片上网络与拓扑定制互连的无死锁路由设计。

Abstract: Degree-six triangular-lattice interconnection networks offer substantial minimal-path diversity, but their additional directions complicate deadlock-free routing under wormhole flow control. We study a finite hexagon-shaped mesh and its periodic torus quotient in a common six-direction coordinate system. For the finite mesh, we construct a minimal partially adaptive routing relation that uses one virtual channel and forbids only two directed turns. For the torus, we prove that every source-destination pair has a unique closest lattice lift, but that the same two-turn physical routing relation still has a cyclic one-VC resource CDG for every n >= 3. We eliminate this residual periodic dependency by combining two virtual channels with Hamiltonian coordinates and group-specific datelines. Each same-group segment crosses its dateline at most once, which permits a global rank on VC-labelled channel resources. We prove minimal all-pairs connectivity for both physical routing relations and acyclicity of the complete resource CDG for the proposed one-VC mesh and two-VC torus constructions. For a single static bidirectional link failure known before a routing epoch, we further rotate the turn rule toward the failed orientation and replace a failed hop by a same-group two-hop triangle bypass. This restricted extension preserves all-pairs connectivity and the original VC counts, with at most one additional hop relative to the healthy shortest-path distance.

</details>


### [19] [HBFSim: Fast and Faithful Simulation of High-Bandwidth Flash Under Real GPU Execution](https://arxiv.org/abs/2609.09800)
*Yanpeng Hu,Yiwei Yang,Yuanwu Zhu,Yusheng Zheng,Wei Zhang,Andi Quinn*

Main category: cs.AR

TL;DR: HBFSim 是首个在真实 GPU 上执行真实推理负载的同时，对 High-Bandwidth Flash（HBF）的时序、容量与热效应进行建模的评估平台；它通过改写 PTX 并门控 kernel 启动，将 issue 与 consumption 解耦，用真实硬件提供可掩盖访存的计算，时序数据来自实测器件而非参数表，结温同时决定 HBF 可持续带宽与强制刷新写回期限。平台在全部 6 个标定点上与实测器件完全吻合、零不安全启动，未修改的 vLLM 0.15.1 服务 Qwen3-30B-A3B 返回与无插桩基线相同的 token，快路径 2s 完成同一用例，比详细参考路径 44s 快 20.8 倍。


<details>
  <summary>Details</summary>
Motivation: LLM 服务受限于内存容量。HBF 把 NAND flash 堆叠进加速器封装内，位于 HBM 之下一个层级，规格已于 2026-08-03 发布，首批推理器件预计 2027 年初送样。但容量与数据放置决策不能等硅片到位：存储模拟器回放录制的访问序列、从不真正执行负载；GPU 模拟器不跑真实计算 kernel；cycle-accurate 模拟器连一次 LLM 推理都跑不完。因此缺少一个能在真实工作负载真实执行的基础上回答容量/放置决策的方法。

Method: HBFSim 在真实 GPU 上运行真实推理负载，并叠加 HBF 的时序、容量与热效应。实现上改写 NVIDIA 编译器输出的中间代码 PTX，并门控 kernel launch；把 issue 与 consumption 分离，使真实硬件提供的计算用于掩盖一次访问。时序来自对真实器件的测量而非参数手册；结温同时决定 HBF 能持续提供的带宽速率，以及强制 refresh 写回的 retention 期限。负载侧使用未修改的 vLLM 0.15.1 服务 Qwen3-30B-A3B。

Result: 在全部 6 个标定断点上与实测器件完全匹配，且零不安全启动；未修改的 vLLM 0.15.1 服务 Qwen3-30B-A3B 返回与未插桩基线完全一致的 token 标识符。设备快路径在 Qwen3-30B-A3B 用例上耗时 2s，而详细参考路径为 44s，加速 20.8 倍。

Conclusion: 在 HBF 器件送样之前，HBFSim 让设计者在真实工作负载下测量容量或放置决策，而不是去假设一个方案。其系统学贡献在于把"真实负载+真实计算"与"在研器件的时序/热约束"在同一个可执行流程中结合，为 HBF 层的容量规划与数据放置提供了可校准、可复现的决策依据；工程上，PTX 改写与 launch 门控是一种低侵入的硬件在环建模范式，可推广到其他尚未流片的分层存储/近存器件评估。

Abstract: Serving a large language model (LLM) is limited by memory capacity. High-Bandwidth Flash (HBF) stacks NAND flash inside the accelerator package, one tier below high-bandwidth memory (HBM); the specification was published on August 3, 2026, and the first inference devices are expected to sample in early 2027. Decisions about capacity and data placement cannot wait for silicon. No existing method settles those decisions: a storage simulator replaying a recorded access sequence never executes the workload, a GPU simulator does not run the real compute kernels, and a cycle-accurate simulator cannot finish one LLM inference run. We present HBFSim, the first evaluation platform that applies HBF timing, capacity, and thermal effects to a real inference workload while that workload executes on a real GPU. HBFSim rewrites PTX, the intermediate code NVIDIA's compiler emits, and gates kernel launches; issue is separated from consumption, so real hardware supplies the computation that hides an access. Timing comes from measurements of a real device rather than a parameter sheet, and junction temperature sets both the rate HBF sustains and the retention deadline that forces refresh writes. HBFSim matches the measured device exactly at all six calibration breakpoints, with zero unsafe launches, and an unmodified vLLM 0.15.1 serving Qwen3-30B-A3B returns the token identifiers of the uninstrumented baseline. The device fast path serves the same Qwen3-30B-A3B case in 2s against 44s on the detailed reference path, 20.8x faster. Before HBF parts sample, HBFSim lets a designer measure a capacity or placement decision under a real workload instead of assuming one.

</details>


### [20] [AMEND: Audited Margins Enable Nonblocking Drops in GPU-PIM LLM Decoding](https://arxiv.org/abs/2609.09823)
*Zuxiong Tan,Will Wei-Jen Wang,Wei Shao,Ali Karkehabadi,Houman Homayoun,Avesta Sasan*

Main category: cs.AR

TL;DR: AMEND 提出 GPU-PIM 协同的注意力解码方案：用历史步的 BLASST 判决边界预测当前 KV 块的稀疏判决，GPU 只取预测存活块，HBM 近 bank 的 PIM 单元并行打分被跳过的补集，栈级控制器融合两路观测并提前生成下一步掩码，从而在不阻塞当前 token 的前提下重新观测每一次预测丢弃。


<details>
  <summary>Details</summary>
Motivation: 自回归解码每步重读不断增长的 KV cache，长上下文注意力受 GPU 显存带宽限制。块稀疏注意力可跳过低贡献 KV 块，但现有选择器存在两条关键路径依赖：BLASST 这类基于当前 QK 乘积的 max-relative 块阈值判决仍需读入全部 K 块，等于没有省带宽；而 PIM 内联过滤器虽能就地判决，却把串行 PIM 阶段放到了关键路径上，拖慢每 token 延迟。核心矛盾是：只靠 GPU 读则带宽浪费，只在 PIM 决策则延迟串行化。

Method: 1) 预测式判决：用先前步审计得到的 margin 预测每个 KV 块的 BLASST 判决（保留/丢弃）。2) 数据路径解耦：GPU 仅 fetch 预测存活块；同时 HBM 近 bank 的 PIM 单元并行对被省略的补集做打分。3) 重新观测机制：栈级（HBM stack）控制器融合 GPU 存活块结果与 PIM 补集打分，更新预测器并提前生成下一步掩码，使被预测丢弃的块也能在非阻塞路径上被复核（不阻塞当前 token 的生成）。4) 离线调参：在 false-drop（误丢）预算约束下用约束贝叶斯优化选取工作点，平衡速度与质量。

Result: 在 LongBench 与 RULER 上保持接近基线的任务质量；仿真中 batch size 8、8K–64K 上下文下，相对 dense attention 取得 1.40–3.63× 端到端解码加速，动态解码能耗降低 28–66%。

Conclusion: AMEND 的系统贡献在于把"稀疏判决"从当前步关键路径上解耦：用跨步预测替代同步选择器，用 PIM 并行复核补集替代串行 PIM 阶段，并以栈级控制器实现预测-观测-更新的流水化闭环。对工程的启示是长上下文 decode 的瓶颈正从算力转向 HBM 带宽与选择器延迟，GPU-PIM 异构分工（GPU 做稀疏稠密主体、近 bank PIM 做廉价打分）配合误丢预算约束的离线调参，是可落地的高性价比路径；代价是依赖 HBM 近 bank 计算能力与可用的仿真/成熟 PIM 硬件，实际收益还取决于预测命中率与 PIM 带宽。

Abstract: Autoregressive large language model (LLM) decoding re-reads a growing key-value (KV) cache at every step, so long-context attention is bound by graphics processing unit (GPU) memory bandwidth. Block-sparse attention skips low-contribution KV blocks, but a selector that decides after the current query-key (QK) product, such as max-relative block thresholding (BLASST), still reads every K block, and a processing-in-memory (PIM) filter that decides from the current query places a serial PIM stage on the critical path. We present AMEND, a GPU-PIM attention design that removes both dependencies. AMEND predicts each block's BLASST verdict from margins audited at earlier steps, so the GPU fetches only predicted survivors while near-bank PIM units in high-bandwidth memory (HBM) concurrently score the omitted complement. A stack-level controller merges both observations, updates the predictor, and eagerly generates the next step's mask, so every predicted drop is re-observed without blocking the current token. Operating points are selected offline by constrained Bayesian optimization under a false-drop budget. Across LongBench and RULER runs, AMEND preserves near-baseline task quality; in simulation at batch size 8, it achieves $1.40$-$3.63\times$ end-to-end decode speedup and 28-66% lower dynamic decode energy than dense attention across 8K-64K contexts.

</details>


### [21] [SAGE: Semantic-Aware Geographic Error Recovery for AI Data Movement](https://arxiv.org/abs/2609.10126)
*Patrick S. Y. Hung,Zitong Wang,Zekai Zhang,Yu Hin Chan,Shengzhe Lyu,Ray C. C. Cheung*

Main category: cs.AR

TL;DR: SAGE是一种语义感知的互联错误恢复架构，将"是否值得重传"与"从何处重启重传"解耦：按比特显著性把故障分为Class-H（灾难性）/Class-M/L（有界/精度损失）三类，仅Class-H触发重传，并用源端区域表按故障地理分布自适应调整检查点间隔。在gem5 Garnet上相对固定34跳恢复方案将质量归一化终端时延降低30.1%。


<details>
  <summary>Details</summary>
Motivation: 传统AI互联对所有丢包/比特翻转统一保护并重传，但数值比特故障后果差异极大：低位尾数翻转近似量化噪声，而高位指数翻转会造成灾难性离群值或非有限值（NaN/Inf）。统一重传既浪费带宽与延迟，也未针对真正致命的故障。因此需要按"语义后果"而非"是否出错"来决定恢复策略。

Method: 1) 对BF16类数据，用工作负载校准的契约将故障划分为灾难性Class-H、有界Class-M与精度Class-L；2) 对Class-H施加"静默投递（silent-delivery）"约束，再以质量归一化终端时延Ψ_del为指标对可行策略排序；3) 源端本地维护区域表（region table），按故障地理分布自适应调整检查点间隔，在高误码区域缩短恢复段；4) Class-H触发受保护的NACK与整flit重传，Class-M/L不触发默认网络重传；5) 在gem5 Garnet中实现端点和重传协议，并在ASAP7中综合全流水线检查器。

Result: 在稳定的合成工作点、十随机种子的直接Garnet竞争中：Ψ_del相对固定34跳恢复降低30.1%（平均时延低28.0%，同时投递语义质量更好）。在高BER压力、相同负载下，SAGE保持队列有界而固定基线积压。DeiT-S通信trace在评估的非零BER下mean与p99.5时延也更低。CRC32解码试验给出同等的95%单原始报文Class-H静默投递上界3.18×10⁻⁷。

Conclusion: 该工作提出并验证了"语义分级+地理感知恢复"的互联容错范式：用极少的Class-H重传成本换取灾难性数值错误的有效拦截，同时兼顾延迟、队列稳定性与语义质量，并可硬件综合，具备在AI训练/推理互联中落地的工程可行性。

Abstract: AI interconnects typically protect and replay packets uniformly, yet numerical bit faults differ sharply in consequence: a low-order mantissa flip may resemble quantization noise, while a high-significance exponent flip can produce a catastrophic outlier or non-finite value. We present SAGE, a semantic-aware geographic error-recovery architecture that decouples whether a detected fault merits replay from where replay restarts. For BF16-like data, a workload-calibrated contract separates catastrophic Class-H faults from bounded Class-M and precision Class-L damage. It first applies a Class-H silent-delivery constraint, then ranks admissible policies by quality-normalized terminal latency, $Ψ_{\rm del}$. Independently, a source-local region table adapts checkpoint intervals to fault geography, shortening recovery segments in noisy regions. Detected Class-H failures may trigger protected negative acknowledgments and full-flit replay; Class-M and Class-L outcomes do not trigger default network replay.
  We implement SAGE's endpoint and replay protocol in gem5 Garnet and synthesize its fully pipelined checker in ASAP7. At a stable synthetic operating point, a ten-seed contention-faithful direct-Garnet campaign shows that SAGE reduces $Ψ_{\rm del}$ by 30.1% relative to fixed 34-hop recovery, combining 28.0% lower mean latency with improved delivered semantic quality. Under higher-BER synthetic stress at the same offered load, SAGE maintains bounded queues while the fixed baseline accumulates backlog. Application-derived DeiT-S communication traces also show lower mean and p99.5 latency at the evaluated nonzero BERs. Within the qualified operating envelope, CRC32 decoder trials yield a simultaneous 95% per-original Class-H silent-delivery upper bound of $3.18\times10^{-7}$.

</details>


### [22] [CertiFlash: A Formal Verification Framework for Flash Translation Layers in Computational Solid State Drives](https://arxiv.org/abs/2609.10347)
*Harshita Gupta,Mayank Kabra,Rakesh Nadig,Nika Mansouri Ghiasi,Sahand Divsalar,F. Nisa Bostanci,Ataberk Olgun,Konstantinos Kanellopoulos,Jisung Park,Haiyu Mao,Abdullah Giray Yaglikci,Mohammad Sadrosadati,Onur Mutlu*

Main category: cs.AR

TL;DR: CertiFlash 是首个面向 FTL（闪存转换层）的、在 Rocq 中机器化的形式化验证框架，用单一全局不变式覆盖映射、隔离、完整性与所有权，并把 FTL 建模为确定性状态机，一次性证明所有操作保持不变式且模型精化一个理想块设备；新设计只需证成 5 个假设（27–3,231 行 vs 16,489 行框架）。


<details>
  <summary>Details</summary>
Motivation: 存储中心计算（storage-centric computing）把计算推向 SSD 内部以削减数据搬运开销，但代价是要改 FTL 固件（地址转换、GC 策略等）。FTL 逻辑直接访问安全关键器件组件，因此即便功能正确，也可能在租户间泄漏数据、丢弃完整性标签、或把 flash block 错配给其他租户。作者先在 DaisyPlus OpenSSD 上指出错误 FTL 可在器件内部五个面上破坏状态。既有 FTL 验证工作有两个缺陷：(1) 只证功能正确性，隔离/完整性/所有权违规仍可通过验证；(2) 与单一 FTL 设计强绑定，每次修改都要重做全部证明。

Method: CertiFlash 在 Rocq（原 Coq）证明助手中机器化：将 FTL 抽象为确定性状态机，定义覆盖 mapping、isolation、integrity、ownership、allocation 的单一全局不变式；在通用 FTL 模型上一次性证明两条元定理——(i) 每个 FTL 操作都保持该不变式，(ii) 模型精化（refinement）一个理想块设备。对新设计，设计者只需证成关于自身操作的五条假设（hypotheses），而无需重做上述任一证明。

Result: 四个 case study 中，设计者仅在 16,489 行框架之上新增 27 至 3,231 行，验证工作量大幅下降；框架以开源形式发布，可用于修改型 FTL（尤其是存储中心计算场景）的机器校验安全性与正确性证明。

Conclusion: CertiFlash 把 FTL 验证从“逐设计、仅功能正确”提升为“框架一次证明 + 每设计证成 5 条假设”，同时覆盖安全属性，显著降低在 SSD 内做存储中心计算时的固件验证成本，是 FTL 及近数据计算可信化的重要基础设施。

Abstract: Data-intensive applications move large amounts of data from storage to the compute unit, incurring significant data movement overhead. Storage-centric computing reduces this overhead by moving computation near or inside solid-state drives (SSDs). Enabling it requires modifying SSD policies, e.g., address translation and garbage collection, which are part of the Flash Translation Layer (FTL), the SSD's firmware. Modifying the FTL is error-prone. Because FTL logic has direct access to security-critical device components, even a functionally correct FTL can leak data between tenants, drop integrity tags, or assign a flash block to the wrong tenant. We show that a faulty FTL can corrupt the device state at five surfaces inside the SSD, and demonstrate them on a DaisyPlus OpenSSD. Prior work verifies individual FTL designs, but has two limitations. (1) It establishes only functional correctness, so a modified FTL can violate isolation, integrity, and ownership and still pass verification. (2) It is tied to a single FTL design, so every modification requires redoing every proof.
  We propose CertiFlash, a formal verification framework for FTLs, mechanized in the Rocq proof assistant, that gives designers a machine-checked proof of security and correctness. CertiFlash models an FTL as a deterministic state machine with a single global invariant over mapping, isolation, integrity, ownership, and allocation. We prove once, over a general FTL model, that (i) every FTL operation preserves the invariant and (ii) the model refines an idealized block device. For a new design, a designer discharges five hypotheses about its own operations instead of redoing either proof. Across four case studies, a designer adds 27 to 3,231 lines against a 16,489-line framework, significantly reducing the verification effort. CertiFlash is open source.

</details>


<div id='cs.PF'></div>

# cs.PF [[Back]](#toc)

### [23] [PASCAL: A Phase-Aware Shared-Cache Model for Parallel Scans](https://arxiv.org/abs/2609.10515)
*Zhongchun Zhou,Chengtao Lai,Songtao Mao*

Main category: cs.PF

TL;DR: PASCAL 是面向并行扫描（parallel scan）类访存模式的共享缓存分析模型，能够在执行前预测多核异步进度发散导致的实际 cache miss rate 与 DRAM 流量，无需目标 trace、timing 或性能计数器，从而支持超大规模设计空间探索。


<details>
  <summary>Details</summary>
Motivation: 在 AI 加速器与 GPGPU 中，attention 的多个 query tile 复用同一 K/V block、GEMM 每行 tile 读取同一 panel 等「parallel scan」模式存在大量数据复用，理想情况下共享 cache 应大幅减少主存请求。但多核固有的异步性导致进度发散，实际 miss rate 与 DRAM traffic 远高于理想值，原有模型难以在无 trace 条件下准确刻画这一动态特征。

Method: 提出 PASCAL 共享缓存模型：显式建模多核间进度发散（progress divergence）的动态特性，并将其与 occupancy、软件流水深度、memory access data path 等因素组合关联，在执行前预测 cache miss rate；模型不依赖目标 trace、时序或硬件计数器，同时给出与替换策略无关的流量下界（policy-independent bound），界定任何替换策略都无法避免的 DRAM 流量。

Result: 在 NVIDIA GB10 GPU 上、覆盖不同软件流水深度、occupancy 与访存数据通路的 60 配置数据集上，PASCAL 取得 13.84% 的 MAPE，优于 physical-wave TileSight 的 44.79% 与精确符号化 SDCM 的 54.16%。

Conclusion: PASCAL 以低成本、无 trace 的方式准确预测并行扫描模式的缓存行为与 DRAM 流量，适用于 cycle-accurate 仿真不可行的大规模设计空间探索，其策略无关界还可作为流量优化的理论上限参考，对 AI 加速器缓存/DRAM 子系统设计具有实用价值。

Abstract: In modern AI Accelerators and GPGPUs, many concurrent cores repeatedly access the same shared data. This pattern occurs in attention, where different query tiles share the same K/V block, GEMM, where every tile in a row reads the same panel, and many other operators. We name this pattern parallel scan. Due to a significant amount of data reuse in this pattern, the cache is expected to capture as much data reuse as possible and largely reduce requests sent to the main memory for both performance and energy consumption concerns. However, in reality, because of the intrinsic asynchrony of multi-cores, the actual cache miss rate and DRAM traffic can be much higher compared to ideal cases. In this paper, we propose PASCAL, a shared-cache model for parallel scans. It is aware of the dynamic feature of progress divergence across multi-cores, correlate the divergence with the combination of different factors such as occupancy, and predicts the cache miss rate before execution. Because prediction needs no target trace, timing, or counters, PASCAL supports design-space exploration at scales where cycle-accurate simulation is impractical, and its policy-independent bound states how much traffic no replacement policy can avoid. A MAPE of 13.84% is achieved in a 60-configuration dataset with various software pipeline depths, occupancies, and memory access data paths on an NVIDIA GB10 GPU, against 44.79% for physical-wave TileSight and 54.16% for exact symbolic SDCM.

</details>


<div id='cs.OS'></div>

# cs.OS [[Back]](#toc)

### [24] [Violet: Enabling Full Virtualization for M-mode RTOS on RISC-V](https://arxiv.org/abs/2609.09833)
*Taro Kito,Ryosuke Yamamoto,Keisuke Horii,Hiroki Masuda,Koichi Mouri*

Main category: cs.OS

TL;DR: 该论文提出 Violet——面向 RISC-V 的嵌入式 hypervisor，将硬件虚拟化扩展与软件模拟相结合，使未经修改的 M-mode RTOS（FreeRTOS/Zephyr）可作为 VM 运行，并在 HiFive Premier P550 上验证了其与 Linux 等 GPOS 的共存能力，同时量化了 M-mode 模拟带来的 CSR 访问、定时器中断与上下文切换开销。


<details>
  <summary>Details</summary>
Motivation: 嵌入式场景常需 RTOS 与 GPOS 同时运行，或并发运行多个 RTOS 实例，ARM/x86 上已有 hypervisor 方案，但 RISC-V 的虚拟化支持（H 扩展）仅覆盖 U-mode 与 S-mode，M-mode 被排除在虚拟化层级之外。FreeRTOS、Zephyr 这类裸机/RTOS 通常直接运行在 M-mode，因此无法用现有基于虚拟化特性的方法将其放入 VM。论文的动机是在不修改 RTOS 源码的前提下，实现 M-mode RTOS 的虚拟化并与 GPOS 共存。

Method: Violet 的做法是把 RISC-V 硬件虚拟化（HS/VS 模式、两级页表、trap 转发等）与软件级 trap 模拟结合：对 guest 在 M-mode 下的特权操作进行拦截与模拟，尤其针对 M-mode CSR 访问、定时器/中断语义和特权指令，由 hypervisor 做翻译与状态维护，使 guest RTOS 认为自己仍运行于 M-mode。系统运行在 S-mode 上，通过 VS-mode 承载 GPOS，同时以模拟方式承载 M-mode RTOS，构成共存的多 VM 架构。实现落到 SiFive HiFive Premier P550 硬件，并用 RISC-V 架构测试套件验证模拟功能的正确性。

Result: 用 RISC-V 架构测试验证了 M-mode 模拟功能的正确性；在 HiFive Premier P550 上实测表明现有 RTOS 可在 Violet 的 VM 中直接运行，并能与 Linux 等 GPOS 共存。性能方面量化了 M-mode 模拟引入的开销，覆盖 M-mode CSR 访问、定时器中断延迟以及上下文切换三类关键路径。

Conclusion: Violet 表明在 RISC-V 上通过"硬件虚拟化 + 软件 M-mode 模拟"的混合路线，可以在不修改 guest 的前提下虚拟化原本被虚拟化规范排除的 M-mode RTOS，并实现与 GPOS 的共存。其主要贡献是填补了 RISC-V 嵌入式虚拟化对 M-mode 的支持空白；工程价值在于让现有 RTOS 二进制可直接迁移到混合关键性/多 OS 的嵌入式平台。代价是 M-mode CSR 访问与 trap 密集路径上的模拟开销，这意味着该方案更适合 CSR 访问不频繁的负载，或需要针对 CSR 访问做影子缓存/批处理优化。

Abstract: In embedded systems, complex configurations may be required, such as the simultaneous execution of a real-time operating system (RTOS) and a general-purpose operating system (GPOS), or the operation of multiple RTOS instances. Embedded system hypervisors have been studied and developed to meet these requirements for architectures like ARM and x86. RISC-V is experiencing growing adoption in embedded systems and faces similar needs. However, RISC-V's virtualization support targets only U-mode (where applications run) and S-mode (where general-purpose OSs run) as virtualization levels. The M-mode, where RTOSs like FreeRTOS or Zephyr run, is excluded from virtualization. This means that, similar to architectures like ARM, running an RTOS on a Virtual Machine (VM) using methods based on virtualization support features is impossible. Therefore, this paper proposes the Violet hypervisor. Violet combines RISC-V's virtualization features with software-based emulation, enabling the execution of unmodified M-mode RTOSs. Evaluation verified the validity of the M-mode emulation functionality using RISC-V architecture tests. Furthermore, this was implemented on the SiFive HiFive Premier P550 hardware, demonstrating that existing RTOSs can run on Violet's VM and that coexistence with GPOSs like Linux is also possible. The performance evaluation also quantified the overhead introduced by M-mode emulation on M-mode CSR accesses, timer interrupt latency, and context switching.

</details>


<div id='cs.NI'></div>

# cs.NI [[Back]](#toc)

### [25] [Improving 5G AI-RAN MCS Selection by Predicting Retransmissions](https://arxiv.org/abs/2609.09324)
*Tamerlan Aghayev,Maxime Elkael,Michele Polese,Reshma Prasad,Salvatore D'Oro,Yunseong Lee,Koichiro Furueda,Tommaso Melodia*

Main category: cs.NI

TL;DR: NOSTRAdAMUS 是一个预测式链路自适应(LA)框架，通过从 HARQ 历史预测下一无线帧是否发生重传，对底层策略选出的 MCS 施加修正，从而在不替换现有算法的前提下提升吞吐并降低重传。


<details>
  <summary>Details</summary>
Motivation: 5G NR 的链路自适应本质是被动的：依赖信道测量与 HARQ 反馈，在信道快速变化时迅速过时；测量噪声大、难以准确跟踪，且需馈入带反馈环的实时控制器，故障难排查。这导致实际部署多采用简单稳健算法，容忍滞后带来的过激进或过保守速率，以牺牲频谱效率换取可预测性。

Method: 从近期 HARQ 历史预测下一无线帧是否将发生重传；基于该预测对底层 LA 策略所选 MCS 施加修正，属于"叠加式"外挂组件而非重设计控制器。对比多种 ML 模型，最终采用 Gradient Boosting。数据在 X5G 测试床通过 OTA 采集，软件栈为开源 OAI 5G、NVIDIA Aerial，配 COTS O-RAN RU 与 UE。模型以 dApp 形式部署，进行 OTA 以及硬件在环信道模拟器上的评估，覆盖 3GPP TDL/CDL 信道、SISO/MIMO、行人及车载移动性。

Result: Gradient Boosting 整体准确率 82.9%，高置信度干预的正确率 94.2%，推理延迟 5.5 μs。在无需重训练的情况下，跨多种场景增强两种 SOTA LA 算法，goodput 提升最高 71.5%，重传减少最高 71.8%，体现鲁棒性与泛化能力。

Conclusion: 预测式、外挂式的 LA 增强路径可行且实用：亚微秒级（5.5 μs）推理开销可嵌入实时调度回路，且跨信道、天线配置与移动性场景免重训练泛化，说明该方案具备直接工程落地的潜力。但其预测精度上限（82.9%）与高置信度门槛下的覆盖率/召回权衡，仍决定实际收益边界，需在真实负载与多用户调度竞争下进一步验证。

Abstract: Link Adaptation (LA) in 5G NR is inherently reactive, relying on channel measurements and HARQ feedback that may become quickly obsolete when the channel changes quickly. This data is also noisy, making it hard to track accurately, and has to be fed to real-time controllers with feedback-loop effects which are hard to troubleshoot. This explains why most practical deployments select simple but robust algorithms, which accept that the lag can leave the scheduler operating at overly aggressive or unnecessarily conservative rates, trading spectrum efficiency for predictable performance. In this paper, we improve on this status-quo with NOSTRAdAMUS, a predictive LA framework which adds foresight to existing algorithms without replacing or redesigning them. NOSTRAdAMUS predicts whether a retransmission will occur in the next radio frame from recent HARQ history, and applies corrections to the Modulation and Coding Scheme (MCS) selected by the underlying policy. We benchmark several ML models and show that Gradient Boosting achieves 82.9% accuracy overall with high-confidence interventions that are correct 94.2% of the time, and an inference latency of 5.5 μs. We train the model based on data collected Over-the-Air (OTA) on the X5G testbed, using the open-source OpenAirInterface (OAI) 5G stack, NVIDIA Aerial, and COTS O-RAN Radio Units and User Equipments. The model is then deployed as a dApp, which we evaluate OTA as well as on various channels with hardware-in-the-loop channel emulators. This includes 3GPP TDL and CDL channels, SISO and MIMO configurations, and pedestrian and vehicular mobility. Our evaluation shows that without retraining, and across this variety of scenarios, the dApp augments two SOTA LA algorithms, and increases goodput by up to 71.5% while reducing retransmissions by up to 71.8%. This demonstrates the robustness and generalization capabilities of our approach.

</details>


### [26] [Efficient User Association and Wireless Scheduling with Shorter Time-Scale Rate Adaptation](https://arxiv.org/abs/2609.09387)
*Xiaoyi Wu,Huacheng Zeng,Bin Li*

Main category: cs.NI

TL;DR: 该论文研究跨时间尺度的无线用户关联/调度与速率自适应的联合设计，提出融合虚拟队列与UCB的MaxWeight型算法，实现累计吞吐次线性regret与公平性零违规，并给出低复杂度的pick-and-compare替代方案。


<details>
  <summary>Details</summary>
Motivation: 速率自适应的时间尺度远短于用户关联与调度，若分别独立优化会割裂两层决策，难以在保证用户公平性的同时最大化累计网络吞吐；现有方法缺少跨时间尺度的联合设计与理论保证。

Method: 在长时隙上用MaxWeight型用户关联与调度：权重中引入虚拟队列以追踪各用户的调度欠账（维持公平），并嵌入UCB估计；被选中的用户在短时隙上用UCB算法做速率自适应。针对MaxWeight需枚举所有可行调度（受干扰约束呈指数级、复杂度高）的问题，提出基于pick-and-compare(PC)的低复杂度替代算法，并通过真实数据trace仿真验证。

Result: 理论上证明所提算法累计regret按时间跨度的平方根增长（至多相差对数因子），且在某些时间帧后累计公平性违规为零；仿真基于真实数据trace验证了两种算法的有效性。

Conclusion: 跨时间尺度的联合设计可在保证长期公平性的同时逼近吞吐最优，PC近似把指数级调度搜索降为低复杂度采样比较，为无线资源管理系统提供了可实现的工程路径。

Abstract: Rate adaptation is a crucial mechanism in IEEE 802.11 networks and next-generation cellular systems. Since the time scale for rate adaptation is typically much shorter than that for user association and scheduling, we investigate a joint design of wireless user association and scheduling and rate adaptation across different time scales to maximize cumulative network throughput while ensuring desired fairness among users. We develop a MaxWeight-type user association and scheduling algorithm that integrates virtual queues -- tracking each user's scheduling debt to maintain fairness -- and Upper Confidence Bound (UCB) estimates in its weight measure. Each selected user then employs the UCB algorithm for rate adaptation on a short time scale. Our theoretical findings reveal that the proposed algorithm achieves cumulative regret that grows with the square root of the time horizon up to a logarithmic factor and results in zero cumulative fairness violation after a certain number of time frames. Furthermore, since the MaxWeight-type algorithm involves evaluating all the feasible schedules that can be exponential to the number of users due to the interference constraints, leading to high computational complexity, we introduce a low-complexity alternative utilizing the so-called pick-and-compare (PC) approach. We demonstrate the effectiveness of both algorithms through simulations based on real-world data traces.

</details>


### [27] [QPS-ToR: A Parallel Iterative Switching Algorithm for Reconfigurable Optical Datacenter Switching](https://arxiv.org/abs/2609.09400)
*Dongzhao Song,Qianru Yu,Jun Xu*

Main category: cs.NI

TL;DR: QPS-ToR 将可重构光数据中心网络（RODCN）的调度引擎从 NegotiaToR 的单轮 iSLIP 替换为滑动窗口算法 SW-QPS，仅需把三阶段 RGA 改为两阶段 RG，即在保持原工作流的前提下把吞吐从约 60% 提升到约 90%，并在 128 ToR 的真实负载仿真中取得最高 36% 吞吐提升与 82% FCT 下降。


<details>
  <summary>Details</summary>
Motivation: RODCN 的容量扩展能力受调度机制制约：流量无关（traffic-oblivious）方案天然限制吞吐；而最先进的流量感知方案 NegotiaToR 使用单轮 iSLIP 作为调度引擎，吞吐上限仅约 60%，且对所有源-目的对（SD pair）一视同仁地分配相同优先级，忽视队列长度差异，导致长队列得不到应有服务、排队时延与 FCT 恶化。

Method: 以 SW-QPS（原为 crossbar 调度设计的滑动窗口算法，单轮低复杂度即可达到约 90% 吞吐）替换 NegotiaToR 的调度逻辑。QPS-ToR 不改变 NegotiaToR 的既有工作流，仅将调度周期由三步 Request-Grant-Accept（RGA）改为两步 Request-Grant（RG）并引入滑动窗口机制，从而在单轮内完成匹配。评估采用 128 个 ToR 的流级（flow-level）仿真，使用真实数据中心负载，并在并行网络拓扑上与代表性 traffic-oblivious 方案 RotorNet 对比。

Result: 相比 NegotiaToR，QPS-ToR 吞吐最高提升 36%，流完成时间（FCT）降低 82%；在并行网络拓扑上稳定优于 RotorNet。

Conclusion: 该工作表明 RODCN 的瓶颈主要在调度算法而非硬件重构能力：把成熟的滑动窗口匹配算法移植进流量感知的 RODCN 工作流，可在单轮低复杂度下获得显著吞吐与尾时延收益。对工程实践的意义在于改动面小（仅调整调度周期结构）、与现有重配置机制兼容，属于低成本高回报的控制面优化，为后续在真实光交换硬件、混合拓扑与多租户负载下的验证提供了方向。

Abstract: Reconfigurable optical data center networks (RODCNs) have emerged as a promising solution for scaling DCN capacity, yet their scheduling mechanisms remain a performance bottleneck: traffic-oblivious schemes inherently limit throughput, while the state-of-the-art traffic-aware scheme, NegotiaToR, uses single-iteration iSLIP as its scheduling engine, which limits throughput to around 60% and treats all source-destination pairs with equal priority regardless of queue length. We propose QPS-ToR, which replaces NegotiaToR's scheduling logic with SW-QPS, a sliding-window algorithm originally proposed for crossbar scheduling that achieves around 90% throughput with a single low-complexity iteration. QPS-ToR operates within NegotiaToR's existing workflow, requiring only a revision of the scheduling cycle from three-step Request-Grant-Accept (RGA) to two-step Request-Grant (RG) with a sliding window mechanism. In flow-level simulations with 128 ToRs under realistic datacenter workloads, QPS-ToR achieves up to 36% higher throughput and 82% lower flow completion time (FCT) compared to NegotiaToR, and consistently outperforms RotorNet, a representative traffic-oblivious scheme, on the parallel network topology.

</details>


### [28] [Prototyping QoE-Aware Rate Adaptation in Cellular Networks with Commercial Applications](https://arxiv.org/abs/2609.09490)
*Szilveszter Nádas,Lars Ernström,Dan Druta,Igor Pruzhansky,David Lindero,Jonathan Lynam,Eric Petajan*

Main category: cs.NI

TL;DR: 本文提出一种“渐进式”QoE感知无线资源分配系统，仅依赖当前实验室可组装的能力（外部QoE测量+基于拥塞信号的速率引导），通过引入“复合空间复杂度”把视频空间复杂度与时变频谱效率合并为单一资源需求函数，并设计带每周期上限的增量重分配算法，从而在不修改商用实时视频应用的前提下逼近QoE感知共享，并给出向标准（IETF SCONE、CAMARA、Media over QUIC）演进的路线。


<details>
  <summary>Details</summary>
Motivation: 已有研究表明，QoE感知的资源共享相比速率公平分配可支持最多约3倍的并发会话数且质量可接受，但其所依赖的三项能力——面向QoE目标的编码、运行时空间复杂度估计、丰富的应用-网络API——在商用部署中尚不可用。因此需要一条可落地的演进路径，用今天可组装的能力近似实现QoE感知分配。

Method: 1) 将效用分配框架扩展到无线资源域，提出复合空间复杂度：把会话的视频空间复杂度与其时变频谱效率合成为单一资源需求函数；2) 针对使用基于速率的拥塞控制、自身无法测量QoE的商用实时视频应用，采用外部工具进行QoE测量，不改动应用；3) 设计增量重分配算法，施加每区间上限，同时编码拥塞控制算法的速率变化速度约束，以及“空间复杂度估计仅在当前速率附近可靠”的约束；4) 原型系统将外部QoE测量与基于拥塞信号的速率引导相结合。

Result: 构建出可用当今实验室能力组装的原型系统：无需修改商用实时视频应用即可实现QoE感知的资源分配，将外部QoE测量与拥塞信号驱动的速率引导结合；并给出从该原型到完全QoE感知资源共享的演进路径，把IETF SCONE、CAMARA、Media over QUIC等新兴标准映射到其逐步赋能的能力上。

Conclusion: 在QoE感知所必需的原生能力尚未商用化的情况下，可用外部QoE测量与拥塞信号速率引导的组合在无线域近似实现效用式资源分配；复合空间复杂度与带每区间上限的增量重分配是使该近似可行且稳定的关键机制；标准演进（SCONE/CAMARA/MoQ）将逐级补齐能力，最终过渡到完全QoE感知的资源共享。

Abstract: Prior work has shown that QoE-aware resource sharing for real-time interactive video can support up to three times more simultaneous sessions at acceptable quality compared to rate-fair allocation. However, the required capabilities (QoE-targeted encoding, runtime spatial complexity estimation, and rich application-network APIs) are not yet available in commercial deployments. In this paper, we take an evolutionary approach: we design a system that delivers QoE-aware resource allocation using only capabilities that can be assembled in a lab today. We extend the utility-based allocation framework to the radio resource domain by introducing composite spatial complexity, which combines a session's video spatial complexity with its time-variant spectral efficiency into a single resource demand function. To operate with commercial real-time video streaming applications that use rate-based congestion control and lack capability to measure QoE, we use external tooling for QoE measurements. We develop an incremental reallocation algorithm with per-interval limits that encode both the congestion control algorithm's speed constraint and that spatial complexity estimates are reliable only near the current rate. The resulting prototype combines external QoE measurements with congestion-signal-based rate steering and does not require modification to commercial applications. We chart an evolution path from this prototype toward full QoE-aware resource sharing, mapping emerging standards (IETF SCONE, CAMARA, Media over QUIC) to the progressive capabilities they enable.

</details>


### [29] [Automated Mobile Video Objective Testing System](https://arxiv.org/abs/2609.09579)
*Eric Petajan,Jonathan Lynam,Morey Antebi,Hessam Moeini,David Lindero,Lars Ernstrom,Gyanesh Patra,Szilveszter Nadas*

Main category: cs.NI

TL;DR: AMVOTS 是 AT&T 开发的 QoE 测量系统，可跨 DASH VoD、视频会议、云游戏等异构应用类型与多种网络条件统一测量 QoE，并被用作闭环原型系统来验证 QoE 感知的无线资源分配。


<details>
  <summary>Details</summary>
Motivation: 运营商希望用 QoE（体验质量）而非传统 QoS 指标（吞吐、时延、丢包）来指导蜂窝频谱资源分配，以提升频谱效率与用户主观体验。但难点在于：不同应用对网络损伤的敏感度差异极大（DASH VoD 主要受吞吐/缓冲影响且可做码率自适应，视频会议与云游戏则对时延、抖动、丢包极度敏感且无缓冲余量），因此需要一套能跨应用类型、跨网络条件统一测量 QoE 的系统，才能支撑闭环的无线资源调度实验。

Method: 论文提出 AMVOTS（AT&T 研发的 QoE 测量系统），核心是可扩展的架构，用以覆盖从 DASH VoD 到交互式应用（视频会议、云游戏）的大范围应用类型与网络条件。其设计目标：1）应用无关/可插拔的测量框架，按应用类型接入相应的 QoE 模型或指标采集探针；2）在真实或受控网络条件下采集 QoE 数据；3）作为闭环系统的测量侧组件——测量结果反馈给无线资源分配策略，形成 QoE 感知的调度/分配原型验证。

Result: 摘要为 overview 类描述，未给出量化结果；论文声称 AMVOTS 具备足够的灵活性以支持大量应用类型与网络条件，并展示了将其嵌入闭环以原型化 QoE 感知无线资源分配（QoE-aware radio resource allocation）的可行性。

Conclusion: 该工作更像是工业界系统/工具论文：把 QoE 测量从单应用、离线分析推进到多应用统一、可闭环在线的工程能力面，为 QoE 驱动的频谱优化提供测量基础设施；对运营商而言，其价值在于打通“QoE 测量 → 资源分配策略 → 反馈验证”的链路。

Abstract: Applying QoE analysis to optimize usage of cellular spectrum is of high interest to mobile network operators. A key challenge is to be able to perform QoE measurement across very different types of apps, from DASH VoD to interactive applications such as Video Conferencing and Cloud Gaming. This paper presents AMVOTS, a QoE measurement system developed by AT&T, which is flexible enough to support a large range of application types and network conditions. We also discuss using AMVOTS as part of a closed loop to prototype QoE-aware radio resource allocation.

</details>


### [30] [Contextual Bandit-Based Decomposition of Network Slice Requirements under Cumulative Resource Budget Constraints](https://arxiv.org/abs/2609.09624)
*Masaki Kobayashi,Akito Suzuki,Ryoichi Kawahara,Masahiro Kobayashi*

Main category: cs.NI

TL;DR: 本文将5G端到端网络切片的跨域SLA分解建模为"网络切片请求分解问题(NSR-DP)"，提出上下文约束核赌博机(CCKB)在线算法，用GP预测候选分解的奖励与资源占用，并用动态惩罚约束长期资源预算，在仿真中多数条件下优于基线。


<details>
  <summary>Details</summary>
Motivation: 分层NS管理中，E2E控制器需把租户NSR的E2E SLA分解为各域的域级SLA并委托域控制器分配资源。分解策略不佳会导致当前请求因不可行被拒，或把资源消耗集中在瓶颈域从而降低未来接纳率。在线运行需同时满足两点：(R1)控制长期资源预算、(R2)让分解适配每个到达NSR的性能目标与保障等级。

Method: 提出上下文约束核赌博机(CCKB)：针对R1，对趋于紧张(瓶颈)的资源提高使用惩罚，抑制消耗瓶颈资源的分解；针对R2，用高斯过程(GP)对当前NSR上下文下候选分解的奖励与资源用量做预测，从而按SLA的性能目标与保障级别选解。论文给出了所提形式化的高概率保证。

Result: 在拓扑、瓶颈与流量混合等多种5G仿真设置下，CCKB在绝大多数条件下优于基线方法。

Conclusion: CCKB给出了满足长期预算控制与SLA自适应两项要求的在线NSR-DP方案，并有理论保证与仿真验证；但属于控制面仿真层面的工作，与AI训练/推理系统栈的关联是间接的——其GP+约束赌博机的思路可迁移到集群/服务调度中的上下文感知在线资源分配与SLA约束决策。

Abstract: End-to-end (E2E) network slices (NSs) are provisioned across multiple domains of the 5G network. In hierarchical NS management, a tenant submits a network slice request (NSR), which specifies E2E service level agreement (SLA) requirements. Rather than managing these domains directly, an E2E controller decomposes each NSR into domain-level SLA requirements and delegates resource allocation to domain-specific controllers, which return feasibility and resource-consumption feedback. A poor decomposition policy can therefore cause rejection of the current request by producing infeasible requirements or reduce future admission opportunities by concentrating resource consumption in bottleneck domains. We call this decomposition-policy optimization problem the network slice request decomposition problem (NSR-DP). For practical operation, online approaches to NSR-DP have been proposed. Such approaches must jointly meet two requirements: (R1) control long-term resource budgets and (R2) adapt each decomposition to the performance targets and guarantee levels specified in the arriving NSR's SLA. To meet these requirements, we introduce contextual constrained kernel bandits (CCKB) as an online solution for NSR-DP. To address (R1), CCKB raises penalties for using resources that become tight, thereby discouraging decompositions that consume bottleneck resources. To address (R2), it uses Gaussian processes (GPs) to predict, for the current NSR, the reward and resource usage of candidate decompositions, allowing it to select a decomposition suited to the performance targets and guarantee levels. We establish high-probability guarantees for the resulting formulation and show through extensive 5G simulations across topology, bottleneck, and traffic-mixture settings that CCKB outperforms the baselines in the large majority of conditions.

</details>


### [31] [Can AI Agents Detect and Repair Artifact Drift in Network Experiments?](https://arxiv.org/abs/2609.09849)
*Tianzhu Zhang,Weichen Tao,Changgang Zheng,Yusheng Zheng,Long Chen,Xiaoyi Fan,Meikang Qiu*

Main category: cs.NI

TL;DR: 该文提出"工件完整性"(artifact integrity)概念与评测基准NetArtifactBench，用于衡量AI agent在修复网络系统实验记录时的可验证性、范围约束与可追溯性；52个注入不一致实例、23种agent配置、5980次输出评测显示平均通过率65.3%，但需跨工件恢复隐式关系并传播修改时无一运行时超过30%。


<details>
  <summary>Details</summary>
Motivation: 网络系统社区开始让AI agent在运维与实验环境中执行多步任务，但仅以"任务是否完成"评判不足：agent修改的实验记录本身必须保持可信。若记录中的断言失去证据支撑、超出证据范围或无法溯源，则下游复现、审计与运维决策都会失真。因此需要一个可度量"记录级可信性"的属性与基准，区分局部纠错与完整记录修复。

Method: 论文给出工件完整性的三项要求（断言有证据支撑、受限于证据范围、可通过编码支撑关系的工件溯源），并构建NetArtifactBench：从公开网络系统工件派生52个含注入不一致的实例，不一致类型从直接矛盾到跨工件的未声明隐式关系；在三种通用AI agent runtime上评测23种agent配置，采用确定性打分，累计5980个输出。

Result: 平均契约通过率为65.3%（5980个输出）；当修复需要恢复隐式关系并跨工件传播变更时，没有任何agent runtime超过30%通过率。结果呈现"局部修正"与"完整记录级修复"之间的显著能力边界。

Conclusion: 作者主张将工件完整性提升为AI agent在运维网络系统时的首要设计与评测要求。对工程实践而言，这意味着面向网络与AI基础设施的agent不应只做任务级成功率评估，还需引入记录/证据一致性、变更传播与溯源的契约式校验，可用于CI/审计流水线中的自动化守门与可观测性。

Abstract: In recent years, AI agents have evolved into capable assistants that carry out multi-step tasks in digital environments. The network systems community is beginning to explore these capabilities in operational and experimental settings. However, an agent operating in network systems should not be judged solely by whether it completes the immediate task. The experiment record it modifies must also remain trustworthy. We call this property artifact integrity: the record's claims must remain supported by the available evidence, confined to the scope established by that evidence, and traceable through the artifacts that encode their support.
  To make this property measurable, we introduce NetArtifactBench, which tests whether AI agents can repair inconsistent records derived from public network-system artifacts while preserving claims that remain supported. The benchmark contains 52 instances with injected inconsistencies ranging from direct contradictions to unstated relations spread across several artifacts. We evaluate 23 agent configurations across three general-purpose AI agent runtimes using deterministic scoring. The average contract pass rate is 65.3 % across 5,980 outputs, but no agent runtime exceeds 30 % when repair requires recovering implicit relations and propagating changes across artifacts. These results reveal a sharp boundary between local correction and complete record-level repair. Therefore, we argue that artifact integrity should become a first-class design and evaluation requirement for AI agents operating on network systems.

</details>


### [32] [Can AI Agents Deliver Verifiable Network-Wide Outcomes Across Authority Boundaries?](https://arxiv.org/abs/2609.10181)
*Tianzhu Zhang,Chih-Kai Huang,Meikang Qiu*

Main category: cs.NI

TL;DR: EvidenceNet 提出了面向多智能体网络自动化的"完成准入"(completion admission)问题：单个 agent 的配置动作成功并不能证明网络级意图达成。系统通过 broker 采集变更后观测证据、admission gate 校验证据的来源域、时效性与任务规则，并辅以 verifier agent 评估观测内容，从而判定协同操作是否真正完成。


<details>
  <summary>Details</summary>
Motivation: AI agent 已能通过受控运维接口发起网络配置变更，但真实运维网络跨多设备、多管理域，权限被划分为不同 scope。这种划分虽限制了错误动作的爆炸半径(blast radius)，却碎片化了评估全网结果所需的证据。一个 agent 提出的配置动作"执行成功"不等于远端设备按预期响应、路由变更到达所需设备；且合法观测在后续变更后会变陈旧。因此需要一个可信保证层，在宣告协同操作完成前，从所需 scope 采集当前观测并判定其是否共同支持运维意图。

Method: EvidenceNet 是运行时保证层(runtime assurance layer)，用于判定协同 agent 操作是否达成运维网络意图。核心组件包括：(1) broker——按 completion contract 采集变更后所需的观测；(2) admission gate——校验证据是否来自要求的 scope、是否保持时效(current)、是否满足 task rules；(3) verifier agent——对观测内容提供额外评估。

Result: 在真实运行的路由网络上实验表明：变更后状态检查能够识别出仅凭配置动作记录无法确认的成功结果；受控干预实验进一步显示，当本可接受的观测来源错误、被替换(substituted)或已陈旧(stale)时，EvidenceNet 会拒绝给出完成判定。

Conclusion: EvidenceNet 将网络自动化中的"执行完成"重新定义为可验证的"意图达成"，通过契约化的证据采集与多维度准入校验，弥补了配置动作记录与网络实际状态之间的语义鸿沟，为多域、多权限 agent 协同提供了可靠性保障。

Abstract: AI agents are increasingly involved in network automation, where they can initiate configuration changes through mediated operational interfaces and assess the resulting state. Nonetheless, operational networks usually span many devices and administrative domains. Realizing an operator's intent requires coordinating agents with distinct authority scopes that define the resources they can access, the operations they can invoke, and the network state they can observe. This division limits the blast radius of an erroneous action but fragments the evidence needed to assess the network-wide outcome. Successful execution of a configuration action proposed by one agent does not establish that remote devices responded as intended or that routing changes reached the required devices. A valid observation may also become stale after a subsequent change. Before the coordinated operation can be declared complete, a trusted assurance layer must collect current observations from the required scopes and determine whether they collectively support the operator's intended network-wide outcome.
  To address the completion admission problem, we present EvidenceNet, a runtime assurance layer for deciding whether coordinated agent operations have achieved an operator's network intent. Its broker collects the post-change observations required by a completion contract, and its admission gate checks that the evidence comes from the required scopes, remains current, and satisfies the task rules. A verifier agent provides an additional assessment of the observation content. Experiments on live routing networks show that post-change state checks recognize successful outcomes that configuration-action records alone cannot establish. Controlled interventions further show that EvidenceNet rejects completion when otherwise satisfactory observations have the wrong source, have been substituted, or are stale.

</details>


### [33] [HybridFLow: SDN-Orchestrated Client Partitioning for Hybrid Federated Learning](https://arxiv.org/abs/2609.10404)
*Osama Abu Hamdan,Rabin Pandey,Hao Che,Engin Arslan,Md Arifuzzaman*

Main category: cs.NI

TL;DR: HybridFLow 是一个由 SDN 驱动的闭环编排框架，将网络层全局视图引入混合联邦学习（同步+异步）客户端分组决策，通过每轮前的通信时间预估与轮后实测反馈迭代校准，显著缩短收敛时间。


<details>
  <summary>Details</summary>
Motivation: 跨机构（cross-silo）联邦学习部署在广域网上时，通信延迟主导每轮完成时间并放大掉队者（straggler）效应。混合 FL 通过组合同步与异步参与来缓解问题，但有效划分客户端依赖全局网络信息——共享瓶颈、链路利用率、路径竞争——这些是单个客户端无法观测的。现有方案（如 SmartFLow）缺少网络层全局可见性与预测-反馈闭环。

Method: HybridFLow 采用 SDN 控制器作为编排中枢：利用其全局拓扑视图，在每轮训练前为每个客户端生成经过校准的通信时间估计；据此把客户端划分为同步组与异步组，在轮次延迟（round latency）与更新陈旧度（staleness）之间做权衡；每轮结束后把实测通信时间回传控制器，持续修正后续预测，形成闭环控制。

Result: 在多种网络拓扑下：达到 80% 目标准确率比 SmartFLow 快 33–40%；平均轮次时长降低 30–40 秒；在非 IID 数据分布下 FedAsync 无法达到目标准确率，而 HybridFLow 可以。摘要未给出绝对收敛时间、参与方规模、拓扑参数与带宽配置细节。

Conclusion: 该工作把可编程网络（SDN 全局视图+遥测反馈）与混合 FL 调度耦合起来，论证了网络状态感知的分组策略对广域联邦训练收敛速度与轮次时延的实际收益。系统含义在于：FL 训练编排应下沉网络遥测能力，按预测-分组-实测-校准的闭环控制来动态平衡同步性与陈旧度；但可扩展性（控制器成为单点与瓶颈）、跨域/多租户下遥测可获取性、以及相对纯调度启发式的开销与鲁棒性仍有待验证。

Abstract: Cross-silo Federated Learning (FL) enables geographically distributed institutions to collaboratively train machine learning models without sharing raw data. In wide-area deployments, however, communication delays often dominate round completion time and exacerbate the straggler effect. Hybrid FL addresses this challenge by combining synchronous and asynchronous client participation, but effective partitioning requires visibility into network conditions such as shared bottlenecks, link utilization, and path contention that individual clients cannot observe. We present HybridFLow, a closed-loop SDN-driven orchestration framework that integrates network-layer intelligence directly into hybrid FL. Leveraging the SDN controller's global topology view, HybridFLow generates calibrated per-client communication-time estimates before each training round and uses them to partition clients into synchronous and asynchronous groups while balancing round latency and update staleness. After each round, measured communication times are fed back to the controller to continuously refine future predictions. Experimental results across multiple network topologies show that HybridFLow reaches 80% target accuracy 33-40% faster than SmartFLow and reduces average round duration by 30-40 seconds, while FedAsync fails to reach the target accuracy under non-IID data distributions.

</details>


### [34] [HAPS-RIS or HAPS-Relay: Which Outperforms Under Impairments with NOMA in 6G NTN?](https://arxiv.org/abs/2609.10468)
*Bilal Karaman,Faicel Khennoufa,Ilhan Basturk,Metin Ozturk,Ferdi Kara,Sezai Taskin,Halim Yanikomeroglu*

Main category: cs.NI

TL;DR: 该论文对比了HAPS辅助NOMA系统中RIS与中继站(RS)两种方案在硬件损伤(HWI)和不完美CSI下的性能，结论是RIS凭借无源特性避免噪声放大，在总速率和能效上均优于半双工RS，且可通过大规模单元部署补偿CSI误差。


<details>
  <summary>Details</summary>
Motivation: 高空平台站(HAPS)被视为6G覆盖增强手段，而RIS与RS是两种候选的中继/增强技术。实际系统存在硬件损伤与非理想CSI，已有研究多假设理想条件，缺乏对RIS与RS在HAPS-NOMA场景下、受损伤条件下的公平性能对比与设计准则。

Method: 建立HAPS辅助NOMA下行系统模型，分别采用RIS(无源反射)与半双工RS(有源放大转发)两种实现；显式建模HWI(发射/接收失真噪声)与不完美CSI(信道估计误差)；在给定NOMA功率分配与SIC解码顺序下推导总速率与能量效率表达式，并通过RIS单元分配策略与用户空间分布参数进行数值仿真对比。

Result: 在非理想条件下，HAPS-RIS的总速率与能效均优于HAPS-RS，原因在于无源反射不放大噪声；RIS单元分配方式与用户空间分布显著影响NOMA性能，用户间距增大且单元分配合理时可增强信道差异、提升系统效率；HAPS-RIS对不完美CSI更敏感，但通过大规模RIS单元部署可有效补偿性能损失，仍保持相对半双工RS的优势。

Conclusion: 为损伤感知(impairment-aware)的HAPS辅助6G通信系统给出设计准则：优先采用RIS而非半双工RS，重视RIS单元分配与用户几何布局，并以规模换性能来抵消CSI误差影响。该工作属通信理论/系统建模层面，对AI基础设施的直接影响有限，主要间接价值在于RIS波束赋形带来的超大规模阵列优化问题。

Abstract: This paper investigates the performance of high-altitude platform station (HAPS)-assisted communication systems employing either reconfigurable intelligent surfaces (RIS) or relay stations (RS) under non-orthogonal multiple access (NOMA) scheme. Practical system impairments, including hardware impairments (HWI) and imperfect channel state information (CSI), are explicitly considered. The results show that HAPS-RIS outperforms HAPS-RS in terms of both sum-rate and energy efficiency under non-ideal conditions due to its passive nature, which avoids noise amplification. Furthermore, it is demonstrated that RIS element allocation and user spatial distribution significantly impact NOMA performance, where increased user separation and proper allocation enhance channel disparity and improve system efficiency. Despite its higher sensitivity to imperfect CSI, HAPS-RIS can effectively compensate for performance degradation through large-scale RIS element deployment, maintaining a performance advantage over half-duplex RS-based systems. These insights provide useful design guidelines for impairment-aware HAPS-assisted 6G communication systems.

</details>
