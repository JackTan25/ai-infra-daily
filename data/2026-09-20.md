<div id=toc></div>

# Table of Contents

- [cs.DC](#cs.DC) [Total: 13]
- [cs.AR](#cs.AR) [Total: 9]
- [cs.PF](#cs.PF) [Total: 5]
- [cs.NI](#cs.NI) [Total: 6]


<div id='cs.DC'></div>

# cs.DC [[Back]](#toc)

### [1] [Detecting Soft Errors in Parallel Software with LLM-tuned Instruction Duplication](https://arxiv.org/abs/2609.19531)
*Yafan Huang,Guanpeng Li*

Main category: cs.DC

TL;DR: PaRID 是一个仅需编译期介入的软件指令复制软错误检测框架，面向含串行与并行混合区域的并行程序，结合并行感知代码变换与 LLM 调优的性能建模，把保护开销从平均 162.79% 降到 59.84%，最高获得 5 倍加速且保持完整检测能力。


<details>
  <summary>Details</summary>
Motivation: 硬件软错误（单粒子翻转等）在并行程序中的检测通常依赖指令复制（Instruction Duplication），但已有方案要么只支持纯串行代码，要么需要昂贵的动态 profiling 来指导保护决策。并行程序中串行区与并行区交错、线程间冗余分担、共享数据结构等特性，使朴素复制带来极高开销（平均 162.79%），在长时间训练/推理任务上不可接受。作者希望在不依赖运行时 profiling 的前提下，用编译期静态决策实现低开销、全覆盖的软错误检测。

Method: 1) 离线特征研究：对并行程序做刻画，提炼出八条可推广的发现（如串并行区保护收益差异、冗余放置位置对开销与覆盖率的影响等），作为设计依据。2) 并行感知代码变换：在编译期对串行区、并行区分别施加指令复制策略，识别可跨线程/跨迭代摊销的保护结构，并处理共享内存与同步点的一致性。3) LLM 调优的性能建模：用大模型辅助构建/标定开销预测模型，替代传统动态 profiling，在编译期选择保护粒度与冗余复制方案，使开销最小化同时不牺牲检测有效性。整体为纯编译期方案，无运行时插桩与采样开销。

Result: 在 NPB 基准上，保护开销平均从 162.79% 降至 59.84%（约 2.7 倍下降），相对基线最高取得 5 倍加速，且错误检测有效性保持完整（full detection effectiveness）。

Conclusion: PaRID 表明并行程序的软错误检测可以完全在编译期完成决策，通过并行感知变换加 LLM 辅助性能建模即可在覆盖率和开销之间取得更好折中，避免了动态 profiling 的成本。对长期运行的分布式训练/推理等场景，这类"零运行时成本、可控开销"的可靠性方案具有直接的工程落地价值，其八条经验性发现也可迁移到其他冗余与容错编译优化中。

Abstract: We propose PaRID (PaRallel Instruction Duplication), a software-directed soft error detection framework that requires only compile-time effort for multithreading parallel programs. PaRID addresses two key challenges: supporting parallel programs with mixed serial and parallel regions and minimizing performance overhead without relying on costly dynamic profiling. It combines parallel-aware code transformation with LLM-tuned performance modeling, guided by eight generalizable findings from an offline characterization study, to enable fast soft error detection in parallel applications. Evaluation on NPB benchmarks shows that PaRID reduces protection overhead from 162.79% to 59.84% on average and achieves up to 5x speedup while maintaining full error detection effectiveness.

</details>


### [2] [Syndrome Decoding for Silent Data Corruption in Quantized Integer GPU Arithmetic](https://arxiv.org/abs/2609.19743)
*Pranav Napolean,Vikas Srivastava,Napolean Periathambi*

Main category: cs.DC

TL;DR: SProbe：一种运行在厂商 GEMM 输出之后的“尾随校验”内核，用 61 位素数域上的随机 Freivalds 门控（漏检率 ≤ 2^-141）检测 INT32 累加器中的静默数据损坏，并借助多素数幂和校验 + Reed-Solomon 解码链定位出错列与精确幅值，支持最多每行 4 个碰撞错误，随后可原地修复或重算。


<details>
  <summary>Details</summary>
Motivation: GPU tensor core 的 INT32 累加器既无 parity 也无 ECC，瞬态故障会产生“合法但错误”的整数且不触发任何中断（SDC），对量化推理（INT8 LLM 等）是隐蔽的正确性风险。现有校验和类 ABFT（如 TR-ABFT、加权网格码）只能给出二值判定：无法指出出错元素及其幅值；且未加权行/列校验和对“在两个轴上都相互抵消”的错误天然盲区，加权网格码虽能检出却无法纠正。

Method: SProbe 作为 GEMM 之后的尾随验证内核，不修改厂商库（cuBLASLt）：1) 随机 Freivalds 门控，在 61 位素数域上用三个独立求值点校验输出，把漏检概率压到 2^-141；2) 门控触发后，对每行做三个素数下的幂和校验（power sum syndrome），用 Berlekamp-Massey + Chien search + Forney 的 RS 解码链恢复出错列与精确幅值，每行可解最多 4 个碰撞错误；3) 支持原地修复累加器或重算 GEMM。

Result: 在 NVIDIA H100 上，覆盖 7 类故障、4 种矩阵规模，SProbe 检出全部注入故障，包括 TR-ABFT 永远检不出的构造模式，以及加权网格码能检出但无法纠正的模式。门控开销：N=16384 时为 cuBLASLt GEMM 时间的 49%，N=65536 时为 11%。在 INT8 医疗 LLM 中，保护消除了全部观测到的静默损坏，吞吐代价 30%。此外实测显示：所有配置下重算都比重建式原地恢复更快；恢复开销主要由“诊断”而非“修复”主导；论文还报告了验证器自身验证过程中发现的缺陷。

Conclusion: SProbe 把量化推理 INT32 累加器的 ABFT 从“二值检测”推进到“可定位、可定量的诊断与恢复”，在 H100 上以可接受的相对开销（随 N 增大摊薄至约 11%）实现高置信检测与最多每行 4 错误的精确纠正，具备作为生产级 GEMM 后校验/自愈层的工程潜力。

Abstract: Quantized neural network inference runs integer matrix multiplications on GPU tensor cores, and the INT32 accumulators inside those cores have neither parity nor ECC. A transient fault in this datapath returns a valid but wrong integer and raises no interrupt. Checksum based Algorithm Based Fault Tolerance (ABFT) can detect such silent data corruptions (SDCs), but its verdict is binary. It cannot identify the corrupted element or its magnitude, and unweighted row and column checksums are blind by construction to errors that cancel on both axes. We present SProbe, a trailing verification kernel that reads the output of an unmodified vendor GEMM. A randomized Freivalds gate with three independent evaluation points in a 61 bit prime field misses a nonzero error with probability at most $2^{-141}$. When the gate fires, per row power sum syndromes over three primes are decoded with the Reed Solomon chain of Berlekamp Massey, Chien search, and Forney, recovering the column and exact magnitude of up to four colliding errors per row. SProbe then repairs the accumulator in place or recomputes the GEMM. On an NVIDIA H100, SProbe detects every injected fault across seven fault classes and four matrix sizes, including constructed patterns that TR-ABFT never detects and patterns that a weighted grid code detects but cannot correct. The gate costs 49% of the cuBLASLt GEMM time at N=16384 and 11% at N=65536. In an INT8 medical LLM, protection eliminates all observed silent corruptions at a 30% throughput cost. Our measurements also show that recomputation is faster than in place recovery in every configuration we tested, that diagnosis rather than repair dominates recovery cost, and we report the defects we found while validating the verifier itself.

</details>


### [3] [Sketching the Error, Not the Product: Post Hoc Fault Recovery for Half Precision GPU Matrix Multiplication](https://arxiv.org/abs/2609.19758)
*Pranav Napolean,Vikas Srivastava,Napolean Periathambi*

Main category: cs.DC

TL;DR: FP-Sketch 是一个运行在未经修改的 Tensor Core GEMM 之后的浮点校验器，用 sum sketch 做逐调用检错，用 hashed first moment sketch + 独立重算做多错定位，并通过运行时测量 sketch 噪声来适配 bucket 数量。


<details>
  <summary>Details</summary>
Motivation: 加速器静默数据损坏（SDC）会打断大规模训练，但现有部署的缓解手段以整节点为粒度，代价过高。单 GEMM 的 ABFT 需要侵入 kernel 或编码操作数，且每个 checksum 最多只能定位一个错误。此外，浮点下 sketch 定位的精度受噪声而非哈希桶碰撞限制，这一噪声特性此前未被刻画。

Method: 在未修改的 Tensor Core GEMM 之后运行校验器，half 精度操作数按 FP32 累加与交付。sum sketch 在每次调用上检测损坏；hashed first moment sketch 通过独立重算确认，按构造无误报地定位多个损坏项，每个故障给出一组坐标与幅值用于集群诊断。用 NVBit 做指令级注入，对比"输出侧注入"与"活跃累加器内 upsets"两种故障分布。

Result: 浮点 sketch 噪声而非桶碰撞限制定位；噪声常数依赖 BLAS 与操作数格式，方阵乘积下桶数需按 n^2.57 增长。按实测噪声（而非拟合的 n 幂次）定桶数，八个 transformer 形状上的恢复率从 0.402 升至 1.000，运行时测量噪声可自适应 kernel 与模型。NVBit 显示活跃累加器中的 upset 常仅为典型条目的 2%~9%，是输出侧注入无法产生的分布；输出侧注入可恢复全部故障，而按典型幅值定尺寸的引擎在 NVBit 下仅恢复 0.550，按实测幅值定尺寸恢复到 1.000。Llama-2-7B 上保护 MLP down projection，消除 2048 次位翻转造成的困惑度损伤的 99.4%（BF16）与 99.9%（FP16），干净路径探针开销 0.78~3.06 ms，对应 GEMM 为 0.35~12.47 ms。

Conclusion: FP-Sketch 证明了后置、非侵入式的浮点 sketch 校验可以在不修改 kernel、不编码操作数的前提下实现多错定位，其关键工程结论是定位上限由 sketch 数值噪声决定，必须以实测噪声而非理论幂律来定桶数，否则在真实指令级故障分布下恢复率会大幅塌陷；同时故障粒度应从整节点下探到单个 GEMM 调用与具体条目。

Abstract: Silent data corruption (SDC) from defective accelerators now interrupts large scale training, yet deployed mitigations act on whole nodes. Algorithm based fault tolerance (ABFT) for a single GEMM has to be fused into the kernel or encode the operands, and it localizes at most one error per checksum. We present FP-Sketch, a verifier that runs after an unmodified tensor core GEMM whose half precision operands are accumulated and delivered at FP32. A sum sketch detects corruption on every call. Hashed first moment sketches, confirmed by independent recomputation, then localize several corrupted entries with no false positives by construction, and each fault yields a coordinate and a magnitude for fleet diagnosis. In floating point, sketch noise rather than bucket collisions limits localization. We measure that noise and find that its constant depends on the BLAS and the operand format and that the bucket count must grow as $n^{2.57}$ for a square product. Sizing the bucket count by measured noise rather than by a fitted power of $n$ raises recovery on eight transformer shapes from 0.402 to 1.000, and measuring the noise at run time adapts the bucket count to the kernel and the model. Instruction level injection with NVBit shows that upsets in a live accumulator are often only 2 to 9% of a typical entry, a population that output side injection cannot produce. Output side injection recovers every fault, while under NVBit the same engine sized for faults of typical magnitude recovers 0.550, and sizing for the measured magnitudes restores 1.000. On Llama-2-7B, guarding the MLP down projections removes 99.4% (BF16) and 99.9% (FP16) of the perplexity damage caused by 2048 bit flips, and the clean path probe costs 0.78 to 3.06 ms against GEMMs of 0.35 to 12.47 ms.

</details>


### [4] [Safe Exploration of Arbitrary Dynamic Dangerous Networks](https://arxiv.org/abs/2609.19845)
*Caterina Feletti,Paola Flocchini,Giuseppe Prencipe,Nicola Santoro*

Main category: cs.DC

TL;DR: 本文研究危险动态网络（含一个或多个黑洞）上的安全探索问题：在时间连通性假设下，分别给出无需先验知识的 oblivious 智能体永久安全探索算法，以及具备唯一 ID 与持久内存、已知安全节点数的可终止安全探索算法，并证明在动态网络中不可能仅正确标记通往黑洞的端口。


<details>
  <summary>Details</summary>
Motivation: 动态网络（时变拓扑）下的分布式探索此前由 Gotoh 等（2021）在时间连通性这一最弱必要假设下开创，但其工作未考虑节点可能消失/失效的场景。黑洞模型是刻画"节点失效且进入即永久失联"的经典抽象，对容错探索具有重要意义。因此本文把探索问题推广到含黑洞的危险动态网络，并探究在何种假设条件下安全探索可解、其下界何在。

Method: 1) 模型：动态图 + 时间连通性假设（任意两节点无限次被时间路径连通）；网络可能含一个或多个黑洞，智能体进入即被永久困住；安全探索要求访问所有安全节点且至少一个智能体不被困。2) 永久探索情形：采用 oblivious（无记忆）智能体、无节点/智能体 ID、无网络与团队信息，在 semi-synchronous 调度器下，给出满足必要（且充分）智能体数量时的算法。3) 终止情形：智能体具备唯一 ID 与持久内存，且已知安全节点数，给出可安全探索并终止的算法。4) 下界：证明在上述两类设定下均不可能让团队正确标记出仅通向黑洞的端口。

Result: 证明了在给定必要（且充分）智能体数量时，oblivious 智能体在无任何先验知识、无 ID、semi-synchronous 调度下可永久探索所有安全节点；给出在智能体有唯一 ID、持久内存且已知安全节点数时能安全探索并终止的算法；并证明无论哪种情形，动态网络中智能体都无法正确标记仅指向黑洞的端口。

Conclusion: 该工作将动态网络探索扩展到含黑洞的容错场景，给出了在极弱能力假设（oblivious、无 ID）下永久安全探索的可行性结果与可终止算法，同时给出"端口标记"不可能性结论，明确了动态拓扑下黑洞定位的内在限制。

Abstract: Given a team of agents on the nodes of a graph-based network, the exploration problem requires each node to be visited by at least one agent. In the classical distributed setting of static networks, agents do not know the topology of the network; in the more recently investigated setting of dynamic networks, they may have prior knowledge about the graph class (e.g., trees, tori, rings) or other parameters (e.g., number of nodes). Gotoh et al. (2021) are the first to study the exploration problem of arbitrary dynamic networks, under the necessary minimal assumption that any two nodes will be connected by a temporal path infinitely often (temporal connectivity assumption). In this paper, we extend their study of exploration under temporal connectivity by considering dangerous dynamic networks, i.e., containing possibly one or more black holes. Whenever an agent enters a black hole, it will be trapped forever. The safe exploration problem requires a team to explore all the safe nodes, ensuring that at least one agent will never be trapped in a black hole. We first prove that, given the necessary (and sufficient) number of agents, a team of oblivious agents can perpetually explore the safe nodes without any prior knowledge of the network or the team, without agent or node IDs, under semi-synchronous schedulers. Then, we provide an algorithm that enables agents to safely explore the network and terminate. In this case, agents are equipped with unique IDs and persistent memory, and they know the number of safe nodes. Yet, in both cases, we prove that it is impossible for a team of agents on dynamic networks to correctly mark only the ports leading to black holes.

</details>


### [5] [Xronos: Heterogeneity-Aware Tensor Parallelism for Collaborative LLM Fine-Tuning on Edge CPUs](https://arxiv.org/abs/2609.19909)
*Wonmi Choi,Sunjae Park,Dohyeok Kwon,Zhixiong Niu,Yeonho Yoo,Chuck Yoo,Gyeongsik Yang*

Main category: cs.DC

TL;DR: Xronos 是面向异构 CPU 边缘设备的协同微调框架，以张量并行（TP）替代流水线并行（PP）作为执行主干，解决 CPU 上计算与通信争抢导致的停顿，并通过轻量 profiling + 异构感知张量切分缓解慢节点瓶颈，微调时间降低 18%（对比 TP）至 56%（对比 PP），设备空闲比降低约 5.9×。


<details>
  <summary>Details</summary>
Motivation: 协同微调可在数据不出本地的前提下让边缘设备适配领域数据。但 SOTA 方案多面向 GPU 边缘设备并依赖流水线并行（PP）。大量边缘平台（IoT 网关、智能家居中枢、车载计算单元）以 CPU 为主：单一 CPU 同时承担模型计算与通信，造成严重 CPU 争抢，实测平均计算停顿率比 GPU 设备高 5.75×，PP 因此失效。改用张量并行（TP）可分离计算与通信以缓解争抢，但现有 TP 假设设备同构；在异构 CPU 设备上该假设会导致快节点在同步点等待慢节点时空闲高达 34%。

Method: 以 TP 作为执行主干，替代 PP；引入轻量级 profiling 在线刻画各设备算力/通信能力；基于 profiling 结果做异构感知的张量分区（heterogeneity-aware tensor partitioning），按设备能力非均匀切分张量以缩小同步点上的 straggler 拖尾；整体构成面向异构 CPU 边缘设备的协同微调框架。

Result: 在多种设备、模型与基准任务上，Xronos 相比 SOTA 将微调时间降低 18%（对比 TP 方案）到 56%（对比 PP 方案），设备空闲时间比例降低约 5.9×，同时保持精度不变。

Conclusion: 该工作揭示并量化了 CPU 边缘设备上 PP 因 CPU 争抢而失效、以及同构 TP 假设在异构环境下造成 straggler 空闲两个系统性问题，说明并行策略必须与设备算力异构性联合设计。工程上为 CPU/IoT 场景的协同微调提供了可落地方案，并可能推动边缘侧异构感知分区与通信-计算重叠调度的发展。

Abstract: Collaborative fine-tuning on edge devices adapts large language models to domain-specific data while keeping each device's data local. State-of-the-art (SOTA) collaborative fine-tuning techniques are largely designed for GPU-based edge devices and rely on pipeline parallelism (PP). However, many edge platforms, including IoT gateways, smart-home hubs, and in-vehicle computers, are primarily CPU-based. This paper reports that PP is ineffective on CPU-based edge devices because the same CPU handles both model computation and communication, which causes severe CPU contention. Our analysis shows that this leads to 5.75$\times$ higher computation stall ratios than on GPU devices on average. Tensor parallelism (TP) can alleviate this contention by separating computation and communication, but existing TP techniques assume homogeneous devices. On heterogeneous CPU edge devices, we find that this assumption causes faster workers to remain idle for up to 34% while waiting for slower devices at synchronization points. To address the limitations, we propose Xronos, a collaborative fine-tuning framework for heterogeneous CPU edge devices. Xronos uses TP as its execution backbone and combines lightweight profiling with heterogeneity-aware tensor partitioning to reduce the straggler bottleneck. Across diverse devices, models, and benchmark tasks, Xronos reduces fine-tuning time by 18% (TP) to 56% (PP) and the ratio of device idle time by $\sim$5.9$\times$ over SOTA techniques, while maintaining the accuracy.

</details>


### [6] [The Life of a Token: from Words to Bits on the Wire](https://arxiv.org/abs/2609.19924)
*Davide Avesani,Pengwenlong Gu,Sotiris Skaperas,Stefano Secci*

Main category: cs.DC

TL;DR: 这是一篇教学性（tutorial）文章，系统梳理LLM训练中"文本→token→embedding→张量→网络流量"的完整路径，结合但丁《神曲》的具体例子、解析流量模型与数值算例，量化模型架构、tokenization、embedding与并行策略对HPC互连中通信量、结构和时序的影响，给出LLM训练对网络需求的实用洞察。


<details>
  <summary>Details</summary>
Motivation: 现代LLM已达数十亿至数万亿参数，训练在数千个互连加速器上展开，通信网络成为关键但往往不透明的组件。工程实践中常见的问题是：算法/模型侧的决策（架构、并行策略、batch、序列长度、词表）如何映射为具体的网络带宽、延迟与流量模式，缺乏面向系统工程师的清晰教学材料。该文旨在打通"语言→通信流"的认知链路，为网络容量规划与训练系统设计提供可量化的依据。

Method: 方法论上属于教程式/解析建模，而非新系统实现：1）以《神曲》文本为贯穿案例，展示tokenization与embedding如何把文本转为向量；2）以解析流量模型刻画数据并行（AllReduce/梯度同步）、张量并行（AllGather/AllReduce）、流水并行（点对点激活传输）的通信量、消息尺寸与发生时机；3）结合具体数值算例（参数量、隐维度、层数、序列长度、微批、并行度、tokenizer词表）估算每步的字节数、链路带宽需求与重叠可行性；4）将模型架构与并行配置作为自变量，输出对网络拓扑、带宽、延迟的定性+定量要求。

Result: 文中给出了若干实用结论性洞察：通信量随并行策略呈现不同标度律（如数据并行的梯度同步量与参数量×并行度相关，张量并行的激活通信对延迟高度敏感，流水并行带来周期性bursty的P2P流量）；序列长度与batch直接放大激活相关通信，tokenization/embedding决定输入侧规模；不同并行维度对应不同网络feature需求（高带宽的all-reduce域 vs 低延迟高带宽的P2P/AllGather域），并说明计算-通信重叠与网络收敛比对有效吞吐的重要性。

Conclusion: 该工作本身不是新的系统或kernel贡献，而是把LLM训练的"文本到网络流量"路径系统化、可量化地讲清楚，并为网络需求提供解析刻画与数值示例。对工程侧的价值在于：a) 可用于训练集群的网络容量规划、拓扑与拥塞控制选型；b) 为并行策略/模型配置与通信代价的权衡提供快速估算工具与直觉；c) 为后续面向LLM训练的互连优化、集合通信调度、计算-通信重叠与故障/尾延迟分析提供教学与建模基准。

Abstract: Large Language Models (LLMs) transform vast collections of unstructured text into semantic patterns used for language generation and reasoning tasks. Behind their ease of use lies a complex process: words become tokens, tokens become vectors, and vectors ultimately give rise to streams of bits that flow through High-Performance Computing (HPC) systems. As modern LLMs grow to billions or trillions of parameters, this path increasingly unfolds across thousands of interconnected accelerators, making the underlying communication fabric a critical and often opaque component of model training. This tutorial aims to walk the reader through the journey from words to network traffic, shedding light on how language is translated into communication flows within HPC training systems. Using concrete examples from Dante's Divine Comedy, we illustrate how model architecture, tokenization, embeddings, and parallelization strategies shape the volume, structure, and timing of data exchanged across the network. We combine architectural analysis with analytical traffic models and numerical examples to characterize the communication requirements of LLM training. We try to demystify how words travel across the network and provide practical insights into the network requirements needed to support the journey from text to trained model.

</details>


### [7] [Accelerating Sharded Data Parallelism at Scale with Federated Learning](https://arxiv.org/abs/2609.20359)
*Gianluca Mittone,Marco Aldinucci*

Main category: cs.DC

TL;DR: 该论文将联邦学习的FedAvg聚合思想引入分片数据并行训练，提出FL+FSDP与FL+HSDP两种混合并行算法，把大规模DP部署解耦为松耦合的联邦组，在512块A100上预训练Llama3.1 8B，数据处理吞吐最高提升8.04倍、评估困惑度降低4.48。


<details>
  <summary>Details</summary>
Motivation: 基础模型需在数千块GPU上训练数月，分片数据并行（FSDP/HSDP）是主流加速手段，但在大规模部署、尤其是多层异构互联（如节点内NVLink、跨节点IB/以太网带宽差异大）下，all-gather/reduce-scatter的通信开销变得不可承受，成为扩展瓶颈。作者借鉴联邦学习中"本地计算为主、周期性低频率全局聚合"的高效通信范式，试图在保持全局批大小可控的前提下削减通信量。

Method: 提出两种混合并行算法：FL+FSDP与FL+HSDP，将分片数据并行与FedAvg式聚合交错执行。核心是把单个大规模DP集群切分为多个松耦合的"联邦组"（federation group）：组内仍执行标准FSDP/HSDP分片与梯度规约，组间仅做周期性、低频率的FedAvg式参数/梯度平均，从而把跨组（通常跨越慢速互联层）的通信量降至最低。全局批大小由组规模而非总卡数决定，因此可随组数扩展而保持有界。论文给出通信代价的形式化分析，并在Llama3.1 8B预训练上做实验验证。

Result: 在512块A100上预训练Llama3.1 8B：在相同超参下，FL+FSDP与FL+HSDP相比对应baseline（FSDP/HSDP）数据处理速度最高提升8.04倍，评估困惑度最多降低4.48（即模型质量更好）。性能收益来源于通信开销下降，以及全局批大小随联邦组规模有界增长（避免了超大批量带来的收敛/泛化退化）。

Conclusion: 把联邦式周期聚合与分片数据并行结合，可在异构多层互联上把大规模DP的通信瓶颈转化为可调的组间低频同步，既提升吞吐又改善收敛质量，具备良好的可扩展性与灵活性。工程上为超大规模预训练提供了一条"拓扑感知、分层同步"的替代扩展路径，对通信调度、checkpoint/容错语义和批大小—学习率联合调优均有直接影响；其代价是引入组间参数一致性的滞后，需在同步周期与收敛性之间权衡。

Abstract: The symbiotic scaling of artificial intelligence models and high-performance computing systems continually creates algorithmic challenges in their convergence. Foundation models (FMs) are a crucial example, requiring months-long training on thousands of cutting-edge GPUs. Sharded data parallelism (DP) is the dominant strategy to accelerate such computations by splitting data and models across multiple GPUs. However, it incurs prohibitive communication overhead when deployed at scale, particularly on multi-tier interconnects with heterogeneous performance. Inspired by the efficient communication principles of federated learning (FL), this work introduces two hybrid algorithms - FL+FSDP and FL+HSDP - interleaving sharded DP with FedAvg-style aggregations. Such approaches decouple large DP deployments into smaller, loosely-coupled federation groups, requiring minimal inter-group traffic while keeping the global batch size bounded by the groups' size. Formal analysis of communication costs and experimental validation prove their scalability and flexibility. A Llama3.1 8B pre-training on 512 A100 GPUs shows that, under identical hyperparameters, FL+FSDP and FL+HSDP achieve up to 8.04 faster data processing and 4.48 lower evaluation perplexity than their counterparts, demonstrating superior computational efficiency and improved model quality. These properties stem from reduced communication overhead and the bounded growth of the global batch size relative to the federation group size.

</details>


### [8] [VERA: Reinforcement Learning for Dynamic Memory Scaling of HPC Workloads in Kubernetes](https://arxiv.org/abs/2609.19936)
*Ade Pramono,Jie Ren,Ivy Peng*

Main category: cs.DC

TL;DR: VERA 用强化学习把纵向内存扩缩容建模为 MDP，在 3353 条真实 Prometheus 轨迹上训练，在 GKE 上对 LAMMPS、图分析、内存分析和 MLPerf 3D-UNet 实测回收了 31.6% 的内存余量且最多 1 次 OOM；同期默认 VPA 为 -7.9%，并在 30 次运行中无法避免 OOM。


<details>
  <summary>Details</summary>
Motivation: HPC 作业跑在 Kubernetes 上时普遍存在内存过度预留，导致资源利用率低。默认 VPA 基于历史统计（回顾式启发式）做推荐，无法预测首次运行作业中由计算阶段（phase）驱动的内存尖峰，因而要么预留过多、要么推荐不足触发 OOM，缺少能在线观测并前瞻性调整纵向内存的机制。

Method: 将纵向内存扩缩容形式化为马尔可夫决策过程（状态来自 Prometheus 采集的内存/阶段观测，动作是内存配额调整，奖励兼顾利用率提升与 OOM 惩罚）；用 3353 条真实 Prometheus 轨迹训练 RL 推荐器（agent）。推理/部署侧以 recommender 形式给出内存配额建议，不侵入作业本身，在真实 GKE 集群上针对 LAMMPS、图分析、内存分析、MLPerf 3D-UNet 评估，与默认 VPA 对照。

Result: 在 live GKE 集群上，VERA 回收 31.6% 的可用内存余量（headroom），整个评测中最多只发生 1 次 OOM；相同工作负载下默认 VPA 的回收率为 -7.9%（即反而抬高了内存预留），且有 30 次运行的推荐不足以避免 OOM。

Conclusion: 结果表明，观测驱动的 RL 推荐器在动态内存扩缩容上可以优于回顾式启发式方法，为 Kubernetes 上 HPC 作业的纵向内存自动调优提供了可行路径；其作为 out-of-band recommender 的设计利于落地，但结论目前局限于单集群、四类工作负载、3553 级别轨迹规模，SLO（近零 OOM）与跨集群/跨负载泛化仍需进一步验证。

Abstract: Memory over-provisioning results in resource underutilization when HPC workloads run on Kubernetes. The default Vertical Pod Autoscaler (VPA) cannot anticipate phase-driven memory spikes for first-run HPC jobs. In this work, we present a reinforcement learning (RL) recommender VERA that formulates vertical memory scaling as a Markov Decision Process and trains an agent on 3353 real Prometheus traces. Evaluated on a live Google Kubernetes Engine cluster using LAMMPS, graph analytics, in-memory analytics, and MLPerf 3D-UNet, the RL agent reclaims 31.6% of the available memory headroom and incurs at most one OOM event while VPA reclaims -7.9% over the same runs, raising memory provisioning, and its recommendation would have been insufficient to avoid OOM in 30 runs. The results demonstrate that an observation-driven RL recommender could outperform retrospective heuristics for dynamic memory scaling.

</details>


### [9] [P-GADMM: Parallel Group-Based ADMM for Asynchronous Optimization in Heterogeneous Edge Networks](https://arxiv.org/abs/2609.20006)
*Gaiguo Wei,Qingying Zhang,Heqiang Wang,Yu Zhang,Xiaoxiong Zhong*

Main category: cs.DC

TL;DR: P-GADMM 通过“算力感知分组 + 云侧有界异步协调”缓解异构边缘网络中 ADMM 的同步等待问题，在保持精度相当的同时降低墙钟训练时间，并给出有界组级陈旧度下的收敛性分析。


<details>
  <summary>Details</summary>
Motivation: 同步 ADMM 在异构边缘网络中效率低：快客户端/组必须等待慢组完成才能进行全局更新。已有的分组式 ADMM 虽降低通信开销，但分组规则基于数据相似性或网络拓扑，未显式考虑计算异构性，导致组内训练速度差异大、同步等待依旧严重。

Method: 提出 P-GADMM：1) 计算感知分组——依据客户端计算能力与本地数据量构建边缘组，减小组内训练速度方差；2) 层次化执行——边缘级聚合 + 云侧有界异步协调，活跃组无需等待慢组即可参与全局更新，并用延迟阈值限制组信息的陈旧度。理论上针对强凸组目标、在组级陈旧度有界的理想化形式下给出收敛保证。

Result: 收敛性上，证明时间平均意义下收敛到一个由陈旧度导致的渐近误差邻域（误差界随陈旧度上界增长）。实验上，相比代表性基线在保持相当的最终精度前提下降低墙钟训练时间。

Conclusion: 将分组维度与异步协调显式对齐到计算异构性，是提升异构边缘分布式优化时间效率的可行路径；其代价是可证明的收敛精度被有界陈旧度“折价”。对工程而言，该工作提供了可落地的参数化设计（延迟阈值↔精度/时间折衷），但组级陈旧度阈值与分组策略的调优是部署关键。

Abstract: The Alternating Direction Method of Multipliers (ADMM) is widely used for distributed optimization, but its synchronous implementation can suffer from efficiency loss in heterogeneous edge networks, where fast clients or groups need to wait for slower ones before global updates can be completed. Existing group-based ADMM methods reduce communication overhead through grouping, but their grouping rules usually focus on data similarity or network topology and do not explicitly account for computation heterogeneity. To address this issue, this paper proposes Parallel Group-Based ADMM (P-GADMM) for distributed optimization in heterogeneous edge networks. P-GADMM forms computation-aware edge groups according to client computational capabilities and local data sizes, which reduces training-speed variation within each group. It further combines edge-level aggregation with bounded asynchronous coordination at the cloud, allowing active groups to participate in global updates without waiting for slower groups while controlling stale group information through a delay threshold. For strongly convex group objectives, we establish convergence guarantees for an idealized form of P-GADMM under bounded group-level staleness, showing a time-averaged convergence behavior up to a staleness-induced asymptotic error neighborhood. Experiments show that P-GADMM reduces wall-clock training time compared with representative baselines while maintaining comparable final accuracy.

</details>


### [10] [Distributed Edge Inference: an Experimental Study on Multiview Detection](https://arxiv.org/abs/2609.20009)
*Gianluca Mittone,Giulio Malenza,Marco Aldinucci,Robert Birke*

Main category: cs.DC

TL;DR: 该论文以真实多视角检测模型 MvDet 为负载，在 C/C++ 高性能边缘推理框架 FastFL 上实测云-边-雾融合的"计算连续体"（compute continuum），在不同边缘算力与网络带宽下验证去中心化推理相对集中式方案的收益，推理时间最高获得 1.92× 加速。


<details>
  <summary>Details</summary>
Motivation: 云计算的集中式 client-server 架构在应用规模增大时成为可扩展性瓶颈。为追求超大规模（ultra-scalability），云/边/雾计算趋向融合为"计算连续体"，将基础设施完全去中心化、覆盖泛在的异构资源。但如何在这种复杂异构环境中设计与部署应用仍是一个开放难题，缺乏真实负载下的量化证据。

Method: 以真实的多视角检测模型 MvDet 作为代表性应用负载，在 FastFL（C/C++ 高性能边缘推理框架）上实现部署，覆盖多种边缘算力配置与不同网络带宽的实验场景，对比计算连续体去中心化推理与同等设备下的集中式方案的推理性能。

Result: 在相同设备条件下，去中心化（连续体）方案相比集中式方案推理时间最高提升 1.92×；性能收益受边缘算力与网络带宽共同影响（原文未给出细粒度分解）。

Conclusion: 初步实证表明计算连续体对多视角检测这类计算密集且可切分的推理负载具备实际加速潜力，但该结论建立在单一模型、单一框架与有限场景之上，尚未给出调度策略、资源编排或通用性论证。

Abstract: Computing is evolving rapidly to cater to the increasing demand for sophisticated services, and Cloud computing lays a solid foundation for flexible on-demand provisioning. However, as the size of applications grows, the centralised client-server approach used by Cloud computing increasingly limits the applications' scalability. To achieve ultra-scalability, cloud/edge/fog computing converges into the compute continuum, completely decentralising the infrastructure to encompass universal, pervasive resources. The compute continuum makes devising applications benefitting from this complex environment a challenging research problem. We put the opportunities the compute continuum offers to the test through a real-world multi-view detection model (MvDet) implemented with the FastFL C/C++ high-performance edge inference framework. Computational performance is discussed considering many experimental scenarios, encompassing different edge computational capabilities and network bandwidths. We obtain up to 1.92x speedup in inference time over a centralised solution using the same devices.

</details>


### [11] [The Price of Decentralization, Paid Twice: A Latency Floor and a Minimal Subsidy from a Geographic Decentralization Invariant](https://arxiv.org/abs/2609.20322)
*Ruiyang Zhang*

Main category: cs.DC

TL;DR: 该论文形式化地刻画了"延迟感知的验证者选择"与"拜占庭容错所需的广域独立性"之间的根本张力：任何协议下，某提议者的β-法定人数提交延迟存在几何下界 ρ；且在平面几何中最小值为 ρ√(β/γ)。进一步，作者在位置选择博弈中证明中心化是超临界 O(2) 对称破缺分岔，维持去中心化目标需付出 Θ(c·ρ/√γ) 规模的反集中补贴——去中心化在延迟与激励两端"被支付两次"，且比值恒定。


<details>
  <summary>Details</summary>
Motivation: 延迟感知的验证者选择机制（按投票权重、领导权或快速法定人数成员资格奖励低延迟验证者）会导致验证者共址部署，从而侵蚀 BFT 假设的地理故障独立性。此前该张力多为个案或协议特定观察，缺乏协议无关的定量刻画，也难以量化"去中心化"的代价与所需补贴。

Method: 1) 定义 (ρ,γ)-去中心化：任意半径 ρ 区域内所持权益不超过 γ 比例。2) 证明协议无关的几何下界：对法定人数阈值 β>γ，任意提议者的 β-法定人数提交延迟至少 ρ；平面情形最小值为 ρ√(β/γ)。3) 构建带真实法定人数延迟奖励的位置选择博弈，用动力系统分析证明中心化对应超临界 O(2) 对称破缺分岔。4) 推导维持去中心化目标所需的反集中补贴量级 Θ(c·ρ/√γ)。5) 数值验证 + 实测地理数据：基于 RIPE-Atlas 延迟矩阵对 719 个 Solana 验证者做普查。

Result: 几何下界与平面精确最小 ρ√(β/γ) 成立；中心化在博弈中表现为超临界 O(2) 对称破缺分岔；反集中补贴量级为 Θ(c·ρ/√γ)，即延迟代价与激励代价成恒定比例。实测：719 验证者 Solana 普查在 RIPE-Atlas 延迟矩阵上得到验证者 Nakamoto 系数为 19，但地理 Nakamoto 系数仅为 1–2。

Conclusion: 论文坦承几何下界本身是初等的，核心贡献在于延迟与补贴之间的耦合关系：去中心化在延迟和激励两侧被"双重付费"且比例恒定。该结论对验证者选择机制与激励设计具有协议无关的指导意义，提示单纯的低延迟奖励会系统性导致地理集中，需要显式反集中补贴来维持故障独立性。

Abstract: Latency-aware validator selection---rewarding validators for low network latency through voting weight, leadership, or fast-quorum membership---is known to push validators to co-locate, eroding the geographic fault-independence that Byzantine fault tolerance assumes. We give this tension a sharp, protocol-agnostic form. Calling a stake configuration (rho,gamma)-decentralized if no radius-rho region holds more than a gamma fraction of stake, we prove that for quorum threshold beta>gamma every proposer's beta-quorum commit latency is at least rho---independent of the protocol---and in the plane the minimum is exactly rho*sqrt(beta/gamma). Our main result couples this geometric floor to an incentive one: in a location-choice game with the true quorum-latency reward, centralization arises as a supercritical O(2)-symmetry-breaking bifurcation, and sustaining a decentralization target costs an anti-concentration subsidy of magnitude Theta(c*rho/sqrt(gamma))---decentralization is paid twice, in latency and in incentives, in a constant ratio. We verify every claim numerically and on measured geography: a 719-validator Solana census on a RIPE-Atlas latency matrix has validator Nakamoto coefficient 19 but geographic Nakamoto 1--2. We are explicit about scope: the floor is elementary; the contribution is the latency--subsidy coupling.

</details>


### [12] [A Kubernetes-Native Request Router for Quality-Aware Inference Serving in the Computing Continuum](https://arxiv.org/abs/2609.20497)
*Ignjat Karanovic,Pantelis A. Frangoudis,Ivan Čilić,Ivana Podnar Žarko,Schahram Dustdar*

Main category: cs.DC

TL;DR: ASRB 是一个面向 Kubernetes 计算连续体的、基于评分的动态请求路由均衡器，通过跨层运行时指标加权打分来调度 ML 服务请求，可在 QoS 策略下灵活权衡延迟与精度，并以自适应监控强度将监控开销降低最多约 70%。


<details>
  <summary>Details</summary>
Motivation: 在云—边—端计算连续体上部署 ML 推理服务时，实例异构（算力、网络、模型精度不一）且运行环境动态变化，传统 K8s 负载均衡（如轮询、最少连接、延迟优先）只考虑单一基础设施或响应时间信号，无法表达"延迟—精度"这类应用级 QoS 权衡，也无法在监控开销与路由质量之间取得平衡。作者希望提供一种无需改动 K8s、可由服务提供方策略驱动的路由机制。

Method: 1) 评分式路由：对每个候选服务实例计算综合得分，输入包括基础设施层信息（如节点负载、资源状态）、响应时间测量、以及应用级质量指标（如 ML 模型精度）。2) 策略可配置：服务提供方以 QoS 准则的加权组合形式编码策略，权重即可调延迟与精度的权衡偏好。3) 跨层运行时指标监控用于驱动决策并快速适应环境变化。4) 自适应监控强度控制：针对大规模部署下的监控开销，选择性地、动态地调整监控频率/范围，在不显著损失路由质量的前提下降低开销。5) 实现层面不改动 Kubernetes，通过原生扩展点集成，便于在现有集群部署运维。

Result: 在真实测试床实验中：以降低延迟为目标调参时，平均响应时间比面向延迟的 SOTA 路由机制至少低 10 ms；当偏向精度时，可获得更高的精度，实现可操作、可控制的灵活权衡。同时故障率更低、对环境变化的响应更快，监控成本相对相关 SOTA 方案最多降低约 70%，代价是在部分配置下仅有适度的延迟惩罚。

Conclusion: ASRB 将跨层指标、应用级 QoS 策略与自适应监控开销控制统一到无侵入的 K8s 评分路由中，为计算连续体上的 ML 服务提供了可调、可运维的延迟—精度权衡能力；其工程价值主要在于降低监控面开销、提升路由对动态环境的适应性，并保持与既有集群的低摩擦集成。

Abstract: We introduce Adaptive Score-based Routing Balancer (ASRB), a dynamic, score-based request routing mechanism for Kubernetes-based service deployments over the computing continuum. ASRB jointly considers infrastructure-level information, response time measurements, and application-level quality indicators, with a particular focus on serving Machine Learning (ML) workloads. For these workloads, ASRB balances requests over service instances deployed in the continuum, following service provider-defined policies encoded as weighted combinations of QoS criteria to flexibly address latency-accuracy trade-offs. To drive routing decisions and swiftly adapt to changes in the operating environment, ASRB monitors a range of runtime metrics across multiple system layers. To deal with the associated monitoring overhead, particularly important for large-scale deployments, it selectively and adaptively controls monitoring intensity without sacrificing on routing quality. ASRB is implemented without requiring any modifications to Kubernetes, making it straightforward to deploy and operate in existing cluster environments. Our testbed experiments demonstrate the versatility of ASRB: When tuned for latency reduction, it achieves at least 10 ms lower mean response time compared with latency-oriented state-of-the-art routing mechanisms, while it achieves higher accuracy when this is prioritized through specific configurations, thus enabling flexible and operator-controllable trade-offs. At the same time, it attains reduced failure rates, higher responsiveness to changes in the operating environment, and up to ~70% less monitoring cost than relevant state-of-the-art solutions, at the potential expense of only a modest latency penalty in some configurations.

</details>


### [13] [PixelFlow: Token-Level Workload Management for Efficient Distributed DiT Serving](https://arxiv.org/abs/2609.20723)
*Zhexiang Zhang,Minchen Yu,Yifan Sun,Xu Bai,Xingliang Yuan,Adel N. Toosi*

Main category: cs.DC

TL;DR: PixelFlow 是一个面向 DiT 在线图像生成的分布式推理服务系统,核心思想是用「token 级」而非「请求级」的工作量划分与批处理,把请求切成可变大小的分区分散到多张 GPU 上,从而在满足延迟 SLO 的前提下提升 GPU 利用率。配套设计了 token 布局优化、计算-通信重叠和 SLO 感知分组调度。在 H100 上实测 SLO 达成率最高提升 43%,goodput 达到 SOTA 系统的 2.8 倍。


<details>
  <summary>Details</summary>
Motivation: DiT 在线服务面临延迟 SLO 与 GPU 利用率的两难:请求级 batching 对 batch size 的控制粒度太粗——batch 太小打不满 GPU 算力,batch 太大又超 SLO;而全局协调调度还要求各 GPU 同步后才能接收新请求,进一步引入排队延迟。根本问题是 request 作为最小调度/批处理单位的粒度与 DiT 的实际计算单位(token/图像 patch)不匹配。

Method: 1) 以 image token 为单位把请求拆成可变大小的 partition,跨 GPU 批处理,使单卡可在延迟约束下承担「额外一部分」工作;2) 运行时支持请求切分与异构 partition 的高效 batching;3) token placement 优化,在均衡各 GPU 负载的同时最小化跨卡数据交换;4) 利用去噪步骤间的相似性,把剩余通信与计算重叠;5) SLO-aware 调度器按延迟需求兼容性把 GPU 分组,组内独立推进,仅在新请求准入需要合并资源时才跨组同步。

Result: 在 H100 上对 Stable Diffusion 3 与 FLUX.1-dev 的评测显示:SLO 达成率最高提升 43%,goodput 最高达 SOTA DiT 服务系统的 2.8 倍。

Conclusion: 论文的主要贡献是提出 token 级工作量管理这一 DiT 服务抽象,把调度粒度从 request 下沉到 token,从而解耦「batch 大小」与「请求边界」;并通过划分-布局-通信重叠-分组调度的完整运行时将其实用化。对工程实践的意义在于:对 diffusion 类多步、计算密集、单请求即可占满 GPU 的生成负载,细粒度切分 + 分层同步比全局同步调度更能同时压低排队延迟与提高利用率。

Abstract: Online image generation with Diffusion Transformers (DiTs) must meet latency service-level objectives (SLOs) while using GPU resources efficiently. Existing systems improve GPU utilization by batching multiple requests for joint execution. However, request-level batching offers limited control over batch size: batches may be too small to saturate GPU compute, while larger ones may violate latency SLOs. Globally coordinated scheduling introduces further delays by requiring independently progressing GPUs to synchronize before admitting new work.
  We present PixelFlow, a distributed DiT serving system that addresses these limitations through token-level workload management. Its key idea is to use image tokens (the units a DiT processes to generate an image) to divide and batch request workloads at a finer granularity. This allows each GPU to take on a portion of additional work under latency constraints. By distributing these portions across GPUs, PixelFlow accommodates more concurrent requests, improving GPU utilization while reducing queueing delays. To realize this flexibility, PixelFlow provides a runtime that splits requests into variable-sized partitions and batches them efficiently on each GPU. To reduce the resulting communication overhead, it optimizes token placement to limit cross-GPU data exchange while balancing GPU workloads. It further exploits similarity across denoising steps to overlap the remaining transfers with computation. An SLO-aware scheduler groups GPUs to share compute resources among requests with compatible latency requirements. Each group progresses independently, synchronizing with others only when their combined resources are needed to admit a new request. Evaluation with Stable Diffusion 3 and FLUX.1-dev on H100 GPUs shows that PixelFlow improves SLO attainment by up to 43% and achieves up to 2.8 times the goodput of state-of-the-art DiT serving systems.

</details>


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [14] [SiliconBench: Speed, Memory, and Fidelity for LLM Serving on Unified-Memory Desktops](https://arxiv.org/abs/2609.19169)
*Ranran Haoran Zhang,Aysa Xuemo Fan,David Munhá Correia,Alex Cheema,Rui Zhang*

Main category: cs.AR

TL;DR: SiliconBench 是面向 Apple Silicon 统一内存桌面并发本地 LLM 服务引擎的系统基准，从速度、内存、保真度三个维度评估九个引擎，并引入 DGX Spark 作为跨平台参照。


<details>
  <summary>Details</summary>
Motivation: 现有本地 LLM 服务榜单多以吞吐/延迟等速度指标排序，忽略了统一内存桌面上两个关键约束：内存余量（headroom）与输出保真度（fidelity）。在并发本地服务场景下，仅看速度会产生误导性结论。

Method: 构建 SiliconBench，对九个 Apple Silicon 服务引擎在 chat 与 agent 两类负载、Qwen3/Qwen3.5/Gemma 4 三代模型上进行并发扫描（并发度 1→16）；用分类任务对照 NVIDIA 参考实现检测质量退化；用 DGX Spark 做互补性能参照；在更大 dense 与 MoE 模型上比较 prefill/decode 调度；并测试双机 Thunderbolt RDMA 张量并行与 TCP 流水线并行。以三项准则解读：服务架构就绪度、内存纪律、多节点扩展性。

Result: 在 Qwen3-0.6B 上，vllm-metal 在两类负载下从并发 1 到 16 吞吐均翻倍以上；CUDA vLLM 与 SGLang 在同一批 prompt 上并发扩展性更强。显式内存预算并不保证内存余量：两个栈虽完成全部请求，但内存占用逼近物理容量且吞吐下降。新模型架构的引擎支持面更窄，但已评估的实现均与保真度参考一致。仅有三个栈同时满足完成率、保真度与模型覆盖三道门槛。在大模型上，vllm-metal 的 packed prefill-decode 路径在并发负载下首 token 延迟优于 omlx，说明 prefill 与 decode 协同调度的重要性。双机配置中 Thunderbolt RDMA 上的张量并行可扩展，而 TCP 上的流水线并行出现性能回退。

Conclusion: 并发本地服务的评估必须从单纯速度转向速度—内存—保真度的联合视角，内存纪律与 prefill/decode 调度是决定实际可用性的关键因素；统一内存桌面上的多节点扩展目前仅在低延迟互连（Thunderbolt RDMA）配合张量并行时成立。作者开源基准代码、逐次运行结果与维护日志，并采用有界 agent 修复加人工复核的工作流。

Abstract: Concurrent local LLM serving on unified-memory desktops must preserve memory headroom and output fidelity, which speed-only rankings overlook. We introduce SiliconBench, which evaluates nine Apple Silicon serving engines through three lenses: speed, memory, and fidelity. We evaluate chat and agent serving on Qwen3, Qwen3.5, and Gemma 4. We use a classification task to check for quality regressions against an NVIDIA reference. DGX Spark provides a complementary serving-performance reference. Three desiderata guide interpretation: serving architecture readiness, memory discipline, and multi-node scaling. On Qwen3-0.6B, vllm-metal alone more than doubles throughput on both workloads from concurrency 1 to 16. CUDA vLLM and SGLang show stronger concurrency scaling on the same prompts. Explicit memory budgets do not guarantee memory headroom: two stacks complete every request while memory use approaches physical capacity and throughput declines. The newer model architectures have narrower engine support. Their evaluated implementations match the fidelity reference. Only three stacks satisfy the completion, fidelity, and model-coverage gates. Comparisons on larger dense and MoE models reinforce the importance of scheduling prompt processing alongside ongoing generation: vllm-metal's packed prefill-decode path maintains lower first-token latency than omlx under concurrent load. In the tested two-machine configurations, tensor parallelism over Thunderbolt RDMA scales while pipeline parallelism over TCP regresses. We release benchmark code, per-run results, and maintenance journals, supported by a workflow combining bounded agent fixes with human review.

</details>


### [15] [CovR: Coverage-Aware Hardware Verification via Reasoning-Guided Reinforcement Learning](https://arxiv.org/abs/2609.19189)
*Manar Abdelatty,Maryam Nouh,Sherief Reda*

Main category: cs.AR

TL;DR: CovR 是一个面向覆盖率驱动的 LLM 测试平台（testbench）自动生成框架：用"自反思 + 仿真/覆盖率反馈"的智能体管线，借助强教师模型构建 16,514 条 spec→RTL 推理→testbench 的覆盖感知数据集，并设计基于工具派生奖励的强化学习框架微调学生模型，在覆盖率指标上显著超越 SOTA，并可回插到智能体精修管线与完整验证流程中继续增益。


<details>
  <summary>Details</summary>
Motivation: 硬件设计验证占整体开发工作量高达 70%，是资源消耗最重的环节。已有 LLM 自动化 testbench 生成工作大多只关注功能正确性，忽略了覆盖率质量这一验证的核心目标，导致生成的激励难以系统性触达设计状态空间，验证完备性无法保证。

Method: 1) 智能体框架：自反思循环（self-reflection）与仿真反馈结合，形成迭代精修的 stimulus/testbench 生成管线；2) 数据构建：用该管线配合强教师模型合成 16,514 条 "自然语言规格—RTL 推理—testbench" 三元组，实现覆盖率感知监督；3) RL 训练：面向覆盖率驱动生成定制强化学习，奖励来自仿真与覆盖率工具反馈（tool-derived rewards），用于优化学生模型；4) 回插部署：微调后的模型重新作为智能体的生成器参与精修，并可作为插件式激励引擎接入完整验证工作流。

Result: 微调模型在 VerilogEval 与 RTLLM V2.0 上 cov@10 达 93.81%，CVDP 上达 87.76%，分别超 SOTA 7.97% 与 3.59%；将模型回插智能体精修管线后进一步提升至 94.27%（VerilogEval/RTLLM V2.0）与 91.39%（CVDP）。作为插件式激励引擎接入完整验证流程时，覆盖率提升 18.95%、变异检测分（mutation detection score）提升 1.19%，并额外暴露 4.46% 此前未被检出的失效。

Conclusion: 覆盖率应作为 LLM 硬件验证优化的首要目标而非附属指标：以工具反馈为奖励的智能体+RL 闭环能同时提升覆盖率、数据/监督质量与下游验证有效性，并揭示传统功能正确性导向方法遗漏的设计缺陷，对 LLM 驱动的 EDA/验证自动化具有直接的工程价值。

Abstract: Design verification remains one of the most resource-intensive stages of hardware development, often consuming up to 70% of the total design effort. While recent work has explored using Large Language Models (LLMs) to automate testbench generation, most existing approaches focus narrowly on functional correctness, overlooking the critical aspect of coverage quality. To bridge this gap, we present CovR, an agentic framework for automated testbench generation that combines self-reflection loops with simulation-based feedback to maximize coverage. Using this pipeline, we construct a large-scale dataset of 16,514 natural specification RTL reasoning testbench tuples with a strong teacher model, enabling coverage-aware supervision. Building on this, we propose a reinforcement learning (RL) framework tailored for coverage-driven testbench generation, leveraging tool-derived rewards from simulation and coverage feedback to optimize a student model. Experimental results show that the CovR finetuned model achieves 93.81% cov@10 on VerilogEval and RTLLM V2.0, and 87.76% cov@10 on CVDP, outperforming state-of-the-art approaches by 7.97% and 3.59%, respectively. Furthermore, deploying the finetuned model back into the agentic refinement pipeline further improves cov@10 to 94.27% on VerilogEval and RTLLM V2.0 and 91.39% on CVDP. Moreover, when integrated as a plug-in stimulus engine for full verification workflows, CovR improves coverage by 18.95% and mutation detection score by 1.19%, while revealing 4.46% undetected failures, highlighting the importance of optimizing for coverage in LLM-based hardware verification.

</details>


### [16] [Epic: Efficient Programming Paradigm for In-Storage Computing](https://arxiv.org/abs/2609.19206)
*Yuyue Wang,Zhenyu Zhang,Glenn Reinman,Huaicheng Li*

Main category: cs.AR

TL;DR: Epic 提出一种基于 NVMe 的存内计算（ISC）栈，用「位置类型 + keep 原语 + dataflow 生命周期推导」统一抽象数据驻留与状态，配合存储感知编译器与运行时，在 12 个负载上比最强前作平均快 1.6×，对主机基线平均 4.2×、最高 16.1×。


<details>
  <summary>Details</summary>
Motivation: 在计算存储设备（CSD）上做 ISC 可以减少主机—存储的数据搬运，但多阶段应用要真正兑现收益，必须同时协调数据放置、I/O–计算重叠以及跨工作流的设备驻留状态。现有接口缺少统一抽象来表达这些决策，导致 ISC 编程复杂、优化机会无法被编译器自动发现。

Method: Epic 通过把数据驻留与生命周期写进程序来提供该抽象：location types 声明逻辑驻留位置，dataflow 在一次调用内推导中间值与算子状态的生命周期，keep 原语把选定状态跨调用保留，从而把完整卸载工作流表达为「有位置、有状态」的数据流图。配套的存储感知编译器对该数据流做变换、进行搬运代价感知的逻辑映射与算子融合，并显式暴露 I/O–计算重叠；运行时用执行期信息补全计划，异步地把工作绑定到物理资源并管理设备驻留状态（NVMe 接口）。

Result: 在 12 个文件扫描、数据库与机器学习负载上：相比 5 个 ISC 系统中最强的一个平均快 1.6×；相比对应主机基线平均加速 4.2×，最高 16.1×；应用侧代码量最多减少 14×。

Conclusion: Epic 的价值在于把 ISC 从「逐个 kernel 手动卸载」提升为「以数据流为中心的编译+运行时协同」：用语言级语义统一表达数据驻留、计算下沉与跨调用状态，使搬运感知的映射/融合和 I/O–计算重叠可被编译器自动规划。对工程的启示是：CSD 编程模型应把位置与生命周期作为一等公民，并由运行时异步绑定物理资源，才能在多阶段工作流中稳定吃到 ISC 的带宽与能效收益。

Abstract: In-storage computing (ISC) reduces host--storage data movement by executing computation inside computational storage devices (CSDs). For multi-stage applications, realizing these benefits requires coordinating data placement, I/O--compute overlap, and device-resident state across the workflow, yet existing interfaces lack a unified abstraction for these decisions. We present Epic, an NVMe-based ISC stack that provides this abstraction by capturing data residency and lifetime in the program: location types declare logical residency, dataflow derives lifetimes for intermediate values and operation state within an invocation, and a keep primitive extends selected state across invocations. These semantics expose the complete offloaded workflow as a located, stateful dataflow. A storage-aware compiler transforms this workflow, performs movement-aware logical mapping and fusion, and exposes I/O--compute overlap; a runtime completes the plan using execution-time information, asynchronously binding work to physical resources and managing device-resident state. Across 12 file-scanning, database, and machine learning workloads, Epic is 1.6$\times$ faster on average than the strongest of five prior ISC systems, while achieving 4.2$\times$ speedup on average and up to 16.1$\times$ over the corresponding host baselines, and reducing application-side code by up to 14$\times$ in our implementations.

</details>


### [17] [MeshKV: A Network-on-Chip KV Cache Fabric for Scalable Transformer Decoding Accelerators](https://arxiv.org/abs/2609.19207)
*Dong Liu,Yanxuan Yu*

Main category: cs.AR

TL;DR: MeshKV 提出一种 KV cache fabric，将 KV block 以 packetized flow 形式通过轻量级 NoC 传输，通过 TaKV affine striping、Mare multicast 与 Pad 流水调度三项协同设计，缓解长上下文自回归解码在 tiled 加速器上的 centralized memory 瓶颈。在 8x8 FPGA（LLaMA-2-7B / Mistral-7B，8K–32K）上最多降低 58% 互连流量、KV 带宽利用率提升 2.1x、多流吞吐提升至 1.9x。


<details>
  <summary>Details</summary>
Motivation: 自回归 transformer 解码受制于 tiled 加速器上不规则的 KV cache 搬运。已有压缩与 DRAM 放置方案仍把流量集中在 centralized memory path 上，长上下文 serving 时形成带宽瓶颈；同时 bisection back-pressure 导致互连资源被浪费而非转化为有效 KV 传输。

Method: 三项协同设计：(i) TaKV affine striping——用仿射映射打散 KV block 的 home 位置，降低 hotspot 负载；(ii) Mare multicast——带可验证 duplicate suppression 的多播，减少重复 KV 传输；(iii) Pad——在 credit-aligned FIFO 背后重叠 prefetch、tile multiply 与 streaming softmax，把 bisection back-pressure 转化为有用的 KV 搬运。整体把 KV block 当作 packetized flow 在轻量 NoC 上搬运。

Result: 8x8 FPGA 实现，LLaMA-2-7B 与 Mistral-7B，序列长度 8K–32K：互连流量最多降低 58%，KV 带宽利用率提升 2.1x，多流吞吐最多提升 1.9x。

Conclusion: 该系统表明将 KV cache 从集中式内存路径迁移到 NoC fabric，并以地址条带化、带校验多播和 credit 对齐流水共同设计，可显著提升长上下文 decode 的互连效率与服务吞吐，为 KV-centric 推理架构提供了可落地的硬件/系统路线。

Abstract: Autoregressive transformer decoding is constrained by irregular key-value (KV) cache movement on tiled accelerators. Prior compression and DRAM-placement systems still concentrate traffic on centralized memory paths that bottleneck long-context serving. We present MeshKV, a KV cache fabric that moves blocks as packetized flows over a lightweight NoC. It co-designs (i) TaKV affine striping to spread homes and cut hotspot load, (ii) Mare multicast with verified duplicate suppression, and (iii) Pad, which overlaps prefetch, tile multiply, and streaming softmax behind credit-aligned FIFOs. Together they convert bisection back-pressure into useful KV transfer. On our 8x8 FPGA implementation with LLaMA-2-7B and Mistral-7B at 8K-32K, MeshKV reduces interconnect traffic by up to 58%, improves KV bandwidth utilization by 2.1x, and delivers up to 1.9x multi-stream throughput.

</details>


### [18] [MiX: Micro-Inverted-Scaling for End-to-End Low-Bit Vision-Language Model Acceleration](https://arxiv.org/abs/2609.19683)
*Yuan Liao,Jae-sun Seo*

Main category: cs.AR

TL;DR: MiX提出"微缩放反转"数据格式：将多个共享指数改为多个私有指数共享一个尾数，配合自适应双格式MiX-MX推理框架与定制加速器，用移位器替代乘法器。在多种VLM上，4.5-bit MiX精度持平或优于NVFP4，加速器面积效率提升25%，相比SOTA Focus加速2.3-4.5x、能耗降低1.4-2.9x。


<details>
  <summary>Details</summary>
Motivation: VLM部署到边缘设备受内存带宽严重制约，需激进的sub-8-bit量化，且边缘加速器受面积/功耗限制要求端到端量化模型。但多模态token间极端动态范围差异导致标准block格式出现"微缩放崩塌"（microscaling collapse）：单个巨大outlier劫持共享指数，使周围元素下溢，破坏attention map。NVFP4等MX类格式在VLM非对称outlier拓扑下失效。

Method: 1) 提出Micro-Inverted-Scaling（MiX）格式：数学上反转微缩放范式，不再"多尾数共享一指数"，而是"多私有逐元素指数共享单一共享尾数"；2) 针对VLM非对称outlier拓扑提出自适应双格式MiX-MX推理框架，按分布动态切换格式；3) 通过代数方式提取（factor out）共享MiX尾数，将计算映射到定制加速器，用移位器（shifter）替代乘法器，降低面积与能耗。

Result: 在多个VLM上端到端评估：4.5-bit MiX在多模态基准上精度与NVFP4相当或更优；MiX加速器面积效率相比NVFP4基线提升25%；相比SOTA加速器Focus实现2.3-4.5x加速、1.4-2.9x能耗降低。

Conclusion: 实验证明反转缩放的数据通路在物理实现上对高效VLM部署更优，为边缘端sub-4-bit VLM量化与加速器协同设计提供了新范式：以"共享尾数+私有指数"替代传统block scaling，兼顾极端动态范围表达力与低比特硬件效率。

Abstract: The deployment of Vision-Language Models (VLMs) on edge devices is severely bottlenecked by memory bandwidth, necessitating aggressive sub-8-bit quantization. Since edge accelerators are strictly constrained by area and power, they require end-to-end quantized models. However, the extreme dynamic range gap between multi-modal tokens causes standard block formats to suffer "microscaling collapse," where a single massive outlier hijacks the shared exponent, underflowing surrounding elements and destroying attention maps. To break this bottleneck, we propose Micro-Inverted-Scaling (MiX), a novel format that mathematically inverts the microscaling paradigm: rather than grouping multiple mantissas under one shared exponent, MiX groups private, per-element exponents under a single shared mantissa. To handle asymmetric VLM outlier topologies, we introduce an adaptive dual-format (MiX-MX) inference framework. By algebraically factoring out the shared MiX mantissa, this framework maps to a custom accelerator, replacing multipliers with efficient shifters. Evaluated end-to-end on multiple VLMs, our 4.5-bit MiX formulation exhibits equivalent or superior accuracy on multi-modal benchmarks compared to NVFP4. Simultaneously, the MiX accelerator delivers a 25% improvement in area efficiency over the NVFP4 baseline and a 2.3-4.5x speedup with 1.4-2.9x energy reduction across models compared to the state-of-the-art accelerator Focus, proving the inverted-scaling datapath is physically superior for efficient VLM deployment.

</details>


### [19] [High-frequency Multispeculative Multiply-Accumulation Unit for Fused Posit Arithmetic](https://arxiv.org/abs/2609.19859)
*Mario Alonso,Miguel Ángel Sacristán,Guillermo Botella,Alberto A. Del Barrio*

Main category: cs.AR

TL;DR: 该论文提出面向32/64位Posit的高频Multispeculative PositMAC架构：通过流水线再平衡、采用Booth-4+Kogge-Stone加法器的高速乘法拓扑、并用Multispeculative Adder替代宽单体quire累加器，在0.5ns(2GHz)目标下实现面积最多减少19.8%、能耗降低超50%，相比64位quire方案周期时间最多缩短79.0%，且面积与每周期能耗均优于所有支持quire的对比设计。


<details>
  <summary>Details</summary>
Motivation: Posit算术相较IEEE 754浮点标准精度更高，其FMA借助quire（覆盖格式全动态范围的宽定点累加器）避免中间舍入，可实现精确可复现的数值结果，尤其利于长累加场景。但quire累加器面积极大、功耗高，成为PositMAC的硬件瓶颈，限制了其在加速器与高能效计算中的落地。

Method: 1) 重构流水线以平衡各级延迟，提升工作频率；2) 评估多种高速乘法拓扑，选定Booth-4编码配合Kogge-Stone加法器以满足0.5ns（2GHz）时序目标；3) 用Multispeculative Adder替换原本宽单体quire累加器，在保留精确累加语义前提下削减面积与能耗；设计同时支持32位与64位Posit格式。

Result: 相较基线，Multispeculative Adder方案面积最多减少19.8%，能耗降低超过50%；与同类最新设计相比取得最高工作频率，相对支持quire的64位方案周期时间最多缩短79.0%；且在不增加资源开销的前提下，面积严格更小、每周期能耗低于所有支持quire的对照设计。

Conclusion: 该工作表明，用Multispeculative Adder替代宽单体quire累加器可在基本保留精确累加与可复现性的同时，显著缓解PositMAC的面积与能耗瓶颈，配合流水线再平衡与高速乘法拓扑可获得最高频率与更低周期时间；这为Posit算术在训练/推理加速器中的高能效、高频实现提供了可行路径，后续可关注推测加法器的错误检测与恢复机制及其在更大规模MAC阵列中的可扩展性与端到端吞吐、能效收益。

Abstract: Posit arithmetic offers a compelling alternative to the IEEE 754 floating-point standard, providing enhanced accuracy. Its fused multiply-accumulate operations avoid intermediate rounding, ensuring exact numerical reproducibility through the quire, a wide fixed-point accumulator spanning the format's full dynamic range to prevent precision loss and overflow during long accumulations. However, integrating such large accumulators incurs significant area and power overheads. This paper presents an optimized, high-frequency Multispeculative PositMAC architecture for 32- and 64-bits Posit. First, the pipeline is restructured to balance the different stages. Second, high-speed multiplication topologies are evaluated, showing that a Booth-4 scheme with Kogge-Stone adders meets a stringent 0.5ns target (2Ghz). Finally, the wide monolithic quire accumulator is replaced with a Multispeculative Adder, diminishing area up to 19.8\% while reducing energy consumption by more than 50\% when compared to the baseline. Compared to other state-of-the-art designs, our proposal achieves the highest operating frequency and reduces cycle time by up to 79.0\% with respect to 64-bit quire-enabled alternatives. This performance is attained without increasing resource overhead, as the design remains strictly smaller in area and achieves lower per-cycle energy consumption than all quire-capable counterparts.

</details>


### [20] [Evaluation of Power-Clock Waveforms for Positive Feedback Adiabatic Logic in 16 nm FinFET Technology](https://arxiv.org/abs/2609.19998)
*Maciej Szymon Pyrzowski,Franciszek Łukowski,Aida Todri-Sanial*

Main category: cs.AR

TL;DR: 该工作在TSMC 16nm FinFET工艺下研究了正反馈绝热逻辑（PFAL），构建了功能完备的门库，并围绕电源时钟的幅度、频率与波形形状做能量-延迟积（EDP）优化；发现最小能量波形趋近三角形而非传统梯形，单门能量最多比静态CMOS低3.83倍，并用2:1 MUX、4位行波进位加法器与4位Brent-Kung超前进位加法器验证了库的可用性与增益。


<details>
  <summary>Details</summary>
Motivation: 绝热逻辑可通过准可逆开关回收负载电容上的部分能量，但其在FinFET工艺、多GHz频率下的波形优化运行尚缺乏系统研究；需要回答在先进工艺与高频条件下，PFAL能否实际取得相对静态CMOS的能量优势，以及电源时钟波形/幅度/频率如何共同决定该优势。

Method: 在TSMC 16nm FinFET上设计并验证功能完备的PFAL门库；以能量-延迟积（EDP）为目标，对电源时钟幅度、频率和波形形状（梯形、正弦、三角形）进行联合扫描与优化表征；随后用该库搭建2:1多路复用器、4位行波进位加法器和4位Brent-Kung超前进位加法器等组合电路，并与等效静态CMOS进行能量对比。

Result: 电源时钟扫描显示最小能量波形趋近三角形而非传统梯形；优化后的单门PFAL单元能量最多比等效静态CMOS低3.83倍；正弦激励相比梯形激励可进一步改善能量最多1.32倍，并扩大有效工作范围；在三角形电源时钟下，4位Brent-Kung超前进位加法器相对静态CMOS能量估算的增益最高达5.3倍。

Conclusion: 该工作表明在16nm FinFET与多GHz频段下，PFAL的能量收益高度依赖电源时钟波形与工作点选择，且波形优化（三角/正弦）与更高层次的电路结构（CLA）可放大收益；为绝热逻辑在先进工艺中的实用化提供了门级到模块级的量化设计依据。

Abstract: Adiabatic logic can recover part of the energy stored on load capacitances through quasi-reversible switching, but its waveform-optimized operation in FinFET technology and at multi-GHz frequencies remains underexplored. This work investigates Positive Feedback Adiabatic Logic (PFAL) in the TSMC 16 nm FinFET process. A functionally complete PFAL gate library is designed, verified, and characterised using energy-delay product optimization over power-clock amplitude, frequency, and waveform shape. The power-clock sweep shows that the minimum-energy waveform approaches a triangular shape rather than a conventional trapezoid. The optimized single-gate PFAL cells achieve up to 3.83x lower energy than equivalent static CMOS gates, while sinusoidal excitation improves energy by up to 1.32x compared with trapezoidal excitation and extends the valid operating range. The library is then used to construct larger combinational circuits, including a 2:1 multiplexer, a 4-bit ripple carry adder, and a 4-bit Brent-Kung carry look-ahead adder. The carry look-ahead adder reaches a gain of up to 5.3x compared with the static CMOS energy estimate for the triangular power-clock.

</details>


### [21] [Evaluating Positive Feedback Adiabatic Logic in 16nm FinFET with a Realistic Power-Clock](https://arxiv.org/abs/2609.19999)
*Franciszek Łukowski,Maciej Pyrzowski,Aida Todri-Sanial*

Main category: cs.AR

TL;DR: 对TSMC 16nm FinFET工艺下的正反馈绝热逻辑(PFAL)进行系统评估，量化非绝热损耗来源、EDP与能耗增益，并验证四相功率时钟生成与实际负载下的可行性。


<details>
  <summary>Details</summary>
Motivation: 绝热逻辑通过准可逆开关复用负载电容能量，理论上最小能耗低于静态CMOS，但其在FinFET工艺与多GHz时钟下的实用性尚缺乏验证，缺少面向设计的评估数据。

Method: 在TSMC 16nm FinFET下仿真PFAL标准单元（含低阈值Buffer/NOT）、2×2乘法器与4位比较器，与静态CMOS对比EDP和能耗优势η=E_CMOS/E_PFAL；通过瞬态仿真识别非绝热损耗源；设计并联耦合正交压控振荡器(PC-QVCO)作为四相功率时钟并进行非理想供电与扇出负载研究。

Result: 识别出三类非绝热损耗：PMOS/NMOS锁存相关的阈值电压损耗与输出节点冗余充电（此前未报道），以及PFAL逻辑树复杂度相关损耗。低阈值Buffer/NOT在V_CLK=0.6V、f_CLK=7.94GHz下最小EDP为1.23×10⁻²⁶ J·s；降频与升压下相对静态CMOS能耗优势最高约5×。采用PC-QVCO非理想供电时，3GHz下Buffer/NOT能耗与理想正弦供电相差2%以内；负载研究量化了扇出引起的相位偏移与幅度衰减。

Conclusion: 该工作给出PFAL在16nm FinFET中面向设计的系统评估，表明其在低频/高电压区间具备显著能效优势，并为绝热逻辑用于未来低功耗系统架构（尤其是低功耗加速器与近阈值计算）提供依据。

Abstract: Adiabatic logic reuses the energy stored on load capacitances through quasi-reversible switching, enabling a lower minimum energy consumption than conventional static CMOS. Yet its practicality in FinFET technologies and at multi-GHz clock rates has yet to be investigated. This work provides a systematic evaluation of Positive Feedback Adiabatic Logic (PFAL) simulated in the TSMC 16nm FinFET process. A set of PFAL standard-cell gates were realised, along with two representative combinational circuits - a 2$\times$2 multiplier and a 4-bit comparator - and compared against static CMOS logic using the energy--delay product (EDP) and the energy advantage metric $η= E_{\mathrm{CMOS}} / E_{\mathrm{PFAL}}$. Transient simulations reveal three sources of non-adiabatic loss: two specific to the PMOS/NMOS latch, threshold-voltage-related loss and a previously unreported redundant charging of the output node and one related to the complexity of PFAL logic trees. The low-threshold Buffer/NOT cell achieves a minimum EDP of $1.23\times10^{-26}$J$\cdot$s at $V_{\mathrm{CLK}} = 0.6$V and $f_{\mathrm{CLK}} = 7.94$GHz, while PFAL preserves an energy benefit over static CMOS of up to roughly $5\times$ at reduced frequencies and elevated supply voltages. A parallel-coupled quadrature voltage-controlled oscillator is designed as a realistic four-phase power-clock generator. With this non-ideal supply, the Buffer/NOT energy stays within $2\%$ of the ideal sinusoidal case at $3$GHz. A loading study quantifies the phase shift and amplitude reduction induced by increasing fan-out. Overall, the results provide a design-oriented evaluation of PFAL in 16nm FinFET and a motivation to exploit adiabatic logic for future low-power system architectures.

</details>


### [22] [Spectral Signatures for Parametric Fault Detection in Flexible Electronics](https://arxiv.org/abs/2609.20315)
*Paula Carolina Lozano Duarte,Sule Ozev,Mehdi Tahoori*

Main category: cs.AR

TL;DR: 该工作面向IGZO单极性柔性TFT的模拟混合信号(AMS)电路，提出频域测试签名电路：用VCO把故障引起的控制电压偏移转成频率偏移，经轻量模拟滤波后取谐波RMS作为紧凑签名，配合比率法(ratiometric)抑制温漂与电源漂移，仅需两个I/O引脚即可在无ATE条件下现场测试。面积6390 μm²、功耗16.7 μW，较紧凑ADC面积降26×、较SAR降约1500×。


<details>
  <summary>Details</summary>
Motivation: 柔性电子(IGZO TFT)适合可穿戴传感、生物医学监测与人机接口，但其AMS模块面临多重测试难题：低成本单极性工艺带来的器件变异大；无刚性封装易受制造后失效影响；参数性故障只降精度不产生硬数字失效；专用于一次性柔性系统的ATE昂贵且现场不可达。传统基于ADC的测试方案在资源受限系统中面积开销过高，且ADC自身精度受PVT变化影响，会导致测试结论失效。

Method: 提出频域测试签名电路：VCO将故障导致的控制电压偏差转换为频率偏移，再经轻量级模拟滤波器处理；以谐波分量的RMS值作为紧凑签名，该签名对工作点偏差具有单调灵敏度，并在PVT角下保持序关系，因而不需要精密参考或匹配比较器。引入比率式(ratiometric)精修方法抑制校准后的温度与电源漂移。签名通过两个I/O引脚读出，支持无ATE的现场测试。在IGZO单极性TFT工艺上实现并测量面积与功耗。

Result: 电路面积6390 μm²，功耗16.7 μW；相比紧凑型ADC实现26×面积缩减，相比SAR实现接近1500×面积缩减。比率式精修将校准后的温度与电源漂移抑制最多一个数量级。签名在PVT角下保持单调性与排序一致性，无需精密参考或匹配比较器即可稳健工作，并能检测AMS被测电路中的故障与参数偏差。

Conclusion: 该工作把AMS电路测试从"ADC+精密参考"路径转为"VCO频域签名+比率法"路径，验证了在资源极度受限、无ATE可达的一次性柔性电子场景中，以极低面积/功耗代价实现内建自测与现场测试的可行性，为柔性TFT系统的可测性设计(DfT)与可靠性/良率筛选提供了可落地的轻量方案。

Abstract: Flexible electronics (FE) based on indium gallium zinc oxide (IGZO) unipolar thin-film transistor (TFT) technologies enable lightweight, conformable systems for wearable sensing, biomedical monitoring, and human-machine interfaces. These applications rely on analog and mixed-signal (AMS) blocks that must remain accurate despite key challenges: increased device variability from low-cost unipolar fabrication; susceptibility to post-manufacturing failures due to the absence of rigid packaging; parametric faults that degrade analog accuracy without hard digital failures; and the high cost and in-field inaccessibility of specialized Automatic Test Equipment (ATE) for disposable FE systems. Conventional test approaches relying on analog-to-digital converters (ADCs) incur prohibitive area overhead in resource-constrained systems; critically, ADC accuracy is itself subject to process, voltage, and temperature (PVT) variations, leading to test invalidations. This work proposes a frequency-domain test signature circuit exploiting spectral energy from a voltage-controlled oscillator (VCO) to detect faults and parametric deviations in AMS circuits under test. The VCO converts fault-induced control-voltage deviations into frequency shifts processed through lightweight analog filters. RMS values of harmonic components serve as compact signatures with monotonic sensitivity to operating-point deviations while preserving ordering across PVT corners, ensuring robust operation without precision references or matched comparators. A ratiometric refinement suppresses post-calibration drift from temperature and supply variation by up to an order of magnitude. The signature is readable via two I/O pins, enabling in-field test without ATE. The circuit occupies 6390 $μ$m$^2$ and consumes 16.7 $μ$W, achieving 26$\times$ area reduction versus compact ADCs and nearly 1500$\times$ versus SAR implementations.

</details>


<div id='cs.PF'></div>

# cs.PF [[Back]](#toc)

### [23] [Rosetta: Automating First-Principles Performance Modeling Using Multi-Agent LLMs](https://arxiv.org/abs/2609.19376)
*Karthikeyan Sankaralingam*

Main category: cs.PF

TL;DR: Rosetta 是一个多智能体 LLM 流水线，仅以研究论文 PDF 为输入，即可自动产出第一性原理的解析性能模型（数学规范、可执行 Python 模型、自然语言解释），全过程零人工干预；在 12 篇经典论文、97 篇未筛选 ISCA 2025/HPCA 2026 论文和 6 个活跃课题组自评三条轨道上验证了其规范质量与洞察价值。


<details>
  <summary>Details</summary>
Motivation: 解析性能模型（从硬件参数推导吞吐/加速比）能让架构论文的结论可独立验证并暴露瓶颈约束，但手工构建需专家数周工作量，因此架构论文中很少附带此类模型。作者希望把这一"形式化"过程自动化，因为形式化本身能揭示隐含假设、识别缺失参数。

Method: Rosetta 采用多智能体 LLM 流水线，针对朴素 LLM 生成的失败模式设计了四项机制：(1) 科学宪章，禁止循环论证；(2) 带独立 critic 智能体的验证-修复循环；(3) 双重验证，将功能正确性与科学有效性分开评估；(4) best-of-N 集成，利用 LLM 的随机性。支持摘要/引言等论文内容的解析。

Result: CS1：12 篇里程碑论文中 10 篇规范质量达 4–5/5，无显著幻觉；CS2：97 篇未筛选的 ISCA 2025 / HPCA 2026 论文中，通过拟合筛选的论文有 56% 达到 Tier A 洞察质量；CS3：6 个活跃课题组中，Rosetta 输出促使其修改论断并开展新实验，6 位作者评估者中有 5 位表示会再次使用。

Conclusion: 自动化的第一性原理解析建模已具备实用可行性：LLM 多智能体 + 验证机制可在无人干预下产出可信的解析性能模型，并在真实投稿流程中产生可观测影响（修正论断、触发新实验）。

Abstract: Analytical performance models --- derivations of throughput or speedup from hardware parameters --- make claims independently verifiable and expose binding constraints, yet rarely accompany architecture papers because building one by hand takes weeks of expert effort. We present Rosetta, a multi-agent LLM pipeline that automatically generates first-principles analytical models from research paper PDFs. Given a paper as sole input, Rosetta produces a mathematical specification, an executable Python model, and a plain-English interpretation --- all autonomously, with zero human intervention. The formalization process itself is the primary value: it surfaces implicit assumptions and identifies missing parameters. Four design decisions address failure modes of naïve LLM-based generation: a scientific constitution that prohibits circular reasoning, verify-repair loops with independent critic agents, dual verification separating functional correctness from scientific validity, and a best-of-$N$ ensemble that exploits LLM stochasticity.
  We evaluate Rosetta across three complementary tracks: expert evaluation of 12 landmark papers (CS1), automated scoring of 97 unfiltered ISCA 2025 and HPCA 2026 papers (CS2), and author self-evaluation by six active research groups (CS3). Across CS1, specification quality scores 4--5/5 on 10 of 12 papers with zero significant hallucinations; across CS2, 56\% of fit-screened papers reach Tier~A insight quality. The strongest finding comes from CS3: Rosetta's output led to revised claims and new experiments in active submissions, and five of six author-evaluators said they would use it again.

</details>


### [24] [PrefixBench-H100: Characterizing Prefix Reuse and Time-to-First-Token in H100 LLM Serving](https://arxiv.org/abs/2609.19657)
*Omkar Shewale,Deepak Kumar,Divakar Kumar Yadav*

Main category: cs.PF

TL;DR: PrefixBench-H100 是一个面向单张 NVIDIA H100 的可复现基准与测量框架，用于刻画 LLM 服务中前缀复用（prefix reuse / KV-cache 复用）的实际收益边界。它不提出新缓存算法，而是在匹配负载下对比两个主流服务运行时（vLLM、TensorRT-LLM），系统扫描共享前缀长度、后缀多样性、到达模式、并发度、输出长度与缓存配置，并采集 TTFT、ITL、E2E 时延、吞吐、缓存命中率、显存占用与 profiling 轨迹。核心结论：前缀复用存在明确的收益区间与"缓存压力侵蚀收益"的边界；缓存有效性对并发度和输出长度基本不敏感，而残余的跨运行时差异主要来自缓存之上的调度层。


<details>
  <summary>Details</summary>
Motivation: 重复前缀在 LLM 服务负载中日益普遍：system prompt、模板化 RAG 流水线、agent 框架、多轮对话都天然产生共享前缀。vLLM 的 Automatic Prefix Caching、TensorRT-LLM 的 KV-cache 复用机制已在生产运行时中可用，但工程实践中缺少一个统一的、可复现的度量来回答关键问题：前缀复用究竟在什么条件下显著改善服务性能，又在什么条件下其收益被调度粒度、缓存块大小、并发竞争或显存压力所吞没。现有工作多聚焦于提出新算法（如 radix tree、块级哈希、缓存淘汰策略），却较少在当代加速器（H100）上、在可控且匹配的负载条件下，对"收益包络（operating envelope）"做系统性测量。缺少这类基线，容量规划、SLO 设定与运行时选型就缺乏可依据的量化边界。

Method: 方法上是一个测量学（measurement）与基准构建工作，而非算法创新。硬件固定在单张 NVIDIA H100，保证可比性与可复现性。负载层面采用两类互补的组合：(1) 受控合成 trace，用于精确解耦各变量；(2) chat-style 与 retrieval-style 的真实形态工作负载。受控变量包括：共享前缀长度、后缀多样性（决定命中率上界）、请求到达模式（bursty vs. Poisson 式）、并发度、输出长度、缓存配置（块大小、显存预算）。被测对象为两个广泛使用的 LLM 服务运行时，在匹配（matched）的负载条件下横向对比，以剥离实现差异。指标覆盖完整时延链路与资源维度：TTFT、inter-token latency、端到端时延、吞吐、cache-hit 统计、GPU 显存占用，并辅以选择性 profiling trace，用于把观测到的差异归因到具体层级（缓存层 vs. 调度层）。

Result: 主要发现有三点。第一，存在明确的双区间结构：在共享前缀长、后缀多样性适中的区间，前缀复用带来显著的首 token 时延下降；当缓存压力升高（显存受限、命中被驱逐）时，这部分收益被侵蚀，形成可界定的收益边界。第二，缓存有效性（命中率/收益相对水平）本身对并发度和输出长度基本不敏感——即缓存层的行为相对稳健，收益变化更多由前缀长度与后缀多样性驱动，而非由并发与生成长度驱动。第三，跨运行时存在的残余性能差异并不位于缓存层，而是出现在缓存之上的调度层，即批处理策略、请求编排与 KV 块分配/回收时机的差异。

Conclusion: 该工作不引入新的缓存算法，其贡献是提供一个可复现的基准与测量方法论，把前缀复用的收益从"定性预期"转化为"可量化的运行包络"。工程含义明确：前缀复用不是无条件有效的优化，其收益由前缀长度/后缀多样性决定上界，由显存压力决定是否被侵蚀，而并发与输出长度对缓存有效性影响有限。因此优化重点应分层——缓存层负责命中率与块粒度效率，调度层（批次构造、请求排序、KV 块生命周期管理）才是跨运行时差异与尾部时延的主要来源。对部署实践而言，它可用于 SLO 与容量规划、运行时选型、缓存显存预算与块大小的调参依据，并提示在多租户高并发场景下应优先优化调度与显存管理，而非继续追逐缓存算法本身。

Abstract: Repeated prompt prefixes are increasingly common in LLM serving workloads, appearing in system prompts, templated retrieval-augmented generation pipelines, agent frameworks, and multi-turn conversations. Modern inference runtimes such as vLLM and TensorRT-LLM provide mechanisms for reusing previously computed KV-cache state across requests, yet it remains unclear when prefix reuse materially improves serving performance on contemporary accelerators and when its benefits are limited by scheduling, cache granularity, concurrency, or memory pressure.
  This paper presents PrefixBench-H100, a reproducible benchmark and measurement framework for characterizing prefix reuse on a single NVIDIA H100. PrefixBench-H100 combines controlled synthetic traces with chat-style and retrieval-style workloads, and evaluates two widely used LLM serving runtimes under matched workload conditions. The benchmark varies shared-prefix length, suffix diversity, request arrival pattern, concurrency, output length, and cache configuration, while collecting time-to-first-token, inter-token latency, end-to-end latency, throughput, cache-hit statistics, GPU memory usage, and selected profiling traces.
  The goal of PrefixBench-H100 is not to introduce a new caching algorithm, but to expose the practical operating envelope of prefix reuse for H100-class LLM serving. The study identifies the regime where prefix reuse provides substantial first-token latency reductions and the regime where cache pressure erodes them, while showing that cache effectiveness itself is largely insensitive to concurrency and output length; the cross-runtime differences that remain arise above the cache, in the scheduling layer.

</details>


### [25] [Whittle index approach to multi-server scheduling with convex delay costs and impatient customers](https://arxiv.org/abs/2609.19792)
*Samuli Aalto*

Main category: cs.PF

TL;DR: 该论文研究带凸延迟成本与指数弃单（impatience）的多类 M/G/N+M 队列动态调度问题，借助 Whittle 指数方法构造启发式策略。核心理论贡献是证明对应离散时间"closed"版本的可索引性（indexability），并给出 Whittle 指数的显式表达式，从而为原始连续时间调度问题导出 Whittle 指数策略。


<details>
  <summary>Details</summary>
Motivation: 多类 M/G/N+M 队列中的动态调度（谁先被服务）在凸延迟成本下是典型的难解问题：状态空间大、连续时间、顾客会因等待过久而流失。经典 cµ 类规则无法覆盖异构延迟成本与弃单行为。作者利用"指数悔时间"这一假设来复用已有 patient-customer 的结果（Queueing Systems, Vol. 110, Art. 2, 2026），把棘手问题归约为可分析的形式，动机是为缺乏最优解的排队控制问题提供有理论支撑的、可计算的启发式。

Method: 1) 模型：多类 M/G/N+M，指数服务/到达、指数弃单时间、凸延迟成本。2) 方法论：Whittle 指数（restless bandit / relaxed 问题分解 + 加性拉格朗日乘子）。3) 关键技巧：指数弃单假设使子问题结构与已有 patient-customer 结果同构，可直接引用。4) 主要理论工作：证明相应离散时间 closed 版本的 indexability，并推导 Whittle 指数的显式解析式。5) 用离散时间结果构造原连续时间问题的 Whittle 指数策略（只做启发式，不主张最优性）。

Result: 论文证明了离散时间 closed 版本问题的可索引性，并给出了 Whittle 指数的显式表达；据此建立了连续时间原始问题的 Whittle 指数调度策略。摘要未给出数值实验、性能界或与最优/基线的对比数据。

Conclusion: 该工作把 Whittle 指数方法扩展到带凸延迟成本与指数弃单的多类 M/G/N+M 排队调度，主要价值在于理论可索引性证明与指数闭式解，并以此产生可实现的调度启发式。对系统工程的直接意义在于为服务/调度（如推理请求调度、SLA 加权排队）提供一个低成本、易计算、可解释的优先级打分规则；但摘要未涉及实现开销、在线估计、非指数分布鲁棒性等落地问题，实际部署价值仍需实验验证。

Abstract: We consider the dynamic scheduling problem in a multi-class M/G/N + M queue with convex delay costs and impatient customers that have exponential abandonment times. We apply the Whittle index approach to find a reasonable heuristic solution for this tricky problem. By assuming exponential abandonment times, we are able to make a very straightforward use of the results of the corresponding problem with patient customers presented in Queueing Systems, Vol. 110, Article no. 2, 2026. Our main theoretical achievements are proving that the closed version of the corresponding discrete-time problem is indexable and deriving an explicit expression for the Whittle index. The discrete-time results are utilized to develop the Whittle index policy for the original continuous-time scheduling problem.

</details>


### [26] [Efficiently Distributed Federated Learning](https://arxiv.org/abs/2609.19972)
*Gianluca Mittone,Robert Birke,Marco Aldinucci*

Main category: cs.PF

TL;DR: 该论文提出开源联邦学习框架 FastFederatedLearning (FFL)，用 C/C++ 实现以获得高性能，并支持用户自定义客户端与服务器之间的任意通信图；在 x86-64、ARM-v8、RISC-V 三类平台上相比 Intel OpenFL 取得 2.5x–3.69x 的稳定加速。未来计划包括 Python 接口、多通信后端中间件以及动态联邦。


<details>
  <summary>Details</summary>
Motivation: 现有联邦学习框架大多忽视机器学习的两个关键软件属性：可定制性（customizability）与性能（performance）。前者体现在难以灵活指定参与者之间的通信拓扑，后者则是脚本语言/高层实现带来的运行时开销，导致训练与推理效率低、跨异构硬件部署受限。作者希望构建一个既能灵活描述联邦拓扑、又能贴近底层高性能实现的框架。

Method: 采用 C/C++ 从底层实现开源框架 FFL，以代码性能为核心设计目标；框架允许用户显式指定联邦中客户端与服务器之间的任意通信图（communication graph），从而把拓扑结构作为一等可配置对象。评测上以 Intel OpenFL 为基线，在 x86-64、ARM-v8、RISC-V 三种计算平台上进行对比测试。论文同时给出后续路线：封装 Python 接口以降低使用门槛、实现可插拔的多通信后端中间件、支持客户端-服务器关系非静态的动态联邦。

Result: 在不同计算平台（x86-64、ARM-v8、RISC-V）上，FFL 相对 Intel OpenFL 取得一致的加速，加速比区间为 2.5x 至 3.69x。该结果表明在跨指令集架构的异构环境中，原生编译实现相比高层框架具有稳定的性能优势。

Conclusion: FFL 验证了在联邦学习场景中以原生 C/C++ 实现换取性能、以可编程通信图换取可定制性的可行性，并具备跨 x86-64/ARM-v8/RISC-V 的可移植性。后续工作将补齐 Python 易用性、通信后端抽象以及动态演化的联邦即服务能力，使联邦从静态结构演变为长期演化的可复用服务。

Abstract: Federated Learning (FL) is experiencing a substantial research interest, with many frameworks being developed to allow practitioners to build federations easily and quickly. Most of these efforts do not consider two main aspects that are key to Machine Learning (ML) software: customizability and performance. This research addresses these issues by implementing an open-source FL framework named FastFederatedLearning (FFL). FFL is implemented in C/C++, focusing on code performance, and allows the user to specify any communication graph between clients and servers involved in the federation, ensuring customizability. FFL is tested against Intel OpenFL, achieving consistent speedups over different computational platforms (x86-64, ARM-v8, RISC-V), ranging from 2.5x and 3.69x. We aim to wrap FFL with a Python interface to ease its use and implement a middleware for different communication backends to be used. We aim to build dynamic federations in which relations between clients and servers are not static, giving life to an environment where federations can be seen as long-time evolving structures and exploited as services.

</details>


### [27] [Scaling Fourier-Based Sparse Matrix Analysis on GPUs](https://arxiv.org/abs/2609.20483)
*Ruifeng Zhang,Sai Krishna Teja Varma Manthena,Jiajia Li,Xipeng Shen*

Main category: cs.PF

TL;DR: 该论文提出 Binary-Sparse FFT（BS-FFT）及其两种压缩变体，用稀疏感知的 FFT 管线在 GPU 上提取超大规模稀疏矩阵（如图邻接矩阵）的谱签名，在精度可控的前提下大幅降低显存与计算开销。


<details>
  <summary>Details</summary>
Motivation: 稀疏计算的性能高度依赖稀疏模式（影响访存合并、块组织与负载均衡），而谱签名可用于分析矩阵全局结构。但大规模图邻接矩阵极其庞大稀疏，现有基于稠密矩阵的 GPU FFT（如 cuFFT）难以扩展，无法有效获得其谱模式。论文给出两个量化的动机点：(1) 稠密 cuFFT 在 40 GB A100 上仅能完成 15 个 GNN 邻接矩阵中的 6 个；(2) 谱分析对精度的容忍度较高（后续变体误差 0.16%–11.56% 仍可用），因此存在大规模压缩空间。

Method: 三层递进方法：(1) BS-FFT：无损的 Binary-Sparse FFT，直接在 GPU 上针对稀疏结构（无稀疏索引、以二进制稀疏表示）执行 FFT，避免稠密化，降低显存占用。(2) Elastic BS-FFT：在采样频率网格上复用 BS-FFT 管线，用部分频率采样近似完整谱，牺牲可控精度换计算量。(3) Density-Map-based spatial compression：基于密度图的空间压缩，进一步减少参与计算的元素。三者构成无损基准 + 频率域采样 + 空间域压缩的组合。

Result: (1) 显存：BS-FFT 相比稠密 cuFFT 降低 2.9–11.6 倍；在 40 GB A100 上完成全部 15 个 GNN 邻接矩阵，而稠密 cuFFT 仅完成 6 个。(2) 计算：Elastic BS-FFT 与 Density Map 压缩相比 BS-FFT 减少 GPU 计算时间 2.0–1466.4 倍。(3) 精度：在 6.25%–0.0061% 采样率范围内，谱特征误差仅 0.16%–11.56%。

Conclusion: 该工作表明，面向稀疏结构的 FFT 比稠密 FFT 更适合大规模图稀疏矩阵的谱分析，且频率域采样与空间域密度压缩可在极小谱误差下带来数量级的计算节省。对工程实践而言，它为 GNN 图预处理（稀疏模式分析、图重排/分区、负载均衡决策）提供了一条可扩展的 GPU 谱分析路径：以“可容忍精度损失换取可扩展性”的设计换取全流程可行性。局限性在于压缩变体的精度上界可达约 11.6%，且效果依赖采样率选择与矩阵密度分布，需按场景调参。后续价值在于把谱签名接入 GNN 训练/推理的性能调优闭环（如按谱分块、稀疏张量核心调度、图划分）。

Abstract: Sparse computations are important workloads in applications such as scientific computing, graph neural networks (GNNs), and machine learning. While many sparse operations can benefit from modern GPUs, the sparsity pattern remains important to performance because it affects memory coalescing, block organization, and load balancing. Previous studies show that spectral signatures can help analyze the global structure of sparse matrices. The fast Fourier transform (FFT) is commonly used to extract spectral signatures, and efficient GPU FFT libraries are available. However, sparse matrices, especially adjacency matrices for large graphs, tend to be very large and sparse. Existing dense-matrix-based FFT implementations are difficult to scale up, making the spectral patterns of these matrices difficult to obtain. We therefore propose a three-fold research approach comprising a lossless Binary-Sparse FFT (BS-FFT) and two compression methods: Elastic BS-FFT, which reuses the BS-FFT pipeline on a sampled frequency grid, and density-map-based spatial compression. Experiments show that BS-FFT reduces GPU memory use by 2.9--11.6 times relative to dense cuFFT and completes all 15 GNN adjacency matrices where dense cuFFT completes 6 on a 40 GB A100. Elastic BS-FFT and Density Map compression reduce GPU computation time by 2.0--1466.4 times relative to BS-FFT with spectral feature error of only 0.16% to 11.56% across the sampling rates from 6.25% to 0.0061%.

</details>


<div id='cs.NI'></div>

# cs.NI [[Back]](#toc)

### [28] [PyStream: Enhancing Video Streaming Evaluation](https://arxiv.org/abs/2609.19823)
*Samuel Radler,Leon Prüller,Emanuele Artioli,Farzad Tashtarian,Christian Timmerer*

Main category: cs.NI

TL;DR: PyStream 提出一种基于 Docker 容器网络的多路视频流带宽仿真方法，能在单机本地并行运行多个播放器测试，并以约 2-3 倍于 Wondershaper 的精度复现真实网络 trace，同时降低相对云方案（CAdViSE）的实验成本。


<details>
  <summary>Details</summary>
Motivation: 流媒体服务日益普及，但其在不同网络条件下的行为评估非常昂贵：通常需要一台高性能本机或云环境模拟多个不同带宽配置的播放器；同时，真实网络行为仿真困难，且难以保证严格贴合给定真实网络 trace。现有工具（如 Wondershaper 流量整形、CAdViSE 云端评估环境）在精度与成本上均有不足。

Method: PyStream 通过 Docker 容器组网构建仿真网络：将播放器、网络整形与网络条件控制分别封装为容器，把底层实现细节（网络命名空间、tc 整形规则、带宽调度等）抽象掉，从而在单台机器上并行运行多个互不干扰的播放器测试。系统以容器网络拓扑映射目标网络的带宽与 trace，并保持组件可替换、易管理与易升级。

Result: 相比常用流量整形器 Wondershaper，PyStream 将原始网络 trace 与播放器实际测得带宽之间的误差平均降低约 2-3 倍，即带宽仿真精度显著提升；相比 CAdViSE 等云基视频流评估环境，PyStream 降低了实验运行成本；同时只需单机即可完成原先需强大本地机器或多个云实例才能负担的多播放器并发测试。

Conclusion: PyStream 表明，用容器化网络仿真替代传统流量整形或云端编排，可在降低硬件与成本门槛的同时提升带宽仿真保真度，为视频流系统（尤其是 ABR 算法与播放器行为）的可复现评测提供了一种轻量、可扩展且易维护的工程方案。

Abstract: As streaming services become more commonplace, analyzing their behavior effectively under different network conditions is crucial. This is normally quite expensive, requiring multiple players with different bandwidth configurations to be emulated by a powerful local machine or a cloud environment. Furthermore, emulating a realistic network behavior or guaranteeing adherence to a real network trace is challenging. This paper presents PyStream, a simple yet powerful way to emulate a video streaming network, allowing multiple simultaneous tests to run locally. By leveraging a network of Docker containers, many of the implementation challenges are abstracted away, keeping the resulting system easily manageable and upgradeable. We demonstrate how PyStream not only reduces the requirements for testing a video streaming system but also improves the accuracy of the emulations with respect to the current state-of-the-art. On average, PyStream reduces the error between the original network trace and the bandwidth emulated by video players by a factor of 2-3 compared to Wondershaper, a common network traffic shaper in many video streaming evaluation environments. Moreover, PyStream decreases the cost of running experiments compared to existing cloud-based video streaming evaluation environments such as CAdViSE.

</details>


### [29] [Application-Integrated Slicing towards 6G: The Musical Metaverse Use Case](https://arxiv.org/abs/2609.20163)
*Ali Al Housseini,Jaime Llorca,Omran Ayoub,Cristina Rottondi,Luca Turchet,Francesco Malandrino*

Main category: cs.NI

TL;DR: 提出"应用集成切片"(application-integrated slicing)框架,将应用编排与网络编排统一到同一端到端服务模型中,使单个逻辑切片内可对多个用户类别设定差异化KPI目标;以"音乐元宇宙"(MM)为代表性场景,在云-边网络上验证,相比传统解耦方案可同时降低资源供给成本与QoS违规率。


<details>
  <summary>Details</summary>
Motivation: 沉浸式应用(如音乐元宇宙)需同时服务通信与计算需求差异极大的异构用户群(如高保真演出用户、普通观众、内容创作者)。当前5G网络切片与编排框架仍以"服务为中心",应用管理与网络管理机制相互解耦:QoS与切片技术只能做流量区分,无法联合考虑应用语义、共享资源,以及同一服务实例内多用户类别的差异化端到端需求。这导致资源过度供给与QoS违规并存。

Method: 提出application-integrated slicing:在统一的端到端服务模型中同时表达应用层语义与网络层资源,把应用编排与网络编排协同进行,并允许在单一逻辑切片内为多个用户类别分别设定KPI目标(差异化SLA)。以MM参考场景在云-边网络上进行部署与评估,对比传统"应用/网络解耦"的编排方式。

Result: 在MM云-边部署场景下,应用集成切片相比传统解耦方案同时降低资源供给成本和QoS违规率,说明联合优化可在不牺牲(甚至提升)服务质量的前提下减少资源开销。

Conclusion: 多用户类别、应用语义敏感的沉浸式业务暴露出服务为中心、应用-网络解耦的切片框架的不足;将应用与网络编排统一到单一端到端服务模型、并支持单切片内多KPI目标,是提升资源效率与QoS保障的有效方向。对工程实践而言,该工作提示需要在切片/编排控制面引入应用级语义与多租户类别KPI映射,为云-边推理与媒体流水线的联合调度提供接口。

Abstract: Emerging immersive applications are expected to support heterogeneous groups of users with fundamentally different communication and computation requirements. Using the Musical Metaverse (MM) as a representative example, we show how such applications expose limitations of current 5G network slicing and orchestration frameworks, which remain largely service-centric and operate through decoupled application and network management mechanisms. While existing Quality of Service (QoS) and slicing techniques provide traffic differentiation, they cannot jointly account for application semantics, shared resources, and differentiated end-to-end requirements across multiple user classes within the same service instance. To address these limitations, we introduce application-integrated slicing, a framework that unifies application and network orchestration within a common end-to-end service model and enables differentiated KPI targets for multiple user classes within a single logical slice. Using a MM reference scenario deployed over cloud-edge network, we show that application-integrated slicing reduces both resource provisioning cost and QoS violation rates compared with conventional decoupled approaches.

</details>


### [30] [STR-Agent: An LLM-Driven Agent for QoS-Aware Routing in LEO Satellite Networks](https://arxiv.org/abs/2609.20347)
*Bowen Lu,Mugen Peng,Yaohua Sun,Hongyu Wang,Kerui Guo,Wenjia Xu*

Main category: cs.NI

TL;DR: STR-Agent：LLM 驱动的 LEO 卫星网络 QoS 感知路由智能体框架，将自然语言意图理解、工具调用执行、经验积累与反思式策略自适应统一到单一 agent 架构中，实现从非结构化服务请求到自适应路由决策的端到端映射。


<details>
  <summary>Details</summary>
Motivation: LEO 星座具有动态拓扑、时变链路与多样化服务需求，传统路由难以支持细粒度 QoS。现有研究多在预定义目标下对网络状态做路由优化，缺少将非结构化自然语言服务请求转化为自适应路由决策的能力，即"意图—路由"语义鸿沟未被解决。

Method: 提出 STR-Agent 框架，统一四个环节：Perception Module 将自然语言请求转为结构化路由语义；Tool-based Execution 执行路由；Experience Accumulation 积累历史路由结果；Reflection Module 依据实时拥塞状况与历史结果动态调整"服务→路由策略"映射，而非固定路由目标。同时训练专用感知模型，并构建 LEO 服务理解领域的监督微调（SFT）数据集。

Result: 在 Walker-Delta 星座仿真中显著优于基线：相较 DQ-Dijkstra 端到端时延最多降低 60%；监督微调后意图理解平均准确率由 45.4% 提升至 92.45%；在 600 Mbps 负载下，Reflection Module 额外降低 120 ms 时延。

Conclusion: 结果表明 LLM 驱动的 agent 架构可为未来 LEO 卫星网络提供业务感知、自适应的 QoS 路由能力。

Abstract: LEO satellite networks feature dynamic topologies, time-varying links, and diverse service requirements, which make conventional routing schemes difficult to support fine-grained quality-of-service (QoS) provisioning. Existing studies mainly optimize routing over network states with predefined objectives, but rarely address the practical challenge of translating unstructured natural-language service requests into adaptive routing decisions. To bridge this gap, we propose STR-Agent, an LLM-driven framework for QoS-aware routing in LEO satellite networks. The key innovation of STR-Agent lies in unifying intent perception, tool-based execution, experience accumulation, and reflection-based policy adaptation within a single agent architecture. Specifically, the Perception Module converts natural-language requests into structured routing semantics, while the Reflection Module dynamically adjusts the service-to-routing-policy mapping according to real-time congestion conditions and historical routing outcomes, rather than relying on a fixed routing objective. In addition, we develop a specialized perception model, and construct a domain-specific supervised fine-tuning dataset for LEO service understanding. Simulation results in a Walker-Delta constellation show that STR-Agent significantly outperforms conventional baselines: it reduces end-to-end delay by up to 60% compared with DQ-Dijkstra, improves average intent-understanding accuracy from 45.4% to 92.45% after supervised fine-tuning, and the Reflection Module further reduces the delay by 120 ms at 600 Mbps. These results demonstrate the potential of LLM-driven agent architectures to enable service-aware and adaptive QoS routing in future LEO satellite networks.

</details>


### [31] [Value-Based Massive Access through Goal-Oriented Irregular Repetition Slotted ALOHA](https://arxiv.org/abs/2609.20569)
*Pietro Talli,Andrea Munari,Federico Mason,Federico Chiariotti,Andrea Zanella*

Main category: cs.NI

TL;DR: GO-IRSA 将目标导向（goal-oriented）通信用现代随机接入（IRSA）与基于信念（belief-based）的调度策略结合，面向大规模设备分布式监测场景，在不增加传感器算力负担的前提下，把分布式 Wiener 过程状态估计的平均误差与最差误差相对最优集中式方案降低超过 30%。


<details>
  <summary>Details</summary>
Motivation: 目标导向通信此前的研究集中在编码层面，介质访问（MAC）方案设计仍处于早期。既有目标导向 MAC 多依赖集中式调度或过度简化的底层机制与不现实假设，且难以扩展到成千上万设备的远程监测场景。本文的动机是在大规模随机接入（含碰撞、串行干扰消除）条件下，仍能按“任务相关性”而非“链路吞吐/可靠性”来决定谁发、何时发。

Method: 提出 GO-IRSA：把现代随机接入的 Irregular Repetition Slotted ALOHA（IRSA）与基于信念的策略结合。传感器侧按信念决策选择是否发送及重复次数（IRSA 的重复度分布），接收侧利用 IRSA 的 SIC（串行干扰消除）解码，并以分布式 Wiener 过程的后验信念（状态估计不确定性）作为调度/接入的价值信号；方案无需显著传感器算力、无需频繁反馈。

Result: 在数千传感器的网络中，GO-IRSA 使分布式 Wiener 过程估计的平均误差与最差误差比最优集中式解降低 30% 以上；对不完美干扰消除（imperfect SIC）和不准确的过程知识（模型失配）具有鲁棒性。

Conclusion: 该工作指出目标导向通信的瓶颈已从编码转向接入/MAC 层，并给出一个可扩展、低反馈、低端侧开销的随机接入+信念调度方案；可作为大规模监测/联邦采样类系统的 MAC 层设计参考，后续可向异构业务、多跳与真实 SIC 硬件约束下的部署验证延伸。

Abstract: The goal-oriented communication paradigm is poised to enable novel real-time applications by easing the burden on communication networks while still delivering task-relevant information. However, efforts so far have focused on the encoding problem, while the design of medium access schemes is still in the early stages of development, especially when connectivity is to be provided to a massive number of devices, e.g., for remote monitoring. In this respect, existing goal-oriented approaches are often centralized or based on simplified underlying mechanisms, requiring unrealistic assumptions. In this work, we present the Goal-oriented Irregular Repetition Slotted ALOHA (GO-IRSA) scheme, which combines modern random access techniques with belief-based policies. GO-IRSA does not impose significant computing loads on the sensors or require frequent feedback, and it can reduce the average and worst-case error of the estimate of a distributed Wiener process by over 30% with respect to the optimal centralized solution in a network with thousands of sensors, and is robust to imperfect interference cancellation and inaccurate process knowledge.

</details>


### [32] [NS3Learn: Transferring 5G NR Mode-2 Reception Realism from ns-3 to the Veins/SUMO Stack for Connected-Vehicle Safety Assessment](https://arxiv.org/abs/2609.20578)
*Rasheed Bello,Arthur Mukwaya,Gurcan Comert,Varghese Vaidyan,Vijay Bendigeri,Anthony Dontoh,Jagruti Sahoo,Judith Mwakalonge*

Main category: cs.NI

TL;DR: 该论文用模型蒸馏方式，从 ns-3 5G-LENA 仿真轨迹中拟合出闭式接收模型 NS3Learn，将 5G NR Sidelink Mode-2 的无线资源竞争丢包引入车联网交通安全评估，避免完整协议重实现；在密集车流下纠正了过于乐观的投递率，使仿真交通速度趋势反转、硬制动事件翻倍以上。


<details>
  <summary>Details</summary>
Motivation: 车联网安全评估依赖交通流与网络联合仿真，但标准信道模型忽略 5G NR Sidelink Mode-2 的无线资源竞争（半双工、调度碰撞、捕获、解码），在密集车流中给出不切实际的过高消息投递率，进而低估了通信受限带来的安全风险。完整复现协议栈代价高、难以嵌入既有交通仿真流水线，因此需要一种轻量、可迁移、物理可解释的丢包建模手段。

Method: 1) 用 ns-3 5G-LENA 生成轨迹，参数按 3GPP 场景标定，车辆轨迹由 SUMO 驱动；2) 标注 1050 万条接收结果，拟合 NS3Learn 闭式模型，显式涵盖半双工损失、调度碰撞、接收端捕获和译码失败四类机制；3) 在两个信号控制城市路网、6 个渗透率（1%–100%）、每种条件 5 个随机种子上评估，并与 ns-3 5G-LENA 及两个替代模型对比；4) 做跨路口参数迁移测试（仅 20% 额外误差）；5) 将真实通信模型接入交通仿真，观察速度趋势与硬制动事件的变化。

Result: NS3Learn 在瞬时投递率上与 ns-3 5G-LENA 的平均绝对偏差为 0.06，显著优于两个替代模型（0.44 与 0.55）；拟合参数迁移到不同路口仅增加 20% 误差；采用真实通信模型后，仿真交通速度趋势发生反转，预测的硬制动事件增加一倍以上。

Conclusion: 通过模型蒸馏而非完整协议重实现，可在仿真器间迁移接收侧真实性；每个建模阶段均对应明确的物理机制。研究者与交通管理部门可在不改动既有仿真流水线的前提下，准确计入密集车流下的丢包与拒绝服务影响，适配新无线配置只需离线重新拟合参数而非改代码。

Abstract: Connected-vehicle safety evaluations rely on coupled traffic and network simulations, but standard channel models ignore radio resource competition in 5G NR sidelink Mode-2, reporting unrealistically high message delivery in dense traffic. This study introduces resource-competition losses without requiring full protocol reimplementation. We labeled 10.5 million reception outcomes from ns-3 5G-LENA traces (calibrated on 3GPP scenarios and driven by SUMO trajectories) to fit NS3Learn - a closed-form model capturing half-duplex loss, scheduling collisions, receiver capture, and decoding. Evaluation spanned two signalized urban networks, six penetration levels (1-100%), and five random seeds per condition. NS3Learn achieved a mean absolute deviation of 0.06 in per-instant delivery compared to ns-3 5G-LENA, outperforming alternative models (0.44 and 0.55 deviation). Fitted parameters transferred to a distinct intersection with only 20% additional error. Crucially, using realistic communication models reversed simulated traffic speed trends and more than doubled predicted hard-braking events. The framework transfers reception realism between simulators via model distillation instead of full reimplementation. Every stage maps directly to an explicit physical mechanism. Researchers and transportation agencies can maintain existing simulation pipelines while accurately accounting for dense-traffic packet loss and denial-of-service impacts. Adapting to new radio configurations requires only offline refitting rather than code modification.

</details>


### [33] [RUN-O-RAN: An O-RAN-Native Architecture Enabling Cooperative Uplink Localization](https://arxiv.org/abs/2609.20640)
*Viola Bernazzoli,Alberto Ceresoli,Ilario Filippini*

Main category: cs.NI

TL;DR: RUN-O-RAN 是一个基于 O-RAN 架构的上行定位框架，利用商用 5G 终端原有的 SRS 信号，实现网络侧协作式定位，无需修改终端或 3GPP 信令。


<details>
  <summary>Details</summary>
Motivation: 室内与密集城市场景对高精度定位需求日益增长，但卫星定位不可用，标准化的 5G 定位方案在商用终端上难以落地。现有方案多依赖终端侧改造或非标准信令，部署门槛高。

Method: RUN-O-RAN 作为 O-RAN 原生 xApp，协调服务基站与邻接基站，使非服务基站也能获取 SRS 上行定时测量（传统 RAN 部署中无法获得）。框架整合了协作式 SRS 采集、首径到达时间（ToA）估计、定时提前（TA）跟踪、时钟漂移补偿与多锚点位置解算，构成完整的网络侧定位服务。

Result: 基于超过 150,000 次 SRS 传输的实验验证表明，该框架在多种传播条件下可实现米级定位精度，并量化了锚点几何布局与多径对定位精度的影响。

Conclusion: 协作式 SRS 定位可在 O-RAN 生态内实现，且无需改造商用终端，为未来网络原生 ISAC 定位服务提供了可落地的基础。

Abstract: Accurate positioning is increasingly required in indoor and dense urban environments; nonetheless, satellite-based systems are not always available, and standardized 5G localization solutions remain difficult to deploy with commercial devices. This paper presents RUN-O-RAN, an O-RAN-native framework that enables network-centric uplink localization using standard Sounding Reference Signal (SRS) transmissions from commercial 5G devices. RUN-O-RAN xApp coordinates serving and neighboring base stations, enabling non-serving base stations to retrieve SRS-based uplink timing measurements that would be unavailable in conventional RAN deployments, without modifying the UE or existing 3GPP signaling procedures. The framework combines cooperative SRS collection, first-path time-of-arrival estimation, timing-advance tracking, clock-drift compensation, and multi-anchor position estimation into a complete network-side localization service. Experimental evaluation over $150,000$ SRS transmissions validates the proposed framework, achieving meter-level localization under diverse propagation conditions while revealing the impact of anchor geometry and multipath on positioning accuracy. These findings demonstrate that cooperative SRS-based localization can be realized within the O-RAN ecosystem without modifying commercial UEs, providing a practical foundation for future network-native ISAC positioning services.

</details>
